import type { Question } from '../../types';

/**
 * 応用数学の確認問題。
 *
 * 書き方の約束（他の分野のファイルも同じ）。
 *   - `sectionId` は必ず付ける。解説から教本へ戻る導線と、節ごとの出題に使う
 *   - 問題文・選択肢・解説は Markdown サブセットが使える（$数式$ も可）
 *   - **通常の文字列リテラルの中ではバックスラッシュを 2 つ重ねる**こと。
 *     `'$\\sum$'` と書いて初めて画面に $\sum$ が出る（1 つだと消える）
 *   - `explanation` では正解の理由だけでなく、**誤答がなぜ誤りか**にも触れる
 *   - 実装を問う場合は `code` にコード断片を置き、空欄は `___` で示す
 */
export const mathQuestions: Question[] = [
  {
    id: 'q-linalg-1',
    categoryId: 'm-linalg',
    sectionId: 'm-linalg-1',
    question: '形が $(4, 6)$ の行列と $(6, 3)$ の行列の行列積を計算したとき、結果の形はどれか。',
    choices: ['$(4, 3)$', '$(6, 6)$', '$(4, 6)$', '計算できない'],
    answer: 0,
    explanation:
      '行列積は**内側の次元が一致していれば計算でき、その次元が消える**。$(4, 6)$ と $(6, 3)$ では内側の 6 が一致しているので計算でき、外側に残る 4 と 3 で $(4, 3)$ になる。\n\n「計算できない」を選ぶのは、一致を確認する向きを取り違えた場合。左の**列数**と右の**行数**を見比べる。',
    level: 1,
  },
  {
    id: 'q-linalg-2',
    categoryId: 'm-linalg',
    sectionId: 'm-linalg-1',
    question: '2 つのベクトルの内積について、正しい記述はどれか。',
    choices: [
      '同じ位置の成分どうしを掛けて総和を取るので、結果はスカラーになる',
      '同じ位置の成分どうしを掛けるので、結果は元と同じ長さのベクトルになる',
      '一方を転置してから掛けるので、結果は行列になる',
      '長さの異なるベクトルどうしでも、短い方を繰り返して計算できる',
    ],
    answer: 0,
    explanation:
      '内積は $\\sum_i a_i b_i$ であり、掛けたあとに**総和を取る**ため結果はスカラー 1 個になる。\n\n選択肢 2 は要素積（アダマール積）の説明。掛けるところまでは同じだが、総和を取らないので形が変わらない。**この 2 つの取り違えは頻出**なので、「総和を取るかどうか」で区別すること。',
    level: 1,
  },
  {
    id: 'q-linalg-3',
    categoryId: 'm-linalg',
    sectionId: 'm-linalg-1',
    question:
      '全結合層の順伝播を $\\mathbf{y} = \\mathbf{x}\\mathbf{W}$ と書くとき、逆伝播で手前の層へ渡す勾配は $\\partial L / \\partial \\mathbf{x}$ である。空欄に入るものはどれか。',
    code: `# x: (N, D_in)  W: (D_in, D_out)  dy: (N, D_out)
dx = dy @ ___          # dx の形は (N, D_in) でなければならない`,
    choices: ['W.T', 'W', 'x.T', 'dy.T'],
    answer: 0,
    explanation:
      '形だけで決まる。`dy` は $(N, D_{out})$、求めたい `dx` は $(N, D_{in})$ なので、右から掛ける行列は $(D_{out}, D_{in})$ でなければならない。`W` は $(D_{in}, D_{out})$ だから、転置した `W.T` がその形になる。\n\n**逆伝播の式を暗記する必要はなく、形を合わせれば復元できる。** これが転置が出てくる理由そのもの。',
    level: 2,
  },
  {
    id: 'q-linalg-4',
    categoryId: 'm-linalg',
    sectionId: 'm-linalg-1',
    question: '逆行列 $\\mathbf{A}^{-1}$ が存在しないのはどのような場合か。',
    choices: [
      '行列式が 0 のとき',
      '行列が正方行列でないとき（この場合に限る）',
      '対角成分に 0 が 1 つでも含まれるとき',
      '行列のすべての成分が正のとき',
    ],
    answer: 0,
    explanation:
      '逆行列が存在する条件は**正方行列であり、かつ行列式が 0 でない**こと。行列式が 0 の行列を特異（正則でない）という。\n\n選択肢 2 は「正方行列でなければ存在しない」こと自体は正しいが、**正方行列でも存在しない場合がある**ため「この場合に限る」が誤り。選択肢 3 は対角成分に 0 があっても行列式が 0 とは限らないので誤り。',
    level: 2,
  },
];
