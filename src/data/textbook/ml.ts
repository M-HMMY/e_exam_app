import type { TextbookSection } from '../../types';

/**
 * この章はこれから書く。書き方は math.ts と intro.ts を見本にすること。
 * 節の骨格：ざっくり言うと → 本論（図・数式・表）→ この節のまとめ → 一問一答（quiz）。
 * 「試験のポイント」「よくある勘違い」の引用ブロックは直前チェックシートに自動で集まる。
 */
export const ml: TextbookSection[] = [
  {
    id: 'ml-basic-1',
    categoryId: 'ml-basic',
    title: '学習の種類と全体像',
    goal: '教師あり・教師なし・強化学習の違いを掴み、機械学習の作業の流れを言葉にできる',
    minutes: 20,
    body: `
# ざっくり言うと

機械学習は「**データからルールを見つける**」仕組みです。人が条件分岐を書く代わりに、データに合うようにパラメータを調整させます。

分け方は 3 つだけ覚えれば足ります。**正解を教えるか（教師あり）、教えないか（教師なし）、報酬で教えるか（強化学習）**。深層学習もこの枠の中にあり、モデルが深いニューラルネットワークになっただけです。

# 3 つの学習

\`\`\`diagram:tree
title: 学習の種類
教師あり学習 :: 入力と正解の組を与える
  回帰 :: 連続値を予測する（価格、気温）
  分類 :: ラベルを予測する（迷惑メールか否か、10 クラスの数字）
教師なし学習 :: 正解のないデータの構造を見つける
  クラスタリング :: 似たもの同士をまとめる
  次元削減 :: 少ない軸で表し直す
  異常検知 :: 大多数と違うものを見つける
強化学習 :: 行動の結果として得られる報酬で学ぶ
  試行錯誤 :: 正解は与えられず、良し悪しだけが返る
\`\`\`

中間的なものとして、少量の正解つきデータと大量の正解なしデータを併用する**半教師あり学習**、データ自身から正解を作り出す**自己教師あり学習**があります。**BERT の穴埋めや GPT の次単語予測は自己教師あり学習**で、「正解を人が付けなくてよい」ことが大規模化を可能にしました。

> **試験のポイント** 「教師あり／なし」は**正解ラベルの有無**で決まります。ニューラルネットワークを使うかどうかとは無関係です。オートエンコーダは入力自身を正解に使うので、**教師なし（あるいは自己教師あり）**に分類されます。

# 作業の流れ

\`\`\`diagram:flow
title: 機械学習の一連の流れ
課題を決める :: 何を予測すれば役に立つのかを決める
データを集める :: ここに最も時間がかかる
前処理する :: 欠損・外れ値・スケール・カテゴリ変数
モデルを学習する :: 訓練データでパラメータを決める
評価する :: 検証・テストデータで汎化性能を測る
運用する :: 推論を組み込み、劣化を監視する
\`\`\`

**モデルを選ぶ工程より、データを整える工程のほうが結果に効く**というのが実務の共通認識です。試験でも前処理の用語は出題されます。

| 前処理 | 内容 | 注意 |
| --- | --- | --- |
| 欠損値処理 | 削除、平均・中央値で補完、欠損フラグを立てる | 補完に使う統計は**訓練データから**求める |
| 標準化 | 平均 0・分散 1 に揃える | 距離や勾配を使う手法では必須級 |
| 正規化（Min-Max） | 0〜1 に収める | 外れ値に弱い |
| カテゴリ変数の変換 | one-hot エンコーディング、ラベルエンコーディング | 順序のないカテゴリに番号を振ると誤った大小関係が生まれる |

> **よくある勘違い** 標準化のための平均と分散は、**訓練データだけ**から求めます。テストデータも含めて計算すると、テストの情報が学習に漏れます（**データリーク**）。この過ちは検証の設計でも繰り返し出てきます。

# 特徴量と次元の呪い

**特徴量**は、データを表す数値の並びです。深層学習は特徴量そのものを学習しますが（**表現学習**）、古典的な機械学習では人が設計します（**特徴量エンジニアリング**）。

特徴量は多ければよいわけではありません。次元が増えるほど**空間が急激に広がり、データがまばらになります**。これを**次元の呪い**といいます。

\`\`\`diagram:compare
title: 次元が増えると
actors: 起きること | 対処
データの間隔が広がり、距離が意味を失う
次元削減（PCA など）
必要なデータ量が指数的に増える
特徴量選択
過学習しやすくなる
正則化
計算量とメモリが増える
ドメイン知識で絞る
\`\`\`

# 深層学習との関係

| | 古典的な機械学習 | 深層学習 |
| --- | --- | --- |
| 特徴量 | 人が設計する | データから学習する |
| 必要なデータ量 | 比較的少なくてよい | 多いほど有利 |
| 計算資源 | CPU でも足りることが多い | GPU が前提 |
| 解釈性 | 高いものが多い（決定木、線形モデル） | 低い |
| 得意な領域 | 表形式データ | 画像・音声・言語 |

**表形式データでは、いまも勾配ブースティング木が強く、深層学習と互角以上の結果を出すことが多い**とされます。「深層学習が常に最良」ではないことは、実務でも試験でも押さえておく価値があります。

# この節のまとめ

- 学習は「正解を教える（教師あり）／教えない（教師なし）／報酬で教える（強化学習）」の 3 つに大別できる
- 教師あり／なしの区別は**正解ラベルの有無**で決まり、モデルの種類とは無関係
- 自己教師あり学習はデータ自身から正解を作る方式で、BERT や GPT の事前学習がこれにあたる
- 前処理の統計量（平均・分散など）は**訓練データだけ**から求める。さもないとデータリークになる
- 次元が増えるとデータがまばらになり、必要なデータ量も過学習の危険も増える（次元の呪い）
- 深層学習は特徴量を自分で学ぶが、表形式データでは勾配ブースティング木が今も強い

\`\`\`quiz
教師あり学習と教師なし学習は何で区別しますか :: 正解ラベルがあるかどうかです。使うモデルの種類とは関係ありません。
オートエンコーダはどちらに分類されますか :: 入力自身を正解として使うので、教師なし（自己教師あり）に分類されます。
自己教師あり学習の代表例を挙げてください :: BERT の穴埋め（マスク言語モデル）や GPT の次単語予測です。人が正解を付ける必要がありません。
標準化に使う平均と分散はどのデータから求めますか :: 訓練データだけからです。テストデータを含めるとデータリークになります。
次元の呪いとは何ですか :: 次元が増えるほど空間が広がってデータがまばらになり、必要なデータ量も過学習の危険も増える現象です。
表形式データでは深層学習が常に最良ですか :: いいえ。勾配ブースティング木が互角以上の性能を出すことが多い領域です。
\`\`\`
`,
  },
  {
    id: 'ml-basic-2',
    categoryId: 'ml-basic',
    title: '線形回帰とロジスティック回帰',
    goal: '2 つの基本モデルを「損失関数の違い」として理解し、ロジスティック回帰が分類である理由を説明できる',
    minutes: 25,
    body: `
# ざっくり言うと

線形回帰は**直線（超平面）を当てはめる**手法、ロジスティック回帰は**その直線を確率に変換して分類する**手法です。

名前が紛らわしいのですが、**ロジスティック回帰は分類の手法**です。そして重要なのは、この 2 つが**ニューラルネットワークの最小構成そのもの**だということです。隠れ層のない 1 層のネットワークで、出力に何を置くかだけが違います。

\`\`\`diagram:compare
title: 2 つは同じ枠組みの中にある
actors: 線形回帰 | ロジスティック回帰
出力層に活性化を置かない
出力層にシグモイドを置く
仮定する分布はガウス
仮定する分布はベルヌーイ
損失は二乗誤差
損失は交差エントロピー
予測するのは連続値
予測するのは確率（そこから分類）
\`\`\`

[最尤推定の節](textbook/m-prob-3)で見た「分布を決めれば損失が決まる」がそのまま効いています。

# 線形回帰

\`\`\`math
\\hat{y} = w_1 x_1 + w_2 x_2 + \\ldots + w_d x_d + b
\`\`\`

損失は**二乗誤差**（平均二乗誤差、MSE）です。

\`\`\`math
L = \\frac{1}{N} \\sum_{i=1}^{N} (y_i - \\hat{y}_i)^2
\`\`\`

パラメータの求め方は 2 通りあります。

| 方法 | 内容 | 使いどころ |
| --- | --- | --- |
| 正規方程式 | $\\mathbf{w} = (\\mathbf{X}^{T}\\mathbf{X})^{-1}\\mathbf{X}^{T}\\mathbf{y}$ で一発で解く | 特徴量が少ないとき |
| 勾配降下法 | 少しずつ更新する | 特徴量やデータが多いとき |

**正規方程式は逆行列の計算が必要**で、特徴量の数が増えると急に重くなります（$\\mathbf{X}^{T}\\mathbf{X}$ は特徴量数の正方行列）。また、特徴量どうしが強く相関していると $\\mathbf{X}^{T}\\mathbf{X}$ が特異に近づき、解が不安定になります（**多重共線性**）。

> **試験のポイント** 多重共線性への対処は、相関の強い特徴量を落とすか、**リッジ回帰（L2 正則化）**を使うことです。$\\lambda$ を足すと $\\mathbf{X}^{T}\\mathbf{X} + \\lambda \\mathbf{I}$ となり、逆行列が安定して求まります。

正則化つきの線形回帰には名前が付いています。

| 名前 | 正則化 | 性質 |
| --- | --- | --- |
| リッジ回帰 | L2 | 係数を全体的に小さくする |
| ラッソ回帰 | L1 | 係数が 0 になり、特徴量選択になる |
| Elastic Net | L1 ＋ L2 | 両者の中間 |

# ロジスティック回帰

線形の出力 $z$ をシグモイド関数に通し、0 から 1 の値にします。

\`\`\`math
p = \\sigma(z) = \\frac{1}{1 + \\exp(-z)}
\`\`\`

この $p$ を「クラス 1 である確率」と読み、0.5 などのしきい値で分類します。損失は**交差エントロピー**です。

> **よくある勘違い** ロジスティック回帰に二乗誤差を使ってはいけません。**損失関数が凸でなくなり、局所解に落ちる**うえ、誤差が大きいときほど勾配が小さくなって学習が進みません。交差エントロピーなら、線形出力 $z$ に関する勾配が $\\hat{y} - y$ という素直な形になります。

**決定境界は直線（超平面）**です。シグモイドを通しても $z = 0$ の面が境界であることは変わらないので、**曲がった境界は表現できません**。表現するには特徴量を増やす（多項式特徴）か、カーネル法やニューラルネットワークを使います。

多クラスに拡張するときは、シグモイドの代わりに**ソフトマックス**を使います（多項ロジスティック回帰、ソフトマックス回帰）。

# コードで確かめる

\`\`\`python
import numpy as np

# 線形回帰を正規方程式で解く
X = np.array([[1.0, 1.0], [1.0, 2.0], [1.0, 3.0], [1.0, 4.0]])   # 先頭列はバイアス項
y = np.array([2.1, 3.9, 6.2, 7.8])
w = np.linalg.inv(X.T @ X) @ X.T @ y
print(np.round(w, 3))            # [0.15  1.94]  切片と傾き

# ロジスティック回帰の予測
def sigmoid(z):
    return 1 / (1 + np.exp(-z))

z = X @ np.array([-3.0, 1.2])
print(np.round(sigmoid(z), 3))   # 確率として読める値が並ぶ
\`\`\`

実務では \`np.linalg.inv\` ではなく \`np.linalg.solve\`（または \`lstsq\`）を使います。**逆行列を陽に作るのは数値的に不利**だからです。

# この節のまとめ

- 線形回帰とロジスティック回帰は、隠れ層のないニューラルネットワークそのもの
- **ロジスティック回帰は分類の手法**。線形の出力をシグモイドで確率に変える
- 線形回帰の損失は二乗誤差（ガウス分布の仮定）、ロジスティック回帰は交差エントロピー（ベルヌーイの仮定）
- 正規方程式は一発で解けるが、特徴量が多いときや多重共線性があるときは不利
- リッジ回帰は L2、ラッソ回帰は L1。ラッソは係数が 0 になり特徴量選択になる
- ロジスティック回帰の決定境界は直線（超平面）で、曲がった境界は表現できない

\`\`\`quiz
ロジスティック回帰は回帰と分類のどちらの手法ですか :: 分類です。線形の出力をシグモイドで確率に変え、しきい値で分類します。
線形回帰の正規方程式の弱点は何ですか :: 逆行列の計算が必要で特徴量が多いと重く、多重共線性があると解が不安定になります。
多重共線性への対処は :: 相関の強い特徴量を落とすか、リッジ回帰（L2 正則化）を使います。λI を足すことで逆行列が安定します。
ラッソ回帰とリッジ回帰の違いは :: ラッソは L1 で係数が 0 になり特徴量選択になります。リッジは L2 で係数を全体的に小さくします。
ロジスティック回帰に二乗誤差を使わないのはなぜですか :: 損失が凸でなくなり局所解に落ちるうえ、誤差が大きいときに勾配が小さくなって学習が進まないからです。
ロジスティック回帰の決定境界はどんな形ですか :: 直線（超平面）です。曲がった境界を表すには特徴量を増やすか、別の手法を使います。
\`\`\`
`,
  },
  {
    id: 'ml-basic-3',
    categoryId: 'ml-basic',
    title: 'SVM・決定木・アンサンブル',
    goal: 'マージン最大化とカーネル法の考え方を掴み、決定木とその集団学習の違いを説明できる',
    minutes: 25,
    body: `
# ざっくり言うと

前の節の線形モデルには、**境界が直線しか引けない**という限界がありました。この節の手法は、その限界の越え方が 2 通りあることを示します。

- **SVM**：データを高次元へ写して、そこで直線を引く（**カーネル法**）
- **決定木**：軸に垂直な線で領域を細かく区切っていく

そして決定木は 1 本では弱いので、**たくさん束ねる**（アンサンブル）のが定石です。表形式データで今も強いのは、この束ねた木です。

# SVM：境界から最も近い点までの距離を最大にする

境界を引ける直線が何本もあるとき、どれを選ぶべきか。SVM の答えは「**境界から最も近いデータまでの距離（マージン）が最大になるもの**」です（文献によっては、その 2 倍にあたる帯の幅をマージンと呼びます）。

\`\`\`diagram:flow
title: SVM の考え方
候補となる境界は無数にある :: どれもデータを分けられる
最も近い点との距離を測る :: この距離がマージン
マージンが最大の境界を選ぶ :: 新しいデータにも余裕を持って対応できる
境界を決めるのは近い点だけ :: これがサポートベクトル
\`\`\`

> **試験のポイント** 境界の位置を決めているのは、**境界の近くにある少数の点（サポートベクトル）だけ**です。**マージンの外側にとどまる限り**、遠くのデータを動かしても境界は変わりません（逆に、動かしてマージンの内側に入れば、その点がサポートベクトルになって境界も動きます）。ここが、全データの誤差を平均する線形回帰との大きな違いです。

現実のデータは完全には分けられないので、多少の誤りを許す**ソフトマージン**を使います。許す度合いはハイパーパラメータ $C$ で決めます。

| $C$ | 意味 | 結果 |
| --- | --- | --- |
| 大きい | 誤りをほとんど許さない | マージンが狭くなり、過学習しやすい |
| 小さい | 誤りを許す | マージンが広くなり、単純な境界になる |

## カーネル法

直線で分けられないデータでも、**高い次元へ写せば分けられる**ことがあります。ただし、実際に高次元の座標を計算するのは重すぎます。

そこで、**写した先での内積だけを、写さずに計算する**のがカーネル法です（**カーネルトリック**）。SVM は**双対問題**に直すと学習にも予測にも内積しか出てこないので、これで足ります。

| カーネル | 使いどころ |
| --- | --- |
| 線形 | 特徴量が多く、そのままで分けられるとき |
| 多項式 | 特徴量どうしの積が効くとき |
| RBF（ガウス） | **既定の選択肢。** 複雑な境界を表現できる |

> **よくある勘違い** カーネル法は「高次元の座標を計算してから内積を取る」のではありません。**内積の値だけを直接求める**ので、無限次元へ写す RBF カーネルでも計算できます。

# 決定木：条件分岐を学習する

「特徴量 A が 5 以上か」といった質問を繰り返して、データを絞り込みます。**人が読める形になる**のが最大の利点です。

分岐の選び方は、[情報理論の節](textbook/m-info-1)で見た**情報利得**そのものです。分割の前後で**不純度**がどれだけ減るかで、使う特徴量としきい値を決めます。

| 不純度の指標 | 使われる場面 |
| --- | --- |
| ジニ不純度 | CART（scikit-learn の既定） |
| エントロピー | ID3・C4.5 系。情報利得の計算に使う |
| 二乗誤差 | 回帰木 |

決定木は**深くすればいくらでも訓練データに適合できる**ので、放っておくと必ず過学習します。深さの上限、葉の最小サンプル数、剪定（枝刈り）で抑えます。

> **試験のポイント** 決定木は**特徴量のスケールに影響されません**（大小の比較しかしないため）。標準化が不要という点は、距離を使う手法（SVM・k-means・k 近傍法）との重要な違いです。

# アンサンブル：弱い学習器を束ねる

\`\`\`diagram:compare
title: バギングとブースティング
actors: バギング | ブースティング
データを復元抽出して並列に学習
前のモデルの誤りを次が順番に補う
それぞれ独立なので並列化できる
直列なので並列化しにくい
主にバリアンスを下げる
主にバイアスを下げる
代表：ランダムフォレスト
代表：AdaBoost、勾配ブースティング
過学習しにくい
木を増やしすぎると過学習しうる
\`\`\`

**ランダムフォレスト**は、バギングに加えて**分岐で使う特徴量もランダムに選ぶ**ことで、木どうしの相関を下げています。木が似すぎていると束ねても効果が薄いためです。

**勾配ブースティング**は、損失関数の**負の勾配（擬似残差）**を次の木が予測する形で順に足していく手法です（損失が二乗誤差なら、これはそのまま残差になります。「勾配」という名前の由来もここです）。実装として XGBoost・LightGBM・CatBoost が広く使われ、**表形式データのコンペでは定番**です。

3 つ目の系統として、複数モデルの出力をさらに別のモデルで統合する**スタッキング**があります。

# k 近傍法（k-NN）

学習らしい学習をせず、**予測時に近いデータを $k$ 個探して多数決する**手法です。「怠惰学習」とも呼ばれます。

- 学習は速い（覚えるだけ）が、**予測が遅い**（毎回距離を計算する）
- 距離を使うので**標準化が必要**
- $k$ が小さいと過学習、大きいと境界がぼやける

# この節のまとめ

- SVM は**マージン最大化**で境界を決める。境界を決めるのは少数のサポートベクトルだけ
- ソフトマージンの $C$ が大きいほど誤りを許さず、過学習しやすくなる
- カーネル法は**写した先の内積だけを直接計算する**手法。RBF カーネルが既定の選択肢
- 決定木は不純度（ジニ・エントロピー）の減り方で分岐を選ぶ。**スケールの影響を受けない**
- バギングはバリアンスを下げ、ブースティングはバイアスを下げる。ランダムフォレストは前者、XGBoost 系は後者
- k 近傍法は学習が速く予測が遅い。距離を使うので標準化が要る

\`\`\`quiz
SVM の境界を決めているのはどのデータですか :: 境界の近くにある少数の点（サポートベクトル）だけです。マージンの外側にとどまる限り、遠くの点を動かしても境界は変わりません。
ソフトマージンの C を大きくするとどうなりますか :: 誤りをほとんど許さなくなり、マージンが狭まって過学習しやすくなります。
カーネルトリックとは何をしていますか :: 高次元へ写した先での内積の値を、実際に写さずに直接計算しています。
決定木に標準化が不要なのはなぜですか :: 特徴量の大小比較しか使わないため、スケールを変えても分岐の結果が変わらないからです。
バギングとブースティングの違いは :: バギングは並列に学習してバリアンスを下げ、ブースティングは直列に誤りを補ってバイアスを下げます。
ランダムフォレストがバギングに加えている工夫は :: 分岐に使う特徴量もランダムに選ぶことです。木どうしの相関を下げて、束ねる効果を高めます。
\`\`\`
`,
  },
  {
    id: 'ml-basic-4',
    categoryId: 'ml-basic',
    title: '教師なし学習',
    goal: 'クラスタリングと次元削減の代表的な手法を掴み、それぞれの前提と限界を説明できる',
    minutes: 20,
    body: `
# ざっくり言うと

正解ラベルがないデータから、**構造そのものを見つける**のが教師なし学習です。やることは大きく 2 つしかありません。

- **まとめる**（クラスタリング）
- **少ない軸で表し直す**（次元削減）

次元削減の主役である主成分分析は、[特異値分解の節](textbook/m-linalg-3)で扱った内容そのものです。ここでは使い方の側から見直します。

# k-means

もっとも基本的なクラスタリングです。まず中心を $k$ 個ランダムに置き、あとは次の 2 つを繰り返すだけです。

\`\`\`diagram:cycle
title: k-means が繰り返す 2 つの操作
各点を最も近い中心に割り当てる :: 距離を計算してグループ分けする
割り当てられた点の平均を新しい中心にする :: 中心が動く
\`\`\`

中心が動かなくなったら収束です。

弱点もはっきりしています。

| 弱点 | 対処 |
| --- | --- |
| クラスタ数 $k$ を先に決める必要がある | エルボー法、シルエット分析 |
| 初期値によって結果が変わる | k-means++ で初期値を賢く選ぶ、複数回試す |
| 球状のクラスタしか見つけられない | 混合ガウス分布、DBSCAN |
| 外れ値に弱い | 実在するデータ点を中心にする k-medoids、中央値を使う k-medians、DBSCAN |

> **試験のポイント** k-means は**距離を使うので標準化が必要**です。単位の大きい特徴量（年収など）が、そのまま距離を支配してしまいます。

ほかに、近いものから順に併合していく**階層的クラスタリング**（樹形図で結果を見られる。$k$ を後から決められる）、密度に基づいてクラスタを見つける **DBSCAN**（$k$ が不要で、外れ値をノイズとして扱える）があります。

# 主成分分析（PCA）

**データが最も散らばっている方向**から順に新しい軸を取り直す手法です。

\`\`\`diagram:flow
title: PCA の手順
データを中心化する :: 各特徴量から平均を引く（必須）
共分散行列を作る :: あるいは直接 SVD を取る
固有ベクトルを求める :: これが主成分の方向
固有値の大きい順に選ぶ :: 固有値がその方向の分散
\`\`\`

$k$ 個の主成分を採用したときに元の分散をどれだけ説明できているかが**寄与率**（累積寄与率）で、「上位 2 つで 80% を説明」といった言い方をします。

| 用途 | 効果 |
| --- | --- |
| 可視化 | 高次元のデータを 2〜3 次元にして眺める |
| 前処理 | 次元の呪いを緩め、計算を軽くする |
| ノイズ除去 | 小さい固有値の成分を捨てる |
| 多重共線性の解消 | 主成分どうしは直交する |

> **よくある勘違い** PCA は**線形な変換**です。曲がった構造（渦巻き状のデータなど）はうまく展開できません。非線形な構造には、カーネル PCA、$t$-SNE、UMAP、オートエンコーダを使います。

**$t$-SNE と UMAP は可視化のための手法**で、局所的な近さを保つように 2 次元へ落とします。ただし**軸に意味はなく、クラスタ間の距離も信用できません**。前処理として使うのではなく、目で眺めるために使うものだと理解してください。

# 異常検知

大多数と違うものを見つける課題です。**正常データばかりで異常がごく少ない**（＝ラベルが偏っている）ため、教師あり分類として解きにくいという事情があります。

| 手法 | 考え方 |
| --- | --- |
| 統計的手法 | 平均から標準偏差の何倍離れているか |
| One-Class SVM | 正常データを囲む境界を学ぶ |
| Isolation Forest | ランダムな分割で「早く孤立する」点を異常とみなす |
| オートエンコーダ | 復元誤差が大きいものを異常とみなす |

**オートエンコーダによる異常検知は、深層学習の章にそのままつながります。** 正常データだけで学習させると、異常なデータはうまく復元できず誤差が大きくなる、という発想です。

# この節のまとめ

- 教師なし学習の柱は「まとめる（クラスタリング）」と「少ない軸で表し直す（次元削減）」
- k-means は割り当てと中心の更新の繰り返し。$k$ を先に決める必要があり、初期値に依存し、球状のクラスタしか見つけられない
- k-means や k 近傍法など**距離を使う手法は標準化が必要**
- PCA は分散が大きい方向から軸を取る。**中心化が必須**で、寄与率で採用する次元を決める
- PCA は線形なので、曲がった構造には $t$-SNE・UMAP・オートエンコーダを使う
- $t$-SNE と UMAP は可視化用。軸の意味やクラスタ間の距離は解釈しない

\`\`\`quiz
k-means の手順を 2 つの操作で説明してください :: 各点を最も近い中心に割り当てる操作と、割り当てられた点の平均で中心を更新する操作の繰り返しです。
k-means の弱点を 3 つ挙げてください :: k を先に決める必要がある、初期値によって結果が変わる、球状のクラスタしか見つけられない、の 3 つです（外れ値に弱い点も含めてよい）。
PCA で中心化が必要なのはなぜですか :: 分散が最大の方向を求める手法なので、平均を引いておかないと「原点から遠い方向」を拾ってしまうためです。
PCA の寄与率とは何ですか :: 採用した主成分が元の分散のどれだけを説明できているかの割合です。固有値（特異値の 2 乗）の比で求めます。
t-SNE や UMAP の結果を解釈するときの注意は :: 軸に意味はなく、クラスタ間の距離も信用できません。可視化のための手法です。
オートエンコーダで異常検知を行う仕組みは :: 正常データだけで学習させ、復元誤差が大きいものを異常とみなします。
\`\`\`
`,
  },
  {
    id: 'ml-method-1',
    categoryId: 'ml-method',
    title: 'データの分け方と交差検証',
    goal: '訓練・検証・テストの役割を区別し、交差検証とデータリークの落とし穴を説明できる',
    minutes: 25,
    body: `
# ざっくり言うと

モデルの良し悪しを測るときに絶対に守るべき原則は 1 つだけです。**評価に使うデータは、学習にも調整にも一度も使っていないこと。**

この原則を破ると、**手元では良い数字が出るのに本番で落ちる**という、最も痛い失敗が起きます。この節は、その原則を守るための手続きの話です。

# 3 つに分ける

\`\`\`diagram:stack
title: データの役割分担
top: 何度でも使ってよい
bottom: 最後に一度だけ
訓練データ :: パラメータ（重み）を決める
検証データ :: ハイパーパラメータやモデルを選ぶ
テストデータ :: 最終的な性能を報告する
\`\`\`

| 用途 | 何を決めるか | 使う回数 |
| --- | --- | --- |
| 訓練（train） | 重み・係数などのパラメータ | 何度でも |
| 検証（validation） | 学習率、層の数、$\\lambda$、モデルの種類 | 何度でも |
| テスト（test） | 何も決めない。性能を報告するだけ | **最後に一度だけ** |

> **試験のポイント** **検証データとテストデータの役割は違います。** 検証データを見ながら調整を繰り返すと、そのデータに対しては最適化されてしまうため、汎化性能の見積もりとしては甘くなります。だから最後の報告には、まだ一度も見ていないテストデータを使います。

# 交差検証

データが少ないと、1 回の分け方の運不運で数字が大きく振れます。そこで**分け方を変えて何度も評価し、平均を取る**のが交差検証です。

\`\`\`diagram:flow
title: k 分割交差検証（k = 5 の場合）
データを 5 等分する :: それぞれを「フォールド」と呼ぶ
1 つを検証、4 つを訓練にする :: 1 回目の評価
検証に使う塊を入れ替える :: これを 5 回繰り返す
5 回の評価を平均する :: これが交差検証のスコア
\`\`\`

**全データがちょうど 1 回ずつ検証に使われる**のが要点です。$k$ を大きくすると評価は安定しますが、学習を $k$ 回行うので計算時間も $k$ 倍になります。極端に $k$ をデータ件数と同じにしたものが**Leave-One-Out 交差検証**です。

| 種類 | 内容 | 使いどころ |
| --- | --- | --- |
| ホールドアウト | 1 回だけ分ける | データが多いとき |
| $k$ 分割 | $k$ 回に分けて平均 | 標準的な選択 |
| 層化 $k$ 分割 | 各分割のクラス比を元と揃える | **不均衡データの分類** |
| Leave-One-Out | 1 件だけを検証に | データが極端に少ないとき |
| 時系列分割 | 過去で学習し、未来で検証する | **時系列データ** |

> **試験のポイント** 時系列データを**ランダムに分割してはいけません。** 未来のデータで学習して過去を予測することになり、実運用ではあり得ない好成績が出ます。必ず時間の順序を保ったまま分けます。

# データリーク

**本番では使えない情報が、学習に混ざり込む**ことをデータリークといいます。検証の設計で最も起きやすい失敗です。

\`\`\`diagram:compare
title: よくあるリークとその防ぎ方
actors: やってしまいがちなこと | 正しい手順
全データで標準化してから分割する
分割してから、訓練データの統計で変換する
全データで特徴量選択してから分割する
分割の内側で特徴量を選ぶ
時系列をランダムに分割する
時間の順序を保って分割する
同じ人物の写真が訓練と検証に散らばる
グループ単位で分割する（GroupKFold）
\`\`\`

> **よくある勘違い** 「テストデータの正解ラベルを見ていないからリークではない」とは限りません。**標準化の平均や特徴量選択の判断に、テストデータの分布が混ざるだけで漏れています。** 前処理は必ず分割の後、訓練データの統計だけを使って行います。

# ハイパーパラメータの探索

学習で決まらない設定（学習率、木の深さ、$\\lambda$、$k$ など）は、検証データで比べて選びます。

| 方法 | 内容 | 性格 |
| --- | --- | --- |
| グリッドサーチ | 候補の全組み合わせを試す | 網羅的だが、組み合わせ爆発を起こす |
| ランダムサーチ | ランダムに選んで試す | **同じ試行回数ならグリッドより有利なことが多い** |
| ベイズ最適化 | これまでの結果から次の候補を推定する | 試行 1 回が高価なときに向く |

ランダムサーチが有利なのは、**効く（結果を大きく変える）パラメータが少数に偏っている**ためです。グリッドでは効かないパラメータの刻みにも試行回数を費やしてしまいます。

# コードで確かめる

\`\`\`python
import numpy as np

def kfold_indices(n, k, seed=0):
    idx = np.random.default_rng(seed).permutation(n)
    folds = np.array_split(idx, k)                    # k 個の塊に分ける
    for i in range(k):
        valid = folds[i]
        train = np.concatenate([folds[j] for j in range(k) if j != i])
        yield train, valid

for train, valid in kfold_indices(10, 5):
    print(len(train), len(valid))                     # 8 2 が 5 回
\`\`\`

**検証に使う塊を入れ替えているだけ**で、難しいことは何もしていません。scikit-learn なら \`KFold\`、クラス比を保つなら \`StratifiedKFold\` を使います。

# この節のまとめ

- 評価に使うデータは、学習にも調整にも一度も使っていないこと。これが唯一の原則
- 訓練はパラメータ、検証はハイパーパラメータ、テストは報告のためだけ（**最後に一度**）
- $k$ 分割交差検証は分け方を変えて $k$ 回評価して平均する。全データが 1 回ずつ検証に使われる
- 不均衡データでは層化分割、時系列では時間の順序を保った分割を使う
- 前処理（標準化・特徴量選択）は**分割の後**に、訓練データの統計だけで行う
- 探索はグリッド・ランダム・ベイズ最適化。同じ試行回数ならランダムのほうが有利なことが多い

\`\`\`quiz
検証データとテストデータの役割の違いは :: 検証はハイパーパラメータやモデルを選ぶために何度でも使い、テストは最終的な性能を報告するために最後に一度だけ使います。
k 分割交差検証の要点は何ですか :: 分け方を変えて k 回評価し平均を取ることです。全データがちょうど 1 回ずつ検証に使われます。
不均衡データの分類ではどんな分割を使いますか :: 層化 k 分割です。各分割のクラス比を元のデータと揃えます。
時系列データをランダムに分割してはいけないのはなぜですか :: 未来のデータで学習して過去を予測することになり、実運用ではあり得ない好成績が出るからです。
全データで標準化してから訓練とテストに分けるのは、なぜ問題ですか :: テストデータの分布が平均や分散を通じて学習に漏れるからです（データリーク）。分割してから訓練データの統計で変換します。
グリッドサーチとランダムサーチでは、同じ試行回数ならどちらが有利とされますか :: ランダムサーチです。結果に効くパラメータが少数に偏っているため、無駄な刻みに試行を使わずに済みます。
\`\`\`
`,
  },
  {
    id: 'ml-method-2',
    categoryId: 'ml-method',
    title: '評価指標',
    goal: '混同行列から各指標を計算でき、不均衡データで正解率を使ってはいけない理由を説明できる',
    minutes: 30,
    body: `
# ざっくり言うと

**この節は計算問題が確実に出ます。** 式を覚えるというより、**混同行列という 1 枚の表の読み方**を身に付けてください。指標はすべてこの表から出てきます。

そして最も重要な事実がひとつあります。**不均衡なデータでは、正解率（accuracy）は役に立ちません。** 1000 人に 1 人の病気を「全員陰性」と答えるだけで、正解率 99.9% になってしまうからです。

# 混同行列

予測と正解の組み合わせを 4 通りに数えた表です。

| | 予測：陽性 | 予測：陰性 |
| --- | --- | --- |
| **実際：陽性** | TP（真陽性） | FN（偽陰性・見逃し） |
| **実際：陰性** | FP（偽陽性・誤検出） | TN（真陰性） |

**頭文字の読み方を固定すると迷いません。** 2 文字目が「モデルが何と予測したか（P か N か）」、1 文字目が「その予測が当たっていたか（T か F か）」です。FN は「陰性と予測して、外した」＝見逃しです。

# 4 つの指標

\`\`\`math
正解率 = \\frac{TP + TN}{TP + TN + FP + FN}
\`\`\`

\`\`\`math
適合率 = \\frac{TP}{TP + FP}
\`\`\`

\`\`\`math
再現率 = \\frac{TP}{TP + FN}
\`\`\`

\`\`\`math
F 値 = \\frac{2 \\times 適合率 \\times 再現率}{適合率 + 再現率}
\`\`\`

**分母だけ見れば区別できます。** 適合率は「**陽性と予測したもの**のうち、当たっていた割合」なので分母が予測の側（縦）、再現率は「**実際に陽性のもの**のうち、拾えた割合」なので分母が正解の側（横）です。

\`\`\`diagram:compare
title: 適合率と再現率、どちらを優先するか
actors: 適合率（precision）を重視 | 再現率（recall）を重視
誤検出の損が大きいとき
見逃しの損が大きいとき
迷惑メール判定（大事なメールを消したくない）
病気の検査（見逃したくない）
製造ラインの不良品判定で捨てすぎたくない
不正取引の検知
しきい値を上げる
しきい値を下げる
\`\`\`

> **試験のポイント** 適合率と再現率は**トレードオフ**です。しきい値を下げれば陽性と判定する数が増え、再現率は上がりますが適合率は下がります。**両方を同時に上げることは普通できない**ので、F 値（調和平均）でバランスを見ます。

**F 値が算術平均ではなく調和平均なのは、片方が極端に低いときに全体を低く評価するため**です。適合率 1.0・再現率 0.02 なら、算術平均は 0.51 ですが F 値は 0.039 にしかなりません。

# 計算してみる

100 件中、実際に陽性が 20 件。モデルは 15 件を陽性と予測し、そのうち 12 件が当たっていたとします。

\`\`\`diagram:flow
title: まず 4 つの数を埋める
TP = 12 :: 陽性と予測して当たり
FP = 15 - 12 = 3 :: 陽性と予測して外れ
FN = 20 - 12 = 8 :: 実際は陽性なのに陰性と予測（見逃し）
TN = 100 - 12 - 3 - 8 = 77 :: 陰性と予測して当たり
\`\`\`

| 指標 | 計算 | 値 |
| --- | --- | --- |
| 正解率 | $(12 + 77) / 100$ | 0.89 |
| 適合率 | $12 / 15$ | 0.80 |
| 再現率 | $12 / 20$ | 0.60 |
| F 値 | $2 \\times 0.8 \\times 0.6 / (0.8 + 0.6)$ | 約 0.686 |

**正解率だけを見ると 0.89 で良さそうですが、再現率は 0.60 で 4 割を見逃しています。** これが「正解率だけを見てはいけない」ということの意味です。

# ROC 曲線と AUC

しきい値を動かすと、すべての指標が変わります。**しきい値を動かしながら性能を一望する**のが ROC 曲線です。

- 横軸：偽陽性率 $FP / (FP + TN)$
- 縦軸：真陽性率（＝再現率）$TP / (TP + FN)$

その下の面積が **AUC** で、1 に近いほど良く、**0.5 はランダムと同じ**です。

> **よくある勘違い** AUC は**しきい値を決めなくても比べられる**指標です。逆に言えば、実運用でどのしきい値を使うかは別に決める必要があります。また、**極端な不均衡データでは AUC も楽観的に見える**ため、その場合は PR 曲線（適合率と再現率の曲線）とその面積を見るほうが実態を反映します。

# 多クラスと回帰

多クラス分類では、クラスごとに計算した指標をどう平均するかを選びます。

| 平均の取り方 | 内容 | 性格 |
| --- | --- | --- |
| マクロ平均 | クラスごとの指標を単純平均 | **少数クラスも同じ重みで評価される** |
| マイクロ平均 | 全クラスの TP・FP・FN を合計してから計算 | 件数の多いクラスに引っ張られる |
| 加重平均 | クラスの件数で重み付け | マイクロ平均に近い性格 |

回帰では次の指標を使います。

| 指標 | 式 | 性格 |
| --- | --- | --- |
| MSE | 誤差の 2 乗の平均 | 外れ値の影響が大きい |
| RMSE | MSE の平方根 | **単位が元のデータと同じ**になり解釈しやすい |
| MAE | 誤差の絶対値の平均 | 外れ値に強い |
| 決定係数 $R^2$ | 1 − 残差平方和 ÷ 全体の平方和 | 1 に近いほど良い。**負にもなりうる** |

> **試験のポイント** $R^2$ は「平均値で予測した場合」を 0 とする指標です。それより悪いモデルなら**負の値になります。** 「0 から 1 の範囲」と思い込まないでください。

# この節のまとめ

- すべての指標は混同行列（TP・FP・FN・TN）から出てくる。2 文字目が予測、1 文字目が当否
- 適合率は分母が予測の側、再現率は分母が正解の側。**分母で区別する**
- 適合率と再現率はトレードオフ。F 値はその調和平均で、片方が極端に低いと大きく下がる
- **不均衡データで正解率を見てはいけない。** 多数派に全部答えるだけで高い値が出る
- AUC はしきい値によらず比べられる。0.5 がランダム、極端な不均衡では PR 曲線のほうが実態に合う
- 回帰では RMSE が解釈しやすく、MAE は外れ値に強い。$R^2$ は負にもなる

\`\`\`quiz
適合率と再現率の分母はそれぞれ何ですか :: 適合率は「陽性と予測した件数」、再現率は「実際に陽性である件数」です。分母で区別できます。
FN（偽陰性）はどんな誤りですか :: 実際は陽性なのに陰性と予測した誤り、つまり見逃しです。
病気の検査では適合率と再現率のどちらを優先しますか :: 再現率です。見逃し（FN）の損失が大きいためです。
F 値が算術平均ではなく調和平均なのはなぜですか :: 片方が極端に低いときに、全体を低く評価するためです。
AUC が 0.5 のとき、そのモデルの性能はどう解釈しますか :: ランダムな予測と同じです。1 に近いほど良い指標です。
決定係数 R² が負になるのはどんなときですか :: 平均値で予測するよりも当たっていないときです。0 から 1 の範囲に収まるとは限りません。
\`\`\`
`,
  },
];
