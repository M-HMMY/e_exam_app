import type { TextbookSection } from '../../types';

export const math: TextbookSection[] = [
  {
    id: 'm-linalg-1',
    categoryId: 'm-linalg',
    title: 'ベクトルと行列',
    goal: '内積と行列積の意味を掴み、層をまたいで形（shape）を追えるようになる',
    minutes: 25,
    body: `
# ざっくり言うと

ニューラルネットワークの計算は、**行列とベクトルの掛け算**です。層をいくつ重ねても、活性化関数を挟む以外にやっていることは変わりません。

だから線形代数は「数学の一分野」というより、**この試験の問題文が書かれている言語**です。読めないと、深層学習の章で何が起きているのかが見えません。逆に言えば、必要なのは計算のしかたであって、証明ではありません。

この節では、ベクトルと行列の形と、掛け算の 2 種類だけを扱います。[コードの読み方](textbook/i-3)で見た shape の話と、ちょうど表と裏の関係になります。

# ベクトルは「数を一列に並べたもの」

$\\mathbf{x} = (x_1, x_2, x_3)$ のように、数を並べたものがベクトルです。深層学習では**データ 1 件**を表します。

たとえば 28×28 の白黒画像は、画素を一列に並べれば 784 個の数になります。これが 1 件ぶんの入力ベクトルです。

ベクトル同士の演算で、この試験に出るのは 2 つだけです。

| 演算 | 書き方 | 結果 | 意味 |
| --- | --- | --- | --- |
| 内積 | $\\mathbf{a} \\cdot \\mathbf{b}$ | 数（スカラー） | 同じ位置どうしを掛けて、全部足す |
| ノルム | 2 乗和の平方根 | 数（スカラー） | ベクトルの長さ |

内積の式は次の通りです。

\`\`\`math
\\mathbf{a} \\cdot \\mathbf{b} = \\sum_{i=1}^{n} a_i b_i
\`\`\`

$\\sum$ が for ループだったことを思い出せば、「掛けて足す」を並べているだけだと分かります。**ニューロン 1 個がやっている計算は、まさにこの内積です。** 入力ベクトルと重みベクトルの内積を取り、バイアスを足す。それだけです。

> **試験のポイント** 内積の結果は**数 1 個**です。ベクトルではありません。ここを取り違えると、層の出力の形が追えなくなります。

# 行列は「数を縦横に並べたもの」

$m$ 行 $n$ 列に数を並べたものが行列で、形を $(m, n)$ と書きます。深層学習で行列が出てくる場面は、大きく 2 つです。

\`\`\`diagram:compare
title: 行列が表しているもの
actors: データの束 | 変換のしかた
32 件のデータを縦に積む :: 形は（バッチサイズ, 特徴量の数）
入力を出力へ移す規則 :: 形は（入力の次元, 出力の次元）
1 行が 1 件のデータ :: ミニバッチはこの形で流れる
中身は学習で更新される重み :: これがパラメータの正体
\`\`\`

**同じ「行列」でも、意味がまったく違う**という点に注意してください。前者は動くもの（データ）、後者は学習されるもの（重み）です。

# 行列積は「内積を並べたもの」

行列積 $\\mathbf{AB}$ の $(i, j)$ 成分は、**$\\mathbf{A}$ の $i$ 行目と $\\mathbf{B}$ の $j$ 列目の内積**です。

\`\`\`math
(\\mathbf{AB})_{ij} = \\sum_{k} a_{ik} b_{kj}
\`\`\`

$k$ が両方に出てきて、$\\sum$ で消えています。これが「内側の次元がつぶれる」ということの正体です。

\`\`\`diagram:flow
title: 形の追いかけ方
(2, 3) と (3, 4) :: 内側の 3 が一致している
内側の 3 が消える :: 一致していなければ計算できない
結果は (2, 4) :: 外側の 2 と 4 が残る
\`\`\`

形を動かして確かめてください。**内側が一致しないと計算できず、一致していればその次元が消えます。**

\`\`\`widget:matmul
\`\`\`

**掛け算の順序を入れ替えることはできません。** $\\mathbf{AB}$ と $\\mathbf{BA}$ は、形すら合わないことがほとんどです。これは数の掛け算との最大の違いです。

> **よくある勘違い** 「行列積は要素どうしを掛ける」——これは要素積（アダマール積）で、別物です。要素積は形が変わりませんが、行列積は形が変わります。LSTM のゲートは要素積、全結合層は行列積、と場面で覚えてください。

# 転置は「行と列の入れ替え」

$\\mathbf{W}^T$ は $\\mathbf{W}$ の行と列を入れ替えたものです。形が $(m, n)$ なら $(n, m)$ になります。

地味な操作ですが、**逆伝播の式には必ず出てきます。** 順伝播で $\\mathbf{xW}$ と掛けたなら、逆伝播では $\\mathbf{W}^T$ を掛けて誤差を手前の層へ戻します。理由は単純で、**形を合わせるにはそうするしかない**からです。

順伝播で $(32, 784) \\times (784, 128) = (32, 128)$ だったなら、戻すときは $(32, 128) \\times (128, 784) = (32, 784)$ が必要で、その $(128, 784)$ が $\\mathbf{W}^T$ です。

> **ここだけ覚える** 逆伝播で転置が出てくる理由は「形を戻すため」。式を暗記しなくても、形から復元できます。

# 特別な行列

| 名前 | 性質 | どこで出るか |
| --- | --- | --- |
| 単位行列 $\\mathbf{I}$ | 掛けても変わらない。対角が 1 で他が 0 | 逆行列の定義、残差接続の説明 |
| 逆行列 $\\mathbf{A}^{-1}$ | 掛けると単位行列になる | 最小二乗法の解 |
| 対角行列 | 対角以外が 0 | 固有値分解・特異値分解 |
| 直交行列 | 転置が逆行列と一致する | 主成分分析、重みの初期化 |

逆行列は**正方行列でも常に存在するとは限りません。** 存在しない場合を特異（正則でない）といい、行列式が 0 になります。

# この節のまとめ

- ベクトルはデータ 1 件、行列はデータの束か、変換の規則
- 内積は「掛けて全部足す」。結果は数 1 個で、ニューロン 1 個の計算そのもの
- 行列積は内積を並べたもの。内側の次元が一致していれば計算でき、その次元が消える
- 行列積は順序を入れ替えられない。要素積とは別物
- 逆伝播で転置が出るのは、形を元に戻すため
- 逆行列は常に存在するとは限らない（行列式が 0 のとき存在しない）

\`\`\`quiz
内積の結果はベクトルですか、数ですか :: 数（スカラー）1 個です。ニューロン 1 個が入力と重みに対して行う計算そのものです。
(2, 3) の行列と (3, 4) の行列の積は、どんな形になりますか :: (2, 4) です。内側で一致している 3 が消え、外側の 2 と 4 が残ります。
行列積と要素積の使い分けは :: 全結合層の計算は行列積、LSTM のゲートのように同じ位置どうしを掛ける場面は要素積です。
逆伝播の式に転置が出てくるのはなぜですか :: 誤差を手前の層に戻すとき、形を元に戻す必要があるからです。
逆行列が存在しないのはどんなときですか :: 行列式が 0 のとき（特異なとき）です。正方行列であっても常に存在するわけではありません。
\`\`\`
`,
  },
  {
    id: 'm-linalg-2',
    categoryId: 'm-linalg',
    title: '固有値と固有ベクトル',
    goal: '行列を「方向ごとの伸び縮み」として読めるようになり、固有値分解が何をしているかを言葉で説明できる',
    minutes: 25,
    body: `
# ざっくり言うと

行列を掛けると、ベクトルは普通「向き」も「長さ」も変わります。ところが行列ごとに、**掛けても向きが変わらない特別な方向**がいくつかあります。その方向が**固有ベクトル**、そのときの伸び率が**固有値**です。

つまり固有値と固有ベクトルは、行列を「数の四角い塊」から「**どの方向にどれだけ伸ばす装置か**」へと読み替える道具です。主成分分析で「データが最も散らばっている方向」を選べるのも、最適化で「谷が細長いと学習が遅い」と言えるのも、正体はここにあります。

# 定義は 1 本の式だけ

\`\`\`math
\\mathbf{A}\\mathbf{v} = \\lambda \\mathbf{v} \\quad (\\mathbf{v} \\ne \\mathbf{0})
\`\`\`

左辺は「行列を掛ける」、右辺は「ただの定数倍」です。**行列を掛けたのに定数倍にしかなっていない**、そんな都合のよい $\\mathbf{v}$ が固有ベクトルで、その $\\lambda$ が固有値です。

$\\mathbf{v} \\ne \\mathbf{0}$ という条件が付いているのは、ゼロベクトルならどんな行列でも式が成り立ってしまい、何も言っていないことになるからです。

> **試験のポイント** 固有ベクトルは 1 つに定まりません。$\\mathbf{v}$ が固有ベクトルなら $2\\mathbf{v}$ も $-\\mathbf{v}$ も固有ベクトルです。定まるのは**方向だけ**なので、実務でも試験でも通常は**長さ 1 に正規化**したものを使います。

# 求め方

$\\mathbf{A}\\mathbf{v} = \\lambda \\mathbf{v}$ を移項すると $(\\mathbf{A} - \\lambda \\mathbf{I})\\mathbf{v} = \\mathbf{0}$ です。これが $\\mathbf{v} \\ne \\mathbf{0}$ の解を持つのは、$\\mathbf{A} - \\lambda \\mathbf{I}$ に逆行列が**ない**ときだけです。逆行列があれば両辺に掛けて $\\mathbf{v} = \\mathbf{0}$ になってしまうからです。

\`\`\`math
\\mathrm{det}(\\mathbf{A} - \\lambda \\mathbf{I}) = 0
\`\`\`

これを**特性方程式**といい、$\\lambda$ についての方程式として解きます。$2 \\times 2$ なら二次方程式なので手で解けます。1 行目が $(4, 1)$、2 行目が $(2, 3)$ の行列なら $\\lambda^2 - 7\\lambda + 10 = 0$ となり、固有値は $5$ と $2$ です。

検算には次の 2 つが効きます。**手計算の問題ではまずこれを使ってください。**

> **試験のポイント** 固有値の**和は対角成分の和（トレース）**、固有値の**積は行列式**に一致します。上の例なら和は $4 + 3 = 7$（$5 + 2$ と一致）、積は $4 \\cdot 3 - 1 \\cdot 2 = 10$（$5 \\times 2$ と一致）。特性方程式を解かずに固有値を当てられることさえあります。

# 実対称行列はとくに扱いやすい

深層学習で出てくる行列のうち、**共分散行列・ヘッセ行列・グラム行列はすべて実対称**（成分が実数で $\\mathbf{A}^{T} = \\mathbf{A}$）です。実対称行列には次の 2 つのご褒美があります。

- 固有値が必ず**実数**になる（対称でない行列は複素数になり得ます）
- 異なる固有値に対応する固有ベクトルが必ず**直交**する

その結果、実対称行列は直交行列 $\\mathbf{V}$ と対角行列 $\\mathbf{\\Lambda}$ を使って次のように分解できます。これを**固有値分解（実対称行列の場合はスペクトル分解）**といいます。

\`\`\`math
\\mathbf{A} = \\mathbf{V} \\mathbf{\\Lambda} \\mathbf{V}^{T}
\`\`\`

$\\mathbf{V}$ の各列が正規化した固有ベクトル、$\\mathbf{\\Lambda}$ の対角が対応する固有値です。直交行列なので $\\mathbf{V}^{-1} = \\mathbf{V}^{T}$ が成り立ち、逆行列を求める手間がありません。

# 固有値分解は「座標を取り替えて伸ばす」操作

$\\mathbf{A}\\mathbf{x} = \\mathbf{V}\\mathbf{\\Lambda}\\mathbf{V}^{T}\\mathbf{x}$ を、**右から順に**読むと意味がはっきりします。

\`\`\`diagram:flow
title: 固有値分解を右から読む
V^T を掛ける :: 固有ベクトルを軸とする座標系に移す（回転）
Λ を掛ける :: 各軸を固有値の倍率で伸ばす（伸縮）
V を掛ける :: もとの座標系に戻す（回転を戻す）
\`\`\`

**行列がやっている複雑な変形は、座標を取り替えれば「軸ごとの伸び縮み」に過ぎない。** これが固有値分解の主張です。固有値が大きい軸ほど、その方向の変化が支配的になります。

# どこで効いてくるか

| 場面 | 何の行列の固有値か | 意味するもの |
| --- | --- | --- |
| 主成分分析（PCA） | データの共分散行列 | 固有ベクトルが主成分の方向、固有値がその方向の分散 |
| 最適化の地形 | 損失関数のヘッセ行列 | **勾配が 0 の点で**、すべて正なら極小、正と負が混ざれば鞍点 |
| 学習の進みにくさ | ヘッセ行列の固有値の絶対値の、最大 ÷ 最小（条件数） | 大きいほど谷が細長く、勾配降下法が振動する |
| 学習の安定性 | RNN の再帰重み行列（正方） | 固有値の絶対値の最大が 1 を超えると発散、小さすぎると勾配が消える |

全結合層の重み行列は $(入力, 出力)$ で正方とは限らないので、**固有値は定義できません。**活性や勾配の発散・消失を左右するのは、次の節で扱う**特異値**（その最大値）のほうです。Xavier や He の初期化、スペクトル正規化が見ているのもそちらです。

**鞍点の話は深層学習の最適化の章にそのまま効いてきます。** 高次元では「すべての固有値が負（極大）」より「正と負が混在（鞍点）」の方が圧倒的に起こりやすい、というのがその章の主題のひとつです。

なお、固有値がすべて 0 以上の対称行列を**半正定値**、すべて正なら**正定値**といいます。**共分散行列は必ず半正定値**です（分散が負にならないことの行列版だと思ってください）。

> **よくある勘違い** 「固有値が 0 だと計算できない」ではありません。固有値に 0 が含まれることと、**行列式が 0（＝逆行列が存在しない）**ことは同じ意味です。固有値の積が行列式なので、ひとつでも 0 があれば積は 0 になります。

# コードで確かめる

\`\`\`python
import numpy as np

A = np.array([[4.0, 1.0],
              [2.0, 3.0]])

vals, vecs = np.linalg.eig(A)      # 一般の正方行列用。並び順は保証されない
print(vals)                        # [5.+0.j 2.+0.j]  NumPy 2 系は常に複素数型で返す
print(vals.real)                   # 実数と分かっているなら実部を取り出す
print(vecs[:, 0])                  # vals[0] に対応する固有ベクトル（長さ 1 に正規化済み）

S = np.array([[2.0, 1.0],
              [1.0, 2.0]])
vals, vecs = np.linalg.eigh(S)     # 対称行列用。実数で、小さい順に返る
print(vals)                        # [1. 3.]
print(vecs.T @ vecs)               # ほぼ単位行列（列どうしが直交している）
\`\`\`

対称行列だと分かっているときは \`eigh\` を使います。実数で返り、直交性も保証されるため、共分散行列を扱う場面ではこちらが定石です。

# この節のまとめ

- 固有ベクトルは「行列を掛けても向きが変わらない方向」、固有値はそのときの伸び率
- 定義は $\\mathbf{A}\\mathbf{v} = \\lambda \\mathbf{v}$ の 1 本だけ。求めるときは特性方程式 $\\mathrm{det}(\\mathbf{A} - \\lambda\\mathbf{I}) = 0$ を解く
- 固有値の和はトレース、積は行列式。手計算の検算はまずこれ
- 実対称行列は固有値が実数で固有ベクトルが直交するので、$\\mathbf{A} = \\mathbf{V}\\mathbf{\\Lambda}\\mathbf{V}^{T}$ と分解できる
- 固有値分解は「座標を取り替えて、軸ごとに伸ばして、戻す」操作
- 共分散行列の固有値は各方向の分散（PCA）、ヘッセ行列の固有値の符号は、勾配が 0 の点が極小か鞍点かを決める

\`\`\`quiz
固有ベクトルはひとつに定まりますか :: 定まりません。定数倍しても固有ベクトルのままなので、決まるのは方向だけです。ふつうは長さ 1 に正規化して使います。
固有値の和と積は、それぞれ何に一致しますか :: 和はトレース（対角成分の和）、積は行列式です。検算に使えます。
実対称行列の固有値と固有ベクトルには、どんな性質がありますか :: 固有値は必ず実数で、異なる固有値の固有ベクトルどうしは直交します。
共分散行列の固有値と固有ベクトルは、PCA では何を表しますか :: 固有ベクトルが主成分の方向、固有値がその方向の分散です。固有値の大きい順に主成分を選びます。
勾配が 0 の点で、ヘッセ行列の固有値に正と負が混ざっているとき、その点は何ですか :: 鞍点です。すべて正なら極小、すべて負なら極大になります。
固有値に 0 が含まれるとき、その行列について何が言えますか :: 行列式が 0 になるので、逆行列が存在しません（特異な行列です）。
\`\`\`
`,
  },
  {
    id: 'm-linalg-3',
    categoryId: 'm-linalg',
    title: '特異値分解と低ランク近似',
    goal: '特異値分解の形と意味を掴み、「上位いくつかで近似する」という発想を圧縮・PCA・LoRA に結び付ける',
    minutes: 20,
    body: `
# ざっくり言うと

固有値分解には弱点があります。**正方行列にしか使えず、正方でも対角化できるとは限らない**ことです（前の節で見たとおり、必ず分解できると言えるのは実対称行列のような場合に限られます）。しかも現実のデータ行列は「1000 人 × 50 項目」のように縦横が違うのが普通です。

そこを引き受けるのが**特異値分解（SVD）**です。どんな形の行列でも、**回転 → 伸縮 → 回転**の 3 つに分解できます。しかも伸縮の量（特異値）は大きい順に並ぶので、**上位いくつかだけ残せば元の行列をほぼ再現できます。** 画像圧縮も、主成分分析も、大規模言語モデルの LoRA も、この一点から出てきます。

# 形はこうなる

$m \\times n$ の行列 $\\mathbf{A}$ は、必ず次の形に書けます。

\`\`\`math
\\mathbf{A} = \\mathbf{U} \\mathbf{\\Sigma} \\mathbf{V}^{T}
\`\`\`

| 記号 | 形 | 中身 |
| --- | --- | --- |
| $\\mathbf{U}$ | $(m, m)$ | 直交行列。列を**左特異ベクトル**という |
| $\\mathbf{\\Sigma}$ | $(m, n)$ | 対角だけに値がある。対角成分が**特異値** $\\sigma_1 \\ge \\sigma_2 \\ge \\ldots \\ge 0$ |
| $\\mathbf{V}^{T}$ | $(n, n)$ | 直交行列。$\\mathbf{V}$ の列を**右特異ベクトル**という |

固有値分解 $\\mathbf{V}\\mathbf{\\Lambda}\\mathbf{V}^{T}$ と見比べると、**両側の直交行列が別物になっただけ**だと分かります。入口と出口で次元が違うのだから、行き先の座標系と来た元の座標系が違うのは当然、と考えると腑に落ちます。

\`\`\`diagram:compare
title: 固有値分解と特異値分解
actors: 固有値分解 | 特異値分解
正方行列限定。対角化できるとは限らない
どんな形の行列にも必ず存在する
対称なら A = VΛVᵀ
つねに A = UΣVᵀ
固有値は負や複素数もあり得る
特異値は必ず 0 以上の実数
両側が同じ直交行列 V
左右で別の直交行列 U と V
\`\`\`

> **試験のポイント** **特異値は必ず 0 以上**です。負の特異値はありません。固有値との最大の違いなので、選択肢に「特異値が負になる場合がある」と出たら誤りです。

# 特異値の正体

$\\mathbf{A}^{T}\\mathbf{A}$ は必ず実対称な正方行列になります。その固有値を大きい順に並べたときの、上位 $\\min(m, n)$ 個の**平方根**が特異値です（特異値は $\\min(m, n)$ 個、$\\mathbf{A}^{T}\\mathbf{A}$ の固有値は $n$ 個なので、余った分は 0 になります）。

\`\`\`math
\\sigma_i = \\sqrt{\\lambda_i(\\mathbf{A}^{T}\\mathbf{A})}
\`\`\`

$\\mathbf{A}^{T}\\mathbf{A}$ は半正定値なので固有値は 0 以上、したがって平方根が実数になり、特異値も 0 以上になります。**「特異値が非負」の理由はここにあります。**

0 でない特異値の個数が、その行列の**ランク**です。ランクは「その行列が実質何次元ぶんの情報を持っているか」を表します。

# 低ランク近似：上位 $k$ 個だけ残す

特異値は大きい順に並んでいます。そこで上位 $k$ 個だけを残し、残りを 0 にしてしまうのが**打ち切り特異値分解（低ランク近似）**です。

\`\`\`math
\\mathbf{A} \\approx \\mathbf{U}_k \\mathbf{\\Sigma}_k \\mathbf{V}_k^{T}
\`\`\`

このとき $\\mathbf{U}_k$ は $(m, k)$、$\\mathbf{V}_k$ は $(n, k)$ です。**この近似は、ランク $k$ 以下の行列のうち、フロベニウスノルム（全成分の 2 乗和の平方根）で測った誤差を最小にする**ことが知られています（エッカート・ヤングの定理）。「とりあえず上位を残す」が、実は最善手だということです。ただし「どんな測り方でも最小」ではありません。成り立つのはフロベニウスノルムやスペクトルノルムで測った場合です。

持つべき数の個数を比べると、圧縮になっている理由が見えます。

| 方式 | 保持する数の個数 | $m = n = 1000, k = 10$ なら |
| --- | --- | --- |
| そのまま | $mn$ | 1,000,000 |
| ランク $k$ 近似 | $k(m + n + 1)$ | 20,010 |

**$k$ が小さいほど劇的に減ります。** 減った分だけ情報も落ちますが、特異値が急に小さくなる行列（現実のデータはたいていそう）なら、落ちるのはほとんどノイズです。

# どこで使われているか

\`\`\`diagram:tree
title: 低ランク近似の顔ぶれ
主成分分析（PCA） :: 行がサンプルのデータ行列を中心化して SVD を取ると、右特異ベクトルが主成分の方向になる
  寄与率 :: 特異値の 2 乗の割合。「上位 2 つで 8 割」の 8 割がこれ
画像・行列の圧縮 :: 特異値の大きい成分だけで画像がほぼ復元できる
レコメンデーション :: 評価行列を低ランクに分解し、埋まっていないマスを推定する
LoRA :: 重みの更新量を低ランクの積 BA で表し、更新するパラメータを大幅に減らす
擬似逆行列 :: 逆行列が存在しない場合でも、最小二乗解を与える
\`\`\`

**LoRA は「新しい発明」というより、この節の考え方を巨大モデルの微調整に持ち込んだもの**です。$(d, d)$ の重み更新を $(d, r)$ と $(r, d)$ の積で表せば、$r$ が小さいほどパラメータが減ります。事前学習済みの重みは凍結したまま、この差分だけを学習します。

> **よくある勘違い** LoRA は重みの更新量を SVD で打ち切っているわけではありません。**最初から $\\mathbf{B}\\mathbf{A}$ という低ランクの形でパラメータを持ち、それを勾配法で学習します。**借りているのは「低ランクで足りる」という発想のほうです。

# コードで確かめる

\`\`\`python
import numpy as np

A = np.random.rand(6, 4)
U, s, Vt = np.linalg.svd(A, full_matrices=False)
print(U.shape, s.shape, Vt.shape)   # (6, 4) (4,) (4, 4)
print(s)                            # 大きい順に並んだ特異値

k = 2                               # 上位 2 つだけ残す
A_k = U[:, :k] @ np.diag(s[:k]) @ Vt[:k, :]
print(np.linalg.norm(A - A_k))      # 打ち切った分だけの誤差
print((s[:k] ** 2).sum() / (s ** 2).sum())   # 残った割合。中心化したデータ行列なら累積寄与率
\`\`\`

NumPy の \`svd\` は $\\mathbf{V}$ ではなく**転置した $\\mathbf{V}^{T}$ を返す**ことと、特異値を**行列ではなく 1 次元配列で返す**ことに注意してください。実装問題で問われやすい箇所です。

> **よくある勘違い** 「SVD は PCA と別物」ではありません。**行がサンプル・列が特徴量のデータ行列を中心化してから SVD を取ると、右特異ベクトルが主成分の方向そのもの**です（共分散行列の固有ベクトルと一致します）。ただし一致するのは**方向**であって値ではなく、分散との関係は $\\lambda_i = \\sigma_i^2 / (n - 1)$ です。行と列が逆に並んだデータ行列なら、主成分になるのは左特異ベクトルのほうです。共分散行列を作らずに済むぶん、数値的にはこちらが安定です。

# この節のまとめ

- 特異値分解はどんな形の行列にも使える。$\\mathbf{A} = \\mathbf{U}\\mathbf{\\Sigma}\\mathbf{V}^{T}$ で、両側が直交行列、真ん中が対角
- 特異値は $\\mathbf{A}^{T}\\mathbf{A}$ の固有値の平方根なので、**必ず 0 以上**
- 0 でない特異値の個数がランク＝その行列が実質持っている情報の次元
- 上位 $k$ 個だけ残す低ランク近似は、ランク $k$ の中で誤差最小の近似になる
- 中心化したデータ行列の SVD は PCA と一致し、特異値の 2 乗の割合が寄与率になる
- LoRA は重みの更新量を低ランクの積で表す手法で、発想はこの節と同じ

\`\`\`quiz
固有値分解と特異値分解の、いちばん大きな違いは何ですか :: 固有値分解は正方行列にしか使えませんが、特異値分解はどんな形の行列にも使えます。
特異値が負になることはありますか :: ありません。AᵀA の固有値（0 以上）の平方根なので、必ず 0 以上です。
行列のランクは、特異値で言うとどう表せますか :: 0 でない特異値の個数です。
上位 k 個の特異値だけを残した近似は、どういう意味で最良ですか :: ランク k 以下の行列の中で、フロベニウスノルムで測った誤差が最小になります（エッカート・ヤングの定理）。
PCA と SVD の関係は :: データを中心化してから SVD を取ると、右特異ベクトルが主成分に一致します。共分散行列の固有値分解と同じ結果です。
LoRA が特異値分解の考え方とつながるのはなぜですか :: 重みの更新量を低ランクの行列の積で表し、少ないパラメータで近似しているからです。ただし SVD を計算しているわけではなく、最初から低ランクの形で持って学習します。
\`\`\`
`,
  },
  {
    id: 'm-prob-1',
    categoryId: 'm-prob',
    title: '確率の基礎とベイズの定理',
    goal: '同時確率・条件付き確率・周辺化を式で書き分けられるようになり、ベイズの定理を計算に使える',
    minutes: 25,
    body: `
# ざっくり言うと

機械学習がやっているのは、**データを見て「もっともらしいパラメータ」を選ぶ**ことです。その「もっともらしさ」を測る物差しが確率です。

この節で押さえるのは 3 つだけです。**同時（両方が起きる）・条件付き（片方が起きたとして）・周辺（片方を無視する）**の書き分けと、それをひっくり返すベイズの定理、そして期待値と分散。損失関数の正体は次の節で明かしますが、その材料はすべてここで揃います。

# 確率変数と確率分布

サイコロの目、明日の気温、画像のラベル。**値が確率的に決まるもの**を確率変数といい、大文字 $X$ で、その実現値を小文字 $x$ で書くのが慣例です。

| | 離散（とびとび） | 連続（つながっている） |
| --- | --- | --- |
| 分布の呼び名 | 確率質量関数 | 確率密度関数 |
| $p(x)$ の意味 | その値をとる確率そのもの | その付近の**確率の濃さ** |
| 全体の和 | $\\sum_x p(x) = 1$ | $\\int p(x) dx = 1$ |
| 例 | サイコロ、クラスラベル | 身長、重みの値 |

> **よくある勘違い** 確率密度は 1 を超えても構いません。たとえば $0$ から $0.5$ の一様分布の密度は $2$ です。**1 以下でなければならないのは確率であって、密度ではありません。** 密度は積分して 1 になればよいのです。

# 同時・条件付き・周辺

3 つの関係は、次の 1 本の式（乗法定理）で結ばれています。

\`\`\`math
p(x, y) = p(y \\mid x) p(x) = p(x \\mid y) p(y)
\`\`\`

$p(y \\mid x)$ は「$x$ が起きたという前提での $y$ の確率」で、**縦棒の右が前提**です。前提の側と結果の側を取り違えると、後で出てくるベイズの定理がまるごと逆になります。

片方の変数を消したいときは、**その変数について足す（積分する）**だけです。これを周辺化といいます。

\`\`\`math
p(x) = \\sum_y p(x, y)
\`\`\`

$x$ と $y$ が**独立**とは、$p(x, y) = p(x)p(y)$ が成り立つこと、言い換えれば $p(y \\mid x) = p(y)$（前提を知っても確率が変わらない）ことです。

> **試験のポイント** 「独立」と「排反（同時には起きない）」は別物です。どちらも確率が 0 でない排反な 2 つの事象は、片方が起きた時点でもう片方の確率が 0 になるので、**むしろ独立ではありません。**

# ベイズの定理

乗法定理の 2 つの表し方を等号で結び、片方を移項するだけで得られます。

\`\`\`math
p(\\theta \\mid x) = \\frac{p(x \\mid \\theta) p(\\theta)}{p(x)}
\`\`\`

名前が付いているので身構えますが、やっていることは**前提と結果の入れ替え**です。機械学習では次の読み方をします。

| 記号 | 呼び名 | 読み方 |
| --- | --- | --- |
| $p(\\theta)$ | 事前分布 | データを見る前に持っていた見込み |
| $p(x \\mid \\theta)$ | 尤度 | そのパラメータなら、このデータはどれくらい出やすいか |
| $p(\\theta \\mid x)$ | 事後分布 | データを見た後の見込み |
| $p(x)$ | 周辺尤度（エビデンス） | 全体を 1 に揃えるための割り算。$\\theta$ を含まない |

分母は $\\theta$ によらない定数なので、比べるだけなら無視できます。ここから**「事後 $\\propto$ 尤度 × 事前」**という覚え方が出てきます。次の節の最尤推定と MAP 推定の違いは、この式のどこまでを使うかの違いです。

## 数字を入れてみる

1000 人に 1 人が罹る病気の検査があり、**罹っている人の 99% が陽性**、**罹っていない人の 5% も陽性**になるとします。陽性と出た人が実際に罹っている確率はいくらでしょうか。

10 万人で考えると数えるだけで済みます。

\`\`\`diagram:flow
title: 10 万人を数える
罹患者 100 人 :: そのうち陽性は 99 人
非罹患者 99,900 人 :: そのうち陽性は 4,995 人
陽性は合計 5,094 人 :: 本当に罹っているのは 99 人
的中率は約 1.9% :: 陽性でも、ほとんどは罹っていない
\`\`\`

**検査の精度が高くても、もともと珍しい病気なら陽性の大半は誤り**です。事前分布（有病率）が効くとはこういうことで、不均衡データで正解率を見てはいけない理由とも地続きです。

# 期待値・分散・共分散

\`\`\`math
\\mathrm{E}[X] = \\sum_x x   p(x)
\`\`\`

期待値は「値を確率で重み付けした平均」です。次の 2 つの性質は、後の章で何度も使います。

- **線形性**：$\\mathrm{E}[aX + bY] = a\\mathrm{E}[X] + b\\mathrm{E}[Y]$。$X$ と $Y$ が独立でなくても成り立ちます
- 独立なら積も分解できる：$\\mathrm{E}[XY] = \\mathrm{E}[X]\\mathrm{E}[Y]$

分散は「平均からのずれの 2 乗の期待値」です。

\`\`\`math
\\mathrm{Var}[X] = \\mathrm{E}[(X - \\mathrm{E}[X])^2] = \\mathrm{E}[X^2] - (\\mathrm{E}[X])^2
\`\`\`

右側の形（2 乗の平均 − 平均の 2 乗）は計算問題で便利なので覚えておく価値があります。2 変数版が共分散 $\\mathrm{Cov}[X, Y] = \\mathrm{E}[XY] - \\mathrm{E}[X]\\mathrm{E}[Y]$ で、これを各々の標準偏差で割って $-1$ 〜 $1$ に収めたものが相関係数です。

> **よくある勘違い** 相関がないこと（無相関）と独立は同じではありません。**独立ならば無相関**ですが、逆は成り立ちません。相関係数は直線的な関係しか捉えられないので、$Y = X^2$ のような関係は相関 0 になり得ます。

# 機械学習とのつながり

本当に小さくしたいのは、**まだ見ぬデータ全体に対する損失の期待値**（期待損失、汎化誤差）です。しかし真の分布は分からないので、手元のデータでの平均（経験損失）で代用します。

\`\`\`math
\\mathrm{E}[L] \\approx \\frac{1}{N} \\sum_{i=1}^{N} L(y_i, \\hat{y}_i)
\`\`\`

**ミニバッチ学習は、この和をさらに一部のデータで済ませているだけ**です。だからミニバッチの勾配は真の勾配のばらつきのある推定値になり、SGD の「ノイズ」の正体もここにあります。バッチサイズを大きくすると勾配が安定するのは、標本数を増やせば平均のばらつきが減るという、ただそれだけの理由です。

# この節のまとめ

- 離散なら確率質量、連続なら確率密度。**密度は 1 を超えてよい**（積分が 1 になればよい）
- 乗法定理 $p(x, y) = p(y \\mid x)p(x)$ が同時・条件付き・周辺をつなぐ。縦棒の右が前提
- 消したい変数について足す（積分する）のが周辺化
- ベイズの定理は前提と結果の入れ替え。**事後 $\\propto$ 尤度 × 事前**
- 珍しい事象では、精度の高い検査でも陽性の的中率は低くなる（事前分布が効く）
- 期待値は線形。分散は「2 乗の平均 − 平均の 2 乗」。独立なら無相関だが、逆は言えない

\`\`\`quiz
確率密度が 1 を超えることはありますか :: あります。1 以下でなければならないのは確率で、密度は積分して 1 になればよいからです。
p(y | x) の縦棒の右にあるのは、前提と結果のどちらですか :: 前提です。x が起きたという条件のもとでの y の確率を表します。
周辺化とは何をする操作ですか :: 消したい変数について和を取る（連続なら積分する）ことです。
ベイズの定理を「事後・尤度・事前」で言うとどうなりますか :: 事後分布は尤度と事前分布の積に比例します。分母の周辺尤度は正規化のための定数です。
有病率が低い病気では、精度の高い検査で陽性でも的中率が低くなるのはなぜですか :: 母数の大きい非罹患者から出る偽陽性の数が、罹患者の真陽性の数を上回るからです。
独立と無相関の関係は :: 独立ならば無相関ですが、無相関でも独立とは限りません。相関係数は直線的な関係しか捉えないためです。
\`\`\`
`,
  },
  {
    id: 'm-prob-2',
    categoryId: 'm-prob',
    title: '代表的な確率分布',
    goal: '主要な分布の使い分けを掴み、出力層の活性化関数がどの分布に対応するかを説明できる',
    minutes: 20,
    body: `
# ざっくり言うと

分布は種類が多く、名前を全部覚えるのは面倒に見えます。しかし E資格で必要なのは、**「どんな種類の値を表す分布か」**という対応だけです。

しかも深層学習では、**出力層の活性化関数がそのまま分布の選択**になっています。シグモイドはベルヌーイ分布、ソフトマックスはカテゴリカル分布、活性化なし（恒等写像）はガウス分布。この対応を掴むと、次の節で損失関数が自動的に決まります。

# 離散の分布

| 分布 | 表すもの | パラメータ | 期待値 |
| --- | --- | --- | --- |
| ベルヌーイ | コイン 1 回。0 か 1 | $p$ | $p$ |
| 二項 | コイン $n$ 回のうち表の回数 | $n, p$ | $np$ |
| カテゴリカル | サイコロ 1 回。$K$ 個から 1 つ | $p_1, \\ldots, p_K$ | — |
| 多項 | サイコロ $n$ 回の出目の集計 | $n, p_1, \\ldots, p_K$ | — |
| ポアソン | 一定時間に起きる回数 | $\\lambda$ | $\\lambda$ |

**ベルヌーイを $n$ 回束ねたものが二項、カテゴリカルを $n$ 回束ねたものが多項**、という関係で覚えると 4 つが 2 つになります。二値分類はベルヌーイ、多クラス分類はカテゴリカルです。

パラメータを動かして、形と期待値・分散の変わり方を見てください。

\`\`\`widget:distribution
\`\`\`

# 連続の分布

いちばん重要なのはガウス分布（正規分布）です。

\`\`\`math
p(x) = \\frac{1}{\\sqrt{2\\pi\\sigma^2}} \\exp\\left(-\\frac{(x - \\mu)^2}{2\\sigma^2}\\right)
\`\`\`

式を覚える必要はありませんが、**指数の肩に $(x - \\mu)^2$ が乗っている**という一点だけは押さえてください。次の節で対数を取ると、この 2 乗がそのまま出てきて**二乗誤差**になります。

多変量版では、平均がベクトル $\\mathbf{\\mu}$、分散が**共分散行列** $\\mathbf{\\Sigma}$ になります。共分散行列が対角なら各次元が独立、対角のうえに全部同じ値なら等方的（球状）です。

| 分布 | 特徴 | 深層学習での出番 |
| --- | --- | --- |
| ガウス | 左右対称の釣り鐘型 | 回帰の出力、重みの初期化、VAE の潜在変数 |
| 一様 | 範囲内が等しい | 初期化、データ拡張の乱数 |
| ラプラス | ガウスより尖り、裾が重い | L1 正則化に対応する事前分布 |
| 指数 | 待ち時間。減衰する形 | 生存時間、待ち行列 |
| ベータ・ディリクレ | 確率そのものの分布（0〜1） | ベルヌーイ・カテゴリカルの共役事前分布 |

**ラプラス分布が L1 正則化に、ガウス分布が L2 正則化に対応する**という話は次の節で扱います。名前の対応だけ頭の隅に置いてください。

# 混合分布

1 つの山では表せないデータは、複数のガウス分布を重み付きで足した**混合ガウス分布（GMM）**で表せます。

\`\`\`math
p(x) = \\sum_{k=1}^{K} \\pi_k \\mathcal{N}(x \\mid \\mu_k, \\sigma_k^2)
\`\`\`

$\\pi_k$ は各山の重みで、合計 1 です。**どの山から来たか**という隠れた変数を導入して交互に推定するのが EM アルゴリズムで、クラスタリング手法としても出題されます。

# 標本の性質：大数の法則と中心極限定理

\`\`\`diagram:compare
title: 2 つの定理の言っていること
actors: 大数の法則 | 中心極限定理
標本平均は真の期待値に近づく
標本平均の分布がガウス分布に近づく
「たくさん集めれば正しい値になる」
「たくさん集めればばらつきの形が決まる」
経験損失で期待損失を代用してよい根拠
標準誤差や信頼区間の根拠
標本数を増やすほど誤差が減る
分散が有限なら、元の分布の形によらず成り立つ
\`\`\`

> **試験のポイント** 中心極限定理が言っているのは「**標本平均**の分布がガウスに近づく」ことであって、「データそのものがガウスになる」ことではありません。ここは選択肢で狙われます。

# 出力層と分布の対応

**この表がこの節でいちばん役に立ちます。**

| タスク | 出力の分布 | 出力層の活性化 | 次の節で決まる損失 |
| --- | --- | --- | --- |
| 二値分類 | ベルヌーイ | シグモイド | 二値交差エントロピー |
| 多クラス分類（排他） | カテゴリカル | ソフトマックス | 交差エントロピー |
| 多ラベル分類（排他でない） | 各クラスがベルヌーイ | 各出力にシグモイド | クラスごとの二値交差エントロピー |
| 回帰 | ガウス | なし（恒等） | 二乗誤差 |

**シグモイドはベルヌーイ分布のパラメータ $p$ を出力している**、と読めるようになるのが理想です。出力を「確率らしきもの」ではなく「分布のパラメータ」と見ると、損失関数が自動的に決まります。

> **よくある勘違い** ソフトマックスとシグモイドは「多クラスか二値か」で選ぶのではなく、**クラスが排他かどうか**で選びます。1 枚の画像に犬と猫が同時に写り得る多ラベル問題では、クラス数が多くてもソフトマックスではなく**各出力にシグモイド**を使います。

# この節のまとめ

- ベルヌーイを束ねたのが二項、カテゴリカルを束ねたのが多項
- ガウス分布は指数の肩に $(x - \\mu)^2$ が乗る。多変量版の分散は共分散行列になる
- ラプラス分布は L1、ガウス分布は L2 の正則化に対応する（詳しくは次の節）
- 混合ガウス分布は複数の山の重み付き和。推定には EM アルゴリズムを使う
- 中心極限定理が言うのは「標本平均の分布」がガウスに近づくこと。データ自体ではない
- 標本平均の**分散は $\\sigma^2 / n$**、標準偏差（**標準誤差**）は $\\sigma / \\sqrt{n}$。**データを 4 倍集めても、ばらつきは半分にしかならない**
- 出力層の活性化は分布の選択そのもの。シグモイド＝ベルヌーイ、ソフトマックス＝カテゴリカル、恒等＝ガウス

\`\`\`quiz
二項分布とベルヌーイ分布の関係は :: ベルヌーイ試行を n 回繰り返したときの成功回数の分布が二項分布です。
多変量ガウス分布のパラメータは何ですか :: 平均ベクトルと共分散行列です。共分散行列が対角なら各次元が独立になります。
中心極限定理は何がガウス分布に近づくと言っていますか :: 標本平均の分布です。データそのものの分布ではありません。
多ラベル分類（1 枚に複数のラベルが付き得る）で使う出力層の活性化は :: 各出力にシグモイドです。クラスが排他でないのでソフトマックスは使いません。
回帰の出力層に活性化関数を置かないのは、どの分布を仮定していることになりますか :: ガウス分布です。その結果、損失は二乗誤差になります。
混合ガウス分布のパラメータ推定に使われる代表的なアルゴリズムは :: EM アルゴリズムです。どの山から生成されたかという隠れ変数を交互に推定します。
\`\`\`
`,
  },
  {
    id: 'm-prob-3',
    categoryId: 'm-prob',
    title: '最尤推定・MAP 推定・ベイズ推定',
    goal: '損失関数が負の対数尤度から出てくることを理解し、正則化が事前分布に対応することを説明できる',
    minutes: 25,
    body: `
# ざっくり言うと

**損失関数は誰かが思い付きで決めたものではありません。** 「出力がどんな分布に従うと仮定するか」を決めた瞬間に、損失は自動的に決まります。その仕組みが最尤推定です。

さらに、そこに事前分布を足すと正則化項が現れます。**二乗誤差も交差エントロピーも L2 正則化も、この節の 1 本の考え方から出てくる**というのが今回の主題です。丸暗記していた式が減るので、費用対効果はきわめて高い節です。

# 尤度と最尤推定

パラメータ $\\theta$ を固定したときに、手元のデータ $D$ がどれくらい出やすいか。これが**尤度**です。データが独立同分布なら、各データの確率の積になります。

\`\`\`math
p(D \\mid \\theta) = \\prod_{i=1}^{N} p(x_i \\mid \\theta)
\`\`\`

積のままでは扱いにくいので、対数を取ります。理由は 3 つあり、どれも実装上の実感を伴います。

- 積が**和**になって微分しやすい
- 小さい確率を掛け続けて 0 になる（アンダーフロー）のを防げる
- 対数は単調増加なので、**最大にする $\\theta$ は変わらない**

こうして得られるのが**最尤推定（MLE）**です。

\`\`\`math
\\hat{\\theta} = \\arg\\max_{\\theta} \\sum_{i=1}^{N} \\log p(x_i \\mid \\theta)
\`\`\`

最適化は最小化で書きたいので、符号を反転します。**これが「負の対数尤度（NLL）」で、損失関数の正体です。**

> **試験のポイント** 「損失関数を最小化する」ことと「尤度を最大化する」ことは、**同じことを符号違いで言っているだけ**です。この対応が言えるかどうかが、この分野の理解の分かれ目になります。

# 分布を決めれば損失が決まる

前の節の「出力層と分布の対応」に、負の対数尤度を当てはめてみます。

**ガウス分布**（分散を固定）を仮定すると、密度の指数の肩にあった $(y - \\hat{y})^2$ が、対数を取ることでそのまま降りてきます。

\`\`\`math
-\\log p(y \\mid \\hat{y}) = \\frac{(y - \\hat{y})^2}{2\\sigma^2} + \\mathrm{const}
\`\`\`

定数と定数倍は最小化に影響しないので、残るのは $(y - \\hat{y})^2$、つまり**二乗誤差**です。

**ベルヌーイ分布**を仮定すると、$p(y \\mid \\hat{y}) = \\hat{y}^{y}(1 - \\hat{y})^{1-y}$ の対数を取って符号を反転し、**二値交差エントロピー**が出てきます。

\`\`\`math
L = -[  y \\log \\hat{y} + (1 - y) \\log (1 - \\hat{y})  ]
\`\`\`

**カテゴリカル分布**なら $L = -\\sum_k y_k \\log \\hat{y}_k$ です。正解ラベルが one-hot なら、正解クラス以外の項はすべて 0 になるので、実質 $-\\log \\hat{y}_{\\text{正解}}$ の 1 項だけが残ります。

| 仮定する分布 | 出力層 | 負の対数尤度 ＝ 損失 |
| --- | --- | --- |
| ガウス | 恒等 | 二乗誤差 |
| ベルヌーイ | シグモイド | 二値交差エントロピー |
| カテゴリカル | ソフトマックス | 交差エントロピー |

> **よくある勘違い** 「分類だから交差エントロピー」ではなく、「**カテゴリカル分布の負の対数尤度だから**交差エントロピー」です。この順序で理解しておくと、見たことのないタスクでも損失を自分で組み立てられます。

# MAP 推定：事前分布を足すと正則化になる

最尤推定はデータだけを見るので、データが少ないと極端な答えに飛びつきます（過学習）。そこでベイズの定理を使い、**事後分布を最大にする $\\theta$** を選ぶのが**MAP 推定（最大事後確率推定）**です。

\`\`\`math
\\hat{\\theta} = \\arg\\max_{\\theta} [  \\log p(D \\mid \\theta) + \\log p(\\theta)  ]
\`\`\`

足された第 2 項が事前分布の対数です。ここに具体的な分布を入れると、見慣れた正則化項が現れます。

\`\`\`diagram:compare
title: 事前分布と正則化の対応
actors: 平均 0 のガウス事前 | 平均 0 のラプラス事前
対数を取ると重みの 2 乗和
対数を取ると重みの絶対値の和
L2 正則化（重み減衰）
L1 正則化
重みを全体的に小さくする
重みをぴったり 0 にしやすい
分散が小さいほど強い正則化
尖りが強いほど強い正則化
\`\`\`

> **試験のポイント** **正則化項は事前分布の対数そのもの**です。「L2 正則化 ⇔ ガウス事前」「L1 正則化 ⇔ ラプラス事前」の対応は頻出で、深層学習の正則化の章でも再登場します。

# ベイズ推定：点で答えず、分布で答える

MAP 推定は事後分布の**山の頂上だけ**を取ります。これに対しベイズ推定は、**事後分布そのものを答えとします。** 予測するときは、パラメータの可能性すべてで重み付けた平均を取ります。

\`\`\`math
p(y \\mid x, D) = \\int p(y \\mid x, \\theta) p(\\theta \\mid D) d\\theta
\`\`\`

利点は、**予測に「自信のなさ」が付いてくる**ことです。欠点は積分が解けないことで、実用では近似します（変分推論、MCMC、モンテカルロドロップアウトなど）。**VAE の背後にあるのが変分推論**なので、生成モデルの章でここに戻ってきます。

なお、事後分布が事前分布と同じ種類になる組み合わせを**共役**といいます（ベルヌーイとベータ、カテゴリカルとディリクレなど）。積分が解析的に解ける代表例です。

\`\`\`diagram:compare
title: 3 つの推定の違い
actors: 何を求めるか | 過学習への強さ
最尤推定：尤度が最大の 1 点
データが少ないと過学習しやすい
MAP 推定：事後確率が最大の 1 点
事前分布のぶんだけ抑えが効く（＝正則化）
ベイズ推定：事後分布そのもの
不確かさも扱えるが計算が重い
どれもデータが増えれば近づく
事前分布の影響は相対的に薄れる
\`\`\`

# 推定量の良し悪し

推定量には「たくさん試したとき平均的に当たるか（**不偏性**）」という尺度があります。分散を推定するとき $n - 1$ で割るのは、**平均も同じ標本から推定している**からです。標本平均のまわりで測った 2 乗和は、真の平均のまわりで測った場合より必ず小さくなるので、$n$ で割ると真の分散より小さめに偏ります。

> **よくある勘違い** 最尤推定量は不偏とは限りません。実際、ガウス分布の分散の最尤推定は $n$ で割る形になり、**過小評価に偏ります**（だから不偏分散では $n - 1$ で割る）。「最尤だから正しい」ではありません。

# コードで確かめる

\`\`\`python
import numpy as np

# コインを 10 回投げて 7 回表だった。表の出る確率 p の最尤推定は？
data = np.array([1, 1, 0, 1, 1, 1, 0, 1, 1, 0])

def log_likelihood(p):
    return (data * np.log(p) + (1 - data) * np.log(1 - p)).sum()

for p in [0.5, 0.6, 0.7, 0.8]:
    print(p, round(log_likelihood(p), 3))
# 0.7 で最大になる。=> 最尤推定は「観測した割合そのもの」
\`\`\`

**ベルヌーイ分布の最尤推定は、単なる標本比率**になります。10 回中 7 回なら 0.7 です。式を解かなくても、log_likelihood を並べてみれば形が見えます。

# この節のまとめ

- 尤度は「そのパラメータのもとでデータが出やすいか」。対数を取れば積が和になり、微分もアンダーフロー対策も楽になる
- 損失関数の正体は**負の対数尤度**。最小化と最尤化は符号違いの同じ操作
- ガウスを仮定すれば二乗誤差、ベルヌーイなら二値交差エントロピー、カテゴリカルなら交差エントロピー
- MAP 推定は尤度に事前分布の対数を足したもので、**その項が正則化項**になる
- ガウス事前は L2 正則化、ラプラス事前は L1 正則化に対応する
- ベイズ推定は事後分布全体を扱い不確かさも出せるが、積分が解けないので近似する（変分推論は VAE につながる）

\`\`\`quiz
損失関数の最小化と尤度の最大化は、どういう関係ですか :: 同じことです。損失関数は負の対数尤度なので、符号を反転しただけの関係にあります。
尤度の対数を取る理由を 3 つ挙げてください :: 積が和になって微分しやすい、アンダーフローを防げる、単調増加なので最大を与える点が変わらない、の 3 つです。
出力にガウス分布を仮定すると、損失は何になりますか :: 二乗誤差です。密度の指数にある (y - ŷ)² が対数を取って降りてくるためです。
MAP 推定と最尤推定の違いは :: MAP 推定は対数尤度に事前分布の対数を足して最大化します。その追加項が正則化項として働きます。
L1 正則化に対応する事前分布は何ですか :: 平均 0 のラプラス分布です。L2 正則化は平均 0 のガウス分布に対応します。
最尤推定量は必ず不偏ですか :: いいえ。ガウス分布の分散の最尤推定は n で割る形になり、真の分散より小さめに偏ります。
\`\`\`
`,
  },
  {
    id: 'm-info-1',
    categoryId: 'm-info',
    title: '自己情報量とエントロピー',
    goal: '「驚きの大きさ」を数値化する道具として情報量を掴み、エントロピーの計算と最大・最小を判断できる',
    minutes: 20,
    body: `
# ざっくり言うと

情報理論は「**どれくらい驚いたか**」を数字にする分野です。

「明日は太陽が昇る」と言われても驚きません（確率が高い＝情報量が小さい）。「明日は雪が降る」と言われれば驚きます（確率が低い＝情報量が大きい）。この直感をそのまま式にしたのが自己情報量で、その平均がエントロピーです。

短い章ですが、**分類の損失関数（交差エントロピー）も、生成モデルの目的関数も、決定木の分岐基準も、すべてここから出てきます。** 費用対効果は全範囲で最上位です。

# 自己情報量：珍しさを測る

\`\`\`math
I(x) = -\\log p(x)
\`\`\`

マイナスが付いているのは、確率が小さいほど大きな値にしたいからです。$p = 1$ なら $I = 0$（確実に起きることには情報がない）、$p$ が 0 に近づけば $I$ は際限なく大きくなります。

**なぜ対数なのか**には理由があります。独立な 2 つの出来事が同時に起きる確率は掛け算になりますが、**情報量は足し算であってほしい**からです。掛け算を足し算に変えてくれるのが対数です。

| 底 | 単位 | よく使う場面 |
| --- | --- | --- |
| 2 | ビット（bit） | 符号化、試験の計算問題 |
| $e$ | ナット（nat） | 機械学習の実装（\`np.log\` の既定） |

> **試験のポイント** 底が変わると値は定数倍だけ変わります（$\\log_2 x = \\log_e x / \\log_e 2$）。**最適化の答えは底に依存しません**が、「何ビットか」を聞かれたら底 2 で計算します。

# エントロピー：情報量の平均

\`\`\`math
H(P) = -\\sum_x p(x) \\log p(x)
\`\`\`

自己情報量を、その確率で重み付けて平均したものです。**「その分布がどれくらい予測しにくいか」の尺度**だと思ってください。

具体的な数字で見ると腑に落ちます（底は 2）。

| 分布 | 計算 | エントロピー |
| --- | --- | --- |
| 表裏が半々のコイン | $-(0.5\\log_2 0.5) \\times 2$ | 1 ビット |
| 表が 90% のコイン | $-(0.9\\log_2 0.9 + 0.1\\log_2 0.1)$ | 約 0.47 ビット |
| 必ず表が出るコイン | $-(1 \\log_2 1)$ | 0 ビット |
| 公平な 4 択 | $-4 \\times (0.25 \\log_2 0.25)$ | 2 ビット |

\`\`\`diagram:flow
title: エントロピーの大小
偏りがない :: 最大。K 択の一様分布なら log₂K ビット
少し偏る :: 減る
完全に決まっている :: 0。驚きが一切ない
\`\`\`

> **試験のポイント** エントロピーが**最大になるのは一様分布**、**最小（0）になるのは値が 1 つに決まっているとき**です。この 2 つの端を覚えておけば、選択肢の大小比較はほぼ解けます。

なお $p(x) = 0$ の項は $0 \\log 0 = 0$ と約束します（極限がそうなるため）。実装では \`log(0)\` が $-\\infty$ になるので、微小な値を足して回避します。

# 結合・条件付き・相互情報量

変数が 2 つになると、次の 3 つが並びます。

| 名前 | 記号 | 意味 |
| --- | --- | --- |
| 結合エントロピー | $H(X, Y)$ | 2 つまとめての不確かさ |
| 条件付きエントロピー | $H(Y \\mid X)$ | $X$ を知ったうえでの $Y$ の不確かさ |
| 相互情報量 | $I(X; Y)$ | $X$ を知ることで減る $Y$ の不確かさ |

\`\`\`math
H(X, Y) = H(X) + H(Y \\mid X)
I(X; Y) = H(Y) - H(Y \\mid X) = H(X) + H(Y) - H(X, Y)
\`\`\`

**「知る前の不確かさ」から「知った後の不確かさ」を引いたもの**が相互情報量です。$X$ と $Y$ が独立なら、知っても何も減らないので $I(X; Y) = 0$ になります。

この差は、決定木で**情報利得**と呼ばれる量そのものです。決定木は「どの特徴で分けるとエントロピーが最も減るか」を貪欲に選んでいるだけで、**その判断基準がここにあります。**

# コードで確かめる

\`\`\`python
import numpy as np

def entropy(p, base=2):
    p = np.asarray(p, dtype=float)
    p = p[p > 0]                       # 0 の項は 0 として除く
    return float(-(p * (np.log(p) / np.log(base))).sum())

print(entropy([0.5, 0.5]))             # 1.0    公平なコイン
print(entropy([0.9, 0.1]))             # 0.469  偏ったコイン
print(entropy([0.25] * 4))             # 2.0    公平な 4 択
print(entropy([1.0]))                  # -0.0   決まりきった結果（符号は表示上のもの）
\`\`\`

2 値の場合の形は、次の道具で確かめられます。**偏るほど小さく、半々のとき最大**という山形になります。

\`\`\`widget:entropy
\`\`\`

> **よくある勘違い** エントロピーは「情報の量が多い＝良い」という価値の話ではありません。**予測しにくさの尺度**です。モデルの出力分布のエントロピーが高いなら、それは「自信がない」ことを意味します。

# この節のまとめ

- 自己情報量 $-\\log p(x)$ は「珍しさ・驚きの大きさ」。確率 1 なら 0
- 対数を使うのは、独立な事象の情報量を足し算にするため
- エントロピーは自己情報量の平均で、その分布の予測しにくさを表す
- 最大は一様分布（$K$ 択なら $\\log_2 K$ ビット）、最小は 0（結果が確定しているとき）
- 底 2 ならビット、底 $e$ ならナット。定数倍の違いだけで最適化の答えは変わらない
- 相互情報量は「知ることで減る不確かさ」。決定木の情報利得はこれそのもの

\`\`\`quiz
自己情報量に負号が付いているのはなぜですか :: 確率が小さいほど大きな値にしたいからです。確率 1 の出来事は情報量 0 になります。
情報量の定義に対数を使う理由は :: 独立な事象が同時に起きたときの情報量を、足し算で表せるようにするためです。
エントロピーが最大になるのはどんな分布ですか :: 一様分布です。K 個の選択肢が等確率なら log₂K ビットになります。
公平な 8 択のエントロピーは何ビットですか :: 3 ビットです（log₂8 = 3）。
相互情報量は何を表していますか :: 一方を知ることで減るもう一方の不確かさです。独立なら 0 になります。
決定木の情報利得は、情報理論のどの量ですか :: 分割前後のエントロピーの差、すなわち相互情報量にあたります。
\`\`\`
`,
  },
  {
    id: 'm-info-2',
    categoryId: 'm-info',
    title: '交差エントロピーと KL ダイバージェンス',
    goal: '2 つの分布の隔たりを測る道具を身に付け、分類の損失と生成モデルの目的関数を同じ形で読めるようになる',
    minutes: 20,
    body: `
# ざっくり言うと

前の節では 1 つの分布を測りました。ここでは**2 つの分布がどれだけ違うか**を測ります。

なぜそれが要るのか。機械学習は結局のところ、**「真のデータの分布」にモデルの分布を近づける作業**だからです。分類も生成も、やっていることは同じです。その「近づける」の目盛りが交差エントロピーと KL ダイバージェンスです。

# 交差エントロピー

真の分布を $P$、モデルの分布を $Q$ とします。

\`\`\`math
H(P, Q) = -\\sum_x p(x) \\log q(x)
\`\`\`

エントロピーの式と見比べてください。**重み（前）は真の分布 $P$、対数の中（後）はモデルの分布 $Q$** です。この非対称な形が、そのまま分類の損失になります。

分類では真の分布が one-hot（正解クラスだけ 1）なので、和のうち生き残るのは正解クラスの項だけです。

\`\`\`math
L = -\\log q(\\text{正解クラス})
\`\`\`

**正解クラスに与えた確率が小さいほど損失が大きい。** 交差エントロピー損失が実際にやっているのは、これだけです。

# KL ダイバージェンス

交差エントロピーから、$P$ 自身のエントロピー（どうしても消せない不確かさ）を引いたものが**KL ダイバージェンス**です。

\`\`\`math
D_{KL}(P \\parallel Q) = \\sum_x p(x) \\log \\frac{p(x)}{q(x)} = H(P, Q) - H(P)
\`\`\`

性質は 3 つ覚えれば足ります。

- **必ず 0 以上**。$P = Q$ のときだけ 0（ギブスの不等式）
- **非対称**：$D_{KL}(P \\parallel Q) \\ne D_{KL}(Q \\parallel P)$
- 三角不等式も成り立たない

> **試験のポイント** 上の 2 つ目と 3 つ目から、**KL ダイバージェンスは「距離」ではありません。** 選択肢に「距離である」「対称である」と出たら誤りです。「ダイバージェンス（隔たり）」という呼び名は、まさにそのために使い分けられています。

## 交差エントロピーと KL の関係

$H(P)$ は真の分布だけで決まり、モデルのパラメータを含みません。つまり**最適化の立場では定数**です。

\`\`\`diagram:flow
title: 分類の損失として同じになる理由
交差エントロピー H(P,Q) :: 実際に計算している損失
＝ H(P) + KL(P‖Q) :: 分解するとこうなる
H(P) は定数 :: 真の分布だけで決まる（one-hot なら 0）
最小化は KL の最小化と一致 :: だから「分布を近づけている」と言える
\`\`\`

**交差エントロピーを小さくすることは、KL ダイバージェンスを小さくすること**です。ラベルが one-hot なら $H(P) = 0$ なので、両者は数値としても一致します。

# どちらを前に置くかで性質が変わる

非対称なので、$P$ と $Q$ のどちらを前に置くかで振る舞いが変わります。生成モデルの章で効いてくる話です。

\`\`\`diagram:compare
title: 前向き KL と逆向き KL
actors: KL(P‖Q)（前向き） | KL(Q‖P)（逆向き）
真の分布が大きいところで q が 0 だと大罰
q が大きいところで p が 0 だと大罰
すべての山を覆おうとする
ひとつの山に絞り込む
平均を取りに行く（ぼやけやすい）
峰を選ぶ（多様性が落ちやすい）
最尤推定はこちらに対応
変分推論（VAE）はこちらに対応
\`\`\`

**VAE の生成画像がぼやけがち**なのは、データ側を最尤推定する（＝前向き KL）ために分布全体を覆いにいくからで、**GAN のモード崩壊**は逆に、ひとつの峰に絞り込む振る舞いに対応します。どちらも「どちら向きの KL か」で説明が付きます。

## JS ダイバージェンス

非対称なのが困る場面のために、**JS ダイバージェンス**があります。2 つの分布の平均 $M = (P + Q) / 2$ を挟んで、$P$ と $M$、$Q$ と $M$ の KL を平均したもので、**対称**かつ**有界**（底 2 なら上限 1）です。両方向の KL をそのまま平均したものではない点に注意してください。

**識別器が最適なとき、GAN の元々の目的関数は JS ダイバージェンスの最小化と等価**であることが知られています。裏を返せば、2 つの分布の台が重ならないと JS ダイバージェンスが一定値に張り付いて勾配が消えるということで、**GAN の学習が不安定になる一因**でもあります。これを Wasserstein 距離に置き換えて改善したのが WGAN です。

# 生成モデルの目的関数への橋渡し

VAE が最大化する ELBO は「**再構成の対数尤度 $-$ KL**」という引き算の形をしています。符号を反転して**損失**として書くと、見慣れた 2 つの項の足し算になります。

\`\`\`diagram:compare
title: VAE の損失（マイナス ELBO）の形
actors: 再構成の項 | 正則化の項
入力をうまく復元できているか
潜在変数の分布が事前分布からずれていないか
負の対数尤度（＝交差エントロピーや二乗誤差）
KL ダイバージェンス
確率・統計の節で見た形
情報理論の節で見た形
\`\`\`

> **よくある勘違い** ELBO は**最大化**する量で、中身は「対数尤度 $-$ KL」の**引き算**です。「負の対数尤度 $+$ KL」という足し算になるのは、符号を反転した損失（$-$ELBO）のほうで、最小化するのはこちらです。**どちら向きの話をしているか**を取り違えないこと。

**応用数学の 2 つの章が、そのまま生成モデルの目的関数の 2 つの項になっています。** 深層学習の応用に進む前にここを固めておく価値は、この一点だけでも十分にあります。

# コードで確かめる

\`\`\`python
import numpy as np

def cross_entropy(p, q):
    p, q = np.asarray(p), np.asarray(q)
    return float(-(p * np.log(q + 1e-12)).sum())

def kl(p, q):
    p, q = np.asarray(p), np.asarray(q)
    return float((p * np.log((p + 1e-12) / (q + 1e-12))).sum())

p = np.array([1.0, 0.0, 0.0])          # 正解ラベル（one-hot）
q1 = np.array([0.7, 0.2, 0.1])         # よい予測
q2 = np.array([0.3, 0.4, 0.3])         # 悪い予測

q3 = np.array([0.05, 0.9, 0.05])       # 山の位置が違う分布

print(cross_entropy(p, q1), kl(p, q1))  # 0.357 0.357  one-hot なら一致
print(cross_entropy(p, q2), kl(p, q2))  # 1.204 1.204  外すほど大きい
print(kl(q1, q3), kl(q3, q1))           # 1.616 と 1.187 ＝ 非対称
\`\`\`

\`1e-12\` を足しているのは $\\log 0$ を避けるためです。実装上の定番なので、コード問題で見かけたら「ゼロ除算・対数のゼロ対策」と読んでください。なお \`np.log\` は自然対数なので、ここに出る値の単位は**ナット**です（前の節のビットとは定数倍だけ違います）。

> **よくある勘違い** 交差エントロピーと KL ダイバージェンスは同じものではありません。**交差エントロピー ＝ エントロピー ＋ KL** です。ラベルが one-hot のときはエントロピーが 0 なので値が一致するだけで、一般には一致しません。

# この節のまとめ

- 交差エントロピー $H(P, Q) = -\\sum p \\log q$ は、重みが真の分布、対数の中がモデルの分布
- 分類の損失は $-\\log q(\\text{正解クラス})$ に帰着する。正解に与えた確率が低いほど損失が大きい
- KL ダイバージェンスは $H(P, Q) - H(P)$。**0 以上で、一致するときだけ 0**
- KL は**非対称で距離ではない**。三角不等式も成り立たない
- $H(P)$ は定数なので、交差エントロピーの最小化は KL の最小化と一致する
- 前向き KL は山を覆い、逆向き KL は山を選ぶ。VAE や GAN の振る舞いはここに根がある

\`\`\`quiz
交差エントロピーの式で、対数の中に入るのはどちらの分布ですか :: モデルの分布 q です。重みとして掛かるのが真の分布 p です。
KL ダイバージェンスは距離ですか :: 距離ではありません。非対称で、三角不等式も成り立たないためです（0 以上で、一致するときだけ 0 という性質はあります）。
交差エントロピーと KL ダイバージェンスの関係は :: 交差エントロピー ＝ 真の分布のエントロピー ＋ KL です。ラベルが one-hot ならエントロピーが 0 なので値が一致します。
分類の損失が -log q(正解クラス) になるのはなぜですか :: 真の分布が one-hot なので、和のうち正解クラスの項だけが残るからです。
前向き KL と逆向き KL の振る舞いの違いは :: 前向きはすべての山を覆おうとしてぼやけやすく、逆向きはひとつの山に絞り込みます。
GAN の元々の目的関数は、どのダイバージェンスの最小化と等価ですか :: 識別器が最適なとき、JS ダイバージェンスの最小化と等価になります。対称で有界な指標ですが、分布の台が重ならないと勾配が消えるため、学習の不安定さが WGAN の動機になりました。
\`\`\`
`,
  },
];
