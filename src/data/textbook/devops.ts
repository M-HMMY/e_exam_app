import type { TextbookSection } from '../../types';

/**
 * 開発・運用環境。分散学習・アクセラレータ・軽量化・実装と運用。
 * 節の骨格：ざっくり言うと → 本論（図・数式・表）→ この節のまとめ → 一問一答（quiz）。
 * 「試験のポイント」「よくある勘違い」の引用ブロックは直前チェックシートに自動で集まる。
 */
export const devops: TextbookSection[] = [
  {
    id: 'dv-env-1',
    categoryId: 'dv-env',
    title: '分散学習',
    goal: 'データ並列とモデル並列を使い分けられ、同期・非同期の違いとメモリを節約する工夫を説明できる',
    minutes: 25,
    body: `
# ざっくり言うと

モデルが大きくなると、**1 台の計算機では終わらない・そもそも載らない**という壁に当たります。壁の種類は 2 つあり、**どちらの壁かで打ち手が変わります**。

\`\`\`diagram:compare
title: 何が足りないのかで分ける
actors: 時間が足りない | メモリが足りない
モデルは 1 台に載る
モデルが 1 台に載らない
データ並列
モデル並列
同じモデルを配って、違うデータを流す
モデルを切り分けて、別々の装置に置く
通信するのは勾配
通信するのは途中の計算結果
\`\`\`

> **試験のポイント**：**データ並列はモデルを複製してデータを分ける、モデル並列はモデルそのものを分割する。** 「モデルが 1 台に載るかどうか」が分かれ目です。

# データ並列

各デバイスに**同じモデルの複製**を置き、ミニバッチを分けて配ります。それぞれが勾配を計算したら、**全デバイスの勾配を平均して**同じ更新を全員に反映します。この集約は **AllReduce** と呼ばれる通信で行うのが主流です。

\`\`\`diagram:flow
title: データ並列の 1 ステップ
ミニバッチを分割 :: 256 件を 4 台に 64 件ずつ
各台で順伝播と逆伝播 :: それぞれが勾配を出す
勾配を集約して平均 :: AllReduce で全台が同じ勾配を持つ
各台で同じ更新 :: 重みは常に同一に保たれる
\`\`\`

ここで**実効的なバッチサイズが台数倍になる**点が重要です。バッチが大きいと勾配のばらつきが減るので、**学習率もあわせて大きくする**のが定石です（バッチサイズに比例させる**線形スケーリング則**）。ただし最初から大きな学習率だと壊れるので、**ウォームアップ**（最初の数エポックで学習率を徐々に上げる）を併用します。

\`\`\`python
# 4 台で分散したときの実効バッチと学習率の目安
base_batch, base_lr = 64, 0.1
devices = 4

eff_batch = base_batch * devices
scaled_lr = base_lr * (eff_batch / base_batch)
print(eff_batch, scaled_lr)      # 256 0.4
\`\`\`

> **よくある勘違い**：バッチサイズを大きくしても、**そのままでは学習が速くならないどころか精度が落ちることがあります。** 1 エポックあたりの更新回数が減るためで、学習率の調整とウォームアップがセットで必要です。

集約のしかたにも 2 通りあります。

| 方式 | 中身 | 弱点 |
| --- | --- | --- |
| **同期** | 全台の勾配がそろってから更新する | **遅い 1 台に全員が待たされる**（ストラグラー） |
| **非同期** | そろうのを待たずに、届いた勾配で更新する | 古い重みで計算した勾配が混ざる（**陳腐化した勾配**）ので収束が不安定 |

現在は同期型が主流です。通信が速くなり、待ち時間の不利より**再現性と安定性**の利点が上回るためです。

# モデル並列

モデルが 1 台のメモリに載らないときは、**モデルを切って別々の装置に置きます**。

- **パイプライン並列**：層のかたまりごとに装置を分ける。素直だが、**前の段が終わるまで次の段が遊ぶ**（パイプラインの気泡）。ミニバッチをさらに細かく分けて流し込むことで埋める
- **テンソル並列**：1 つの層の行列積を分割して複数の装置で分担する。層の中で通信が発生するため、装置間の帯域が要求される

大規模言語モデルの学習では、これらを**組み合わせます**（データ並列 × パイプライン並列 × テンソル並列）。

なお **ZeRO**（や FSDP）は**モデル並列ではありません**。データ並列のまま、各台が同じものを重複して持っていたパラメータ・勾配・最適化器の状態を**分割して持ち合う**ことでメモリを削る工夫です。**「データ並列のままメモリを減らす」**ものとして押さえてください。

# メモリを節約する工夫

学習時のメモリは、**重みだけでは終わりません**。

| 内訳 | 量の目安 |
| --- | --- |
| パラメータ | $P$ 個 × 4 バイト（FP32） |
| 勾配 | パラメータと同じだけ |
| 最適化器の状態 | **Adam なら 2 倍**（1 次と 2 次のモーメント） |
| 中間の活性 | バッチサイズと層数に比例。**これが一番大きいことも多い** |

\`\`\`python
params = 100_000_000                     # 1 億パラメータ

weights = params * 4 / 1024**3           # FP32 の重み（GiB）
adam = weights * 4                       # 重み + 勾配 + モーメント 2 つ
print(round(weights, 3), round(adam, 3))  # 0.373 1.49
\`\`\`

**1 億パラメータのモデルでも、Adam で学習するなら重みだけの 4 倍**のメモリが要ります。ここに活性の分が乗ります。

対処の定番は次の 3 つです。

- **混合精度学習**：計算を FP16 / BF16 で行い、重みの原本は FP32 で持つ。**重みの原本も最適化器の状態も FP32 のまま残るので、減るのは主に中間の活性**である点に注意。計算時間も短くなる。FP16 では勾配が小さすぎて 0 に潰れるため**損失スケーリング**（損失を定数倍してから逆伝播する）を併用する。**BF16 は FP32 と同じ指数部を持ち範囲が広い**ので、この工夫が要らないことが多い
- **勾配累積**：小さいバッチで数回ぶん勾配を足してから 1 回更新する。**メモリを増やさずに実効バッチを大きくできる**
- **勾配チェックポインティング**：中間の活性を保存せず、逆伝播で**もう一度計算し直す**。メモリと計算時間を交換する

# この節のまとめ

- **データ並列はモデルを複製してデータを分割**、**モデル並列はモデル自体を分割**する。分かれ目は「1 台に載るか」
- データ並列は勾配を **AllReduce で平均**して同じ更新をかける。実効バッチが台数倍になる
- バッチを大きくしたら**学習率も大きくし（線形スケーリング則）、ウォームアップを併用**する
- **同期型は遅い 1 台に待たされ、非同期型は陳腐化した勾配で不安定**になる。現在は同期型が主流
- モデル並列は**パイプライン並列**（層で分ける・気泡が出る）と**テンソル並列**（層の中で分ける・通信が重い）
- 学習時のメモリは**重み・勾配・最適化器の状態・活性**。Adam は重みの 4 倍。**混合精度・勾配累積・勾配チェックポインティング**で削る

\`\`\`quiz
データ並列とモデル並列の違いは :: データ並列はモデルを複製してデータを分け、モデル並列はモデル自体を分割します。
データ並列で装置間をやり取りするものは :: 勾配です。集約して平均し、全台に同じ更新をかけます。
バッチサイズを 4 倍にしたとき、学習率はどうするのが定石ですか :: 同じく 4 倍にします（線形スケーリング則）。急に上げると壊れるのでウォームアップを併用します。
非同期型の分散学習の弱点は :: 古い重みで計算された勾配が混ざるため、収束が不安定になることです。
Adam で学習するとき、重みの何倍のメモリが要りますか :: およそ 4 倍です（重み・勾配・1 次と 2 次のモーメント）。ほかに中間の活性の分もかかります。
勾配チェックポインティングは何と何を交換していますか :: メモリと計算時間です。活性を保存せず、逆伝播のときに再計算します。
FP16 の混合精度学習で損失スケーリングが要るのはなぜですか :: 小さい勾配が FP16 の表現範囲を下回って 0 になるのを防ぐためです。BF16 では範囲が広いため不要なことが多いです。
\`\`\`
`,
  },
  {
    id: 'dv-env-2',
    categoryId: 'dv-env',
    title: 'アクセラレータとエッジ',
    goal: 'GPU・TPU・FPGA・ASIC の違いを説明でき、エッジ推論の利点と制約を言える',
    minutes: 20,
    body: `
# ざっくり言うと

深層学習の計算は、**同じ形の演算を大量に、独立に**行うものです。行列積の中身は積和の繰り返しで、しかも互いに依存していません。

CPU は**複雑な処理を順番に速く**こなす設計なので、この形の仕事には向きません。**単純な計算機をたくさん並べたほう**が圧倒的に速い。これがアクセラレータの存在理由です。

\`\`\`diagram:compare
title: CPU と GPU の設計思想
actors: CPU | GPU
少数の強力なコア
多数の単純なコア
分岐や条件判断が得意
同じ演算の繰り返しが得意
1 つの仕事を速く終わらせる
たくさんの仕事を同時に進める
遅延を小さくする設計
スループットを大きくする設計
\`\`\`

# 主なアクセラレータ

| 種類 | 特徴 | 位置付け |
| --- | --- | --- |
| **GPU** | 多数のコアで並列計算。**汎用性が高く、開発環境が充実**（CUDA・cuDNN） | 学習の標準 |
| **TPU** | Google が行列演算に特化して作った専用チップ（ASIC の一種） | 大規模学習・推論 |
| **FPGA** | **回路を書き換えられる**。低遅延・低電力にできるが、開発の手間が大きい | 特定用途・低遅延 |
| **ASIC** | 用途を固定した専用チップ。**最も高速・低電力だが、後から変えられない** | 量産・組み込み |

> **試験のポイント**：**FPGA は「書き換えられる」、ASIC は「書き換えられない代わりに最も効率が良い」。** TPU は Google が作った、行列演算に特化した ASIC です。この 3 つの関係が問われます。

GPU の性能を左右するのは**演算能力だけではありません**。**メモリ容量**（載せられるモデルとバッチの大きさ）と**メモリ帯域**（データを運ぶ速さ）も同じくらい効きます。深層学習の処理には、計算そのものより**データの移動が足を引っ張る**場面が多くあります。

**GPU の利用率が上がらないときは、たいてい「仕事が届いていない」**のが原因です。データの読み込みや前処理が追いつかない、バッチが小さすぎる、CPU との転送が頻繁——いずれも GPU を待たせます。演算能力そのものが足りない場合は、利用率は 100% に張り付いたまま遅いはずです。

> **よくある勘違い**：GPU を増やせば必ず速くなる、とは限りません。**装置間の通信が支配的になると、台数を増やしても頭打ち**になります。分散学習で通信量を減らす工夫が重視されるのはこのためです。

# 学習と推論では要求が違う

| | 学習 | 推論 |
| --- | --- | --- |
| 必要なメモリ | 大きい（勾配・最適化器の状態・活性） | 小さい（重みと一時領域） |
| 数値精度 | ある程度必要（FP16 / BF16 が主流） | **粗くてよい**（INT8 でも実用になることが多い） |
| 重視する指標 | スループット | **遅延**（応答時間）も重要 |
| 実行場所 | データセンター | データセンターまたは**端末側** |

推論のほうが要求がゆるいことが、次の節の軽量化と、この節の後半のエッジ処理につながります。

# エッジコンピューティング

**エッジ**とは、クラウドではなく**データが生まれる現場側**（スマートフォン、カメラ、車、工場の機器）で処理することです。

\`\`\`diagram:compare
title: クラウド推論とエッジ推論
actors: クラウド | エッジ
大きなモデルを動かせる
モデルの大きさに厳しい制約
通信の往復ぶん遅れる
応答が速い
データを外へ送る必要がある
データを外に出さずに済む
通信が切れると使えない
通信が不安定でも動く
\`\`\`

エッジを選ぶ理由は、**遅延・通信量・プライバシー**の 3 つでほぼ説明できます。自動運転が典型で、**判断のたびにクラウドへ問い合わせる余裕はありません**。

制約は**電力・メモリ・発熱**です。そこで次の節の軽量化（量子化・枝刈り・蒸留）や、専用の推論ランタイム（TensorFlow Lite、現在の名称は LiteRT など）が効いてきます。

関連して、**連合学習**（フェデレーテッドラーニング）も押さえておきましょう。**データを端末に置いたまま、学習結果（勾配やモデルの更新）だけを集めて統合する**やり方です。**データそのものを集めずに学習できる**ので、プライバシーの要請が強い領域で使われます。

# この節のまとめ

- 深層学習の計算は**単純な演算の大量並列**なので、**多数の単純なコアを持つ GPU** が向く
- **GPU は汎用で開発環境が充実、TPU は行列演算に特化した ASIC、FPGA は書き換え可能、ASIC は固定で最も効率が良い**
- 性能は演算能力だけでなく**メモリ容量と帯域**で決まる。分散すると**通信が頭打ちの原因**になる
- **推論は学習よりメモリも精度の要求も小さい**。遅延が重視される
- **エッジ推論の利点は遅延・通信量・プライバシー**、制約は電力とメモリ
- **連合学習はデータを端末に残したまま、更新だけを集約する**

\`\`\`quiz
GPU が深層学習に向いている理由は :: 同じ演算を大量に並列実行できるからです。行列積のように独立した積和の繰り返しに向きます。
FPGA と ASIC の違いは :: FPGA は回路を書き換えられ、ASIC は用途を固定する代わりに最も高速・低電力です。
TPU とは何ですか :: Google が行列演算に特化して開発したアクセラレータで、ASIC の一種です。
エッジで推論する利点を 3 つ挙げると :: 応答が速い（低遅延）、通信量が減る、データを外に出さずに済む（プライバシー）です。
連合学習（フェデレーテッドラーニング）とは :: データを端末に置いたまま、モデルの更新だけを集めて統合する学習方法です。
推論が学習よりメモリを必要としない理由は :: 勾配や最適化器の状態、逆伝播用の中間の活性を保持しなくてよいからです。
\`\`\`
`,
  },
  {
    id: 'dv-env-3',
    categoryId: 'dv-env',
    title: 'モデルの軽量化',
    goal: '量子化・枝刈り・蒸留の違いを説明でき、それぞれの効果と副作用を言える',
    minutes: 25,
    body: `
# ざっくり言うと

学習し終えたモデルは、たいてい**そのままでは現場に置けません**。大きすぎる、遅すぎる、電力を食いすぎる。

軽くする道具は 3 つあり、**削る対象がそれぞれ違います**。

\`\`\`diagram:tree
title: 何を削るのか
数値の細かさを削る :: 量子化（FP32 → INT8）
つながりを削る :: 枝刈り（小さい重みを 0 にする）
モデルそのものを小さく作り直す :: 蒸留（大きいモデルの出力を小さいモデルに教える）
\`\`\`

> **試験のポイント**：**量子化は精度（ビット数）を落とす、枝刈りは重みを削除する、蒸留は小さいモデルに学ばせ直す。** 何を削っているかで区別すれば取り違えません。3 つは併用できます。

# 量子化

重みや活性を **FP32 から INT8 のような低いビット数**へ落とします。効果は分かりやすく、**32 ビットから 8 ビットなら容量は 4 分の 1**、整数演算が使えるぶん速度も上がります。

\`\`\`python
params = 50_000_000

for bits, name in [(32, 'FP32'), (16, 'FP16'), (8, 'INT8'), (4, 'INT4')]:
    mb = params * bits / 8 / 1024**2
    print(name, round(mb, 1))
# FP32 190.7
# FP16 95.4
# INT8 47.7
# INT4 23.8
\`\`\`

やり方は 2 通りです。

| 方式 | 中身 |
| --- | --- |
| **学習後量子化**（PTQ） | 学習済みモデルをあとから変換する。**手軽だが精度が落ちやすい** |
| **量子化を考慮した学習**（QAT） | 学習中に量子化の丸めを模擬しておく。**手間はかかるが精度を保ちやすい** |

> **よくある勘違い**：量子化しても**認識精度（正解率）が必ず落ちるとは限りません**。冗長性の高いモデルでは INT8 化しても実用上ほぼ変わらないことがあります。一方で、大規模言語モデルでは活性の**一部の特徴次元に極端な外れ値**が現れ、そこが原因で大きく劣化することがあります。その次元だけ高い精度のまま計算する、といった対処が取られます。

# 枝刈り

**重要でない結合を削る**手法です。基準としては**重みの絶対値が小さいものから削る**のが基本で、削ったあと**再学習**して性能を戻します。

| 種類 | 中身 | 実際に速くなるか |
| --- | --- | --- |
| **非構造化** | 個々の重みを 0 にする。0 の割合は高くできる | **疎形式で保存し直さないと容量も減らず**、**疎行列を速く計算できるハードがないと速くもならない** |
| **構造化** | チャネルやフィルタ、ヘッドの単位でまとめて削る | **行列そのものが小さくなる**ので、素直に速くなる |

> **試験のポイント**：**非構造化枝刈りは疎形式にすれば容量は減らせるが速度に直結せず、構造化枝刈りは行列そのものが小さくなるので実際の高速化につながる。** ここは対比で問われます。

関連する話として**宝くじ仮説**があります。「大きなネットワークの中には、**元の初期値を保ったまま単独で学習すれば、元と同等の性能に達する小さな部分ネットワーク（当たりくじ）が潜んでいる**」という主張です。**初期値を引き直すと同じ構造でも当たらない**というのが実験の要点で、枝刈りが成り立つ理由の説明として言及されます。

# 蒸留

**大きな教師モデルの出力を、小さな生徒モデルに真似させます。** 正解ラベル（0 か 1）だけで学ぶより、**教師が出す確率分布**から学ぶほうが情報量が多いのが要点です。

「この画像は猫 0.7 / 犬 0.25 / 車 0.05」という教師の出力には、**「猫と犬は似ている」という知識が含まれています**（暗黒知識と呼ばれます）。正解ラベルだけではこれが伝わりません。

分布を学びやすくするため、ソフトマックスに**温度 $T$** を入れて滑らかにします。

\`\`\`math
p_i = \\frac{\\exp(z_i / T)}{\\sum_j \\exp(z_j / T)}
\`\`\`

\`\`\`python
import numpy as np

z = np.array([4.0, 2.0, 1.0])

def softmax_t(z, t):
    e = np.exp((z - z.max()) / t)
    return e / e.sum()

print(np.round(softmax_t(z, 1), 3))   # [0.844 0.114 0.042]
print(np.round(softmax_t(z, 4), 3))   # [0.481 0.292 0.227]
\`\`\`

**温度を上げるほど分布が平らになり、正解以外のクラスの情報が浮かび上がります。** 生徒の損失は、**教師の分布に近づける項**と、**正解ラベルに対する通常の交差エントロピー**の重み付き和にするのが一般的です。**温度は教師だけでなく生徒の出力にも同じ値を使い、学習が終わったら $T = 1$ に戻して推論します。**

代表例が **DistilBERT** で、BERT の層数を減らしながら性能の大半を保っています。

# その他の手立て

- **低ランク近似**：重み行列そのものを 2 つの小さな行列の積で置き換える（[特異値分解](textbook/m-linalg-3)）
- **LoRA**：同じ低ランクの考え方を**ファインチューニングの効率化**に使うもの。**更新量だけ**を低ランクで表すので学習するパラメータは激減するが、**推論時は元の重みに足し込むため、モデル自体は小さくならない**
- **効率的なアーキテクチャ**：そもそも軽い構造を選ぶ（[MobileNet の深さ方向分離畳み込み](textbook/dl-cnn-2) など）
- **推論ランタイム**：計算グラフの最適化（層の融合など）を行う TensorRT・ONNX Runtime・TensorFlow Lite

# この節のまとめ

- 軽量化は**量子化（ビット数）・枝刈り（結合）・蒸留（モデルごと作り直す）**の 3 本柱。併用できる
- 量子化は **32 → 8 ビットで容量 4 分の 1**。**PTQ は手軽で精度が落ちやすく、QAT は手間がかかるが精度を保ちやすい**
- 枝刈りは**絶対値の小さい重みから削って再学習**する。**非構造化は速度に直結せず、構造化は実際に速くなる**
- **宝くじ仮説**は「大きな網の中に、**元の初期値のまま**学習すれば同等になる小さな部分網が潜む」という主張
- 蒸留は**教師の確率分布（暗黒知識）を生徒に学ばせる**。**温度を上げると分布が平らになり、正解以外の情報が伝わる**
- 低ランク近似・軽量アーキテクチャ・推論ランタイムも軽量化の手立て。**LoRA は学習を軽くする手法**で、推論時のモデルは小さくならない点に注意

\`\`\`quiz
量子化・枝刈り・蒸留は、それぞれ何を削っていますか :: 量子化は数値のビット数、枝刈りは結合（重み）、蒸留はモデルの大きさそのものです。
FP32 のモデルを INT8 に量子化すると、容量はどうなりますか :: 4 分の 1 になります。
PTQ と QAT の違いは :: PTQ は学習後にまとめて量子化する手軽な方法、QAT は学習中に量子化を模擬して精度を保つ方法です。
非構造化枝刈りが必ずしも速度につながらないのはなぜですか :: 行列の形は変わらず疎になるだけなので、疎行列を高速に扱えるハードや実装が必要だからです。
蒸留で温度を上げると何が起きますか :: 教師の確率分布が平らになり、正解以外のクラスの情報（クラス間の似ている度合い）が伝わりやすくなります。
宝くじ仮説とは :: 大きなネットワークの中に、元の初期値のまま学習すれば同等の性能に達する小さな部分ネットワークが潜んでいる、という仮説です。初期値を引き直すと当たりません。
\`\`\`
`,
  },
  {
    id: 'dv-env-4',
    categoryId: 'dv-env',
    title: '実装と運用',
    goal: 'フレームワークの計算グラフの違いを説明でき、運用で何を監視するかを言える',
    minutes: 20,
    body: `
# ざっくり言うと

最後は、**作ったモデルを動かし続ける**ための話です。試験では用語の対応関係が問われるので、**それぞれが何のためにあるか**を押さえれば足ります。

# フレームワーク

深層学習の実装は、**計算グラフ**（どんな演算をどうつなぐか）を組み立て、自動微分にまかせる、という形をとります。この組み立て方に 2 つの流儀があります。

\`\`\`diagram:compare
title: 2 つの計算グラフ
actors: 定義して実行（Define-and-Run） | 実行しながら定義（Define-by-Run）
先にグラフを固めてからデータを流す
コードを実行した順にグラフができる
最適化しやすい
書きやすく、デバッグしやすい
条件分岐や可変長は書きにくい
Python の if や for がそのまま使える
初期の TensorFlow
PyTorch・Chainer（開発終了。この方式の発祥）
\`\`\`

> **試験のポイント**：**PyTorch は Define-by-Run（動的計算グラフ）、初期の TensorFlow は Define-and-Run（静的計算グラフ）。** TensorFlow も 2 系からは実行しながら定義する方式が既定になり、両者の差は縮まっています。

| 名前 | 位置付け |
| --- | --- |
| **PyTorch** | 動的計算グラフ。研究から実用まで広く使われる |
| **TensorFlow** | 静的グラフから始まり、2 系で動的が既定に。**Keras** が高水準の API |
| **ONNX** | フレームワークをまたいでモデルを受け渡すための**共通形式** |
| **TensorRT / TensorFlow Lite（現 LiteRT）** | 推論に特化したランタイム。層の融合や量子化で速くする |

**ONNX の役割**は覚えておく価値があります。**学習は PyTorch、推論は別のランタイム**という組み合わせが普通なので、その橋渡しが要るのです。

# 環境の再現性

「自分の環境では動く」を避けるために、**コンテナ**（Docker など）を使います。**ライブラリのバージョンや CUDA の組み合わせごと固めて配れる**のが利点です。GPU をコンテナから使うには専用の仕組み（NVIDIA Container Toolkit）が要ります。多数のコンテナを束ねて動かすなら Kubernetes が使われます。

再現性のためには、環境のほかに**乱数の種・データのバージョン・ハイパーパラメータ**も記録しておく必要があります。**モデルの重みだけ残しても、作り直せません。**

# 運用（MLOps）

学習して終わりではありません。**モデルは置いておくと劣化します。** 世の中が変わるからです。

\`\`\`diagram:cycle
title: 運用は輪になる
データを集めて学習する :: 実験の設定と結果を記録する
デプロイして推論を提供する :: 監視の仕組みとセットで出す
性能の低下を検知する :: 入力の分布や指標を見張る
再学習する :: 新しいデータで作り直す
\`\`\`

劣化の種類は 2 つに分けて呼びます。

| 名前 | 中身 | 例 |
| --- | --- | --- |
| **データドリフト** | **入力の分布**が変わる | カメラを交換して画像の色味が変わった |
| **コンセプトドリフト** | **入力と出力の関係**が変わる | 流行が変わり、同じ属性の人が別の商品を買うようになった |

> **よくある勘違い**：**モデルを変えていなくても精度は下がります。** 「デプロイしたら終わり」ではなく、**入力分布と指標を監視して、必要なら再学習する**までが運用です。

出し方の工夫も押さえておきます。

- **A/B テスト**：一部の利用者に新モデルを出して比べる
- **シャドー実行**：新モデルに同じ入力を流すが、結果は使わずに記録だけする
- **カナリアリリース**：少しずつ切り替えて、問題があれば戻す

最後に、技術以外の要請にも触れておきます。学習データに含まれる**個人情報の扱い**、**著作権**、**予測の偏り（バイアス）**、そして**判断根拠の説明**。E資格の範囲としては深追いしませんが、**運用で最初に問題になるのはこの部分**であることは知っておいてください。

# この節のまとめ

- **PyTorch は Define-by-Run（動的）、初期の TensorFlow は Define-and-Run（静的）**。TF 2 系では動的が既定
- **ONNX はフレームワーク間でモデルを受け渡す共通形式**。TensorRT・TensorFlow Lite は推論特化のランタイム
- **コンテナ**は環境ごと固めて配るための道具。再現には乱数の種・データ・ハイパーパラメータの記録も要る
- 運用は**学習 → デプロイ → 監視 → 再学習**の輪
- **データドリフトは入力の分布の変化、コンセプトドリフトは入力と出力の関係の変化**
- **モデルを変えなくても精度は下がる。** 監視と再学習までが運用の仕事

\`\`\`quiz
Define-by-Run（動的計算グラフ）の利点は :: コードの実行順にグラフができるため、条件分岐や可変長の入力を書きやすく、デバッグもしやすい点です。
PyTorch と初期の TensorFlow は、それぞれどちらの方式ですか :: PyTorch は Define-by-Run、初期の TensorFlow は Define-and-Run です。
ONNX は何のための仕組みですか :: フレームワークをまたいでモデルを受け渡すための共通形式です。
コンテナを使う目的は :: ライブラリやドライバのバージョンごと環境を固めて配り、どこでも同じように動かすためです。
データドリフトとコンセプトドリフトの違いは :: データドリフトは入力の分布の変化、コンセプトドリフトは入力と出力の関係の変化です。
シャドー実行とは何ですか :: 新しいモデルに同じ入力を流して結果を記録するだけで、実際の応答には使わない検証方法です。
\`\`\`
`,
  },
];
