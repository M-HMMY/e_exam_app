import type { TextbookSection } from '../../types';

/**
 * この章はこれから書く。書き方は math.ts と intro.ts を見本にすること。
 * 節の骨格：ざっくり言うと → 本論（図・数式・表）→ この節のまとめ → 一問一答（quiz）。
 * 「試験のポイント」「よくある勘違い」の引用ブロックは直前チェックシートに自動で集まる。
 */
export const dlArch: TextbookSection[] = [
  {
    id: 'dl-cnn-1',
    categoryId: 'dl-cnn',
    title: '畳み込みとプーリング',
    goal: '出力サイズとパラメータ数を自力で計算でき、全結合層と比べた畳み込みの利点を説明できる',
    minutes: 30,
    body: `
# ざっくり言うと

画像を全結合層に流すと、**位置がずれただけで別物**になってしまいます。しかも 224×224×3 の画像を 1000 ユニットの層につなぐだけで、パラメータが 1 億 5 千万個を超えます。

畳み込みは、この 2 つを同時に解決します。**小さなフィルタを画像全体で使い回す**ことで、パラメータを激減させ、しかも「どこにあっても同じ模様を検出できる」性質を手に入れます。

**この節の出力サイズとパラメータ数の計算は、確実に出題されます。** 公式を 2 本覚えれば解けます。

# 畳み込み演算

小さな窓（**フィルタ・カーネル**）を画像の上で滑らせ、重なった部分の**積の和**を取ります。ニューロン 1 個の計算が内積だったのと同じで、**畳み込みも内積の並び**です。

\`\`\`diagram:flow
title: 畳み込みの 1 手
フィルタを画像の左上に重ねる :: 3×3 なら 9 個の値が重なる
重なった値どうしを掛けて足す :: 出力の 1 マスが決まる
フィルタをずらす :: ずらす幅がストライド
右端まで行ったら 1 段下へ :: これを繰り返して出力を埋める
\`\`\`

重要なのは、**同じフィルタを画像全体で使い回す**点です。これを**重み共有**といい、次の 2 つの性質を生みます。

| 性質 | 意味 |
| --- | --- |
| パラメータが少ない | 位置ごとに別の重みを持たない |
| 位置がずれても検出できる | どこに現れても同じフィルタが反応する |

> **試験のポイント** 畳み込み層の利点は「**重み共有によるパラメータ削減**」と「**局所的な結合**」の 2 つです。全結合層のようにすべての画素を全ユニットにつながず、近くの画素だけを見ます。

# 出力サイズの公式

入力の 1 辺を $I$、フィルタを $F$、ストライドを $S$、パディングを $P$ とすると、出力の 1 辺は次のようになります。

\`\`\`math
O = \\lfloor \\frac{I + 2P - F}{S} \\rfloor + 1
\`\`\`

**分子は「フィルタが動ける距離」、それをストライドで割って「動ける回数」、最初の 1 回を足す**、と読めば覚える必要がありません。

| 例 | 計算 | 出力 |
| --- | --- | --- |
| $I=28, F=3, S=1, P=0$ | $(28 - 3) / 1 + 1$ | 26 |
| $I=28, F=3, S=1, P=1$ | $(28 + 2 - 3) / 1 + 1$ | 28（**サイズが保たれる**） |
| $I=32, F=5, S=2, P=2$ | $\\lfloor (32 + 4 - 5) / 2 \\rfloor + 1 = 15 + 1$ | 16 |

3 行目のように割り切れないときは**切り捨て**ます。

> **試験のポイント** $F=3, S=1, P=1$ なら出力サイズは入力と同じになります（同様に $F=5$ なら $P=2$）。**「サイズを保ちたいなら $P = (F-1)/2$」**は暗算で使える関係です。

パディングを入れる目的は 2 つあります。**サイズを保つこと**と、**端の画素が畳み込まれる回数を増やすこと**（パディングなしでは端の情報が薄れる）です。

次の道具で値を動かしてみてください。出力サイズだけでなく、**パラメータ数が画像サイズに依存しない**ことも同時に確かめられます。

\`\`\`widget:convsize
\`\`\`

# パラメータ数の公式

畳み込み層のパラメータは、**フィルタの中身 ＋ バイアス**です。入力チャネル数を $C_{in}$、出力チャネル数（フィルタの枚数）を $C_{out}$ とすると、

\`\`\`math
(F \\times F \\times C_{in} + 1) \\times C_{out}
\`\`\`

**フィルタは入力チャネルすべてにまたがる**（3×3 のフィルタでも、入力が 3 チャネルなら実体は 3×3×3）ことに注意してください。ここを落とすのが典型的な失点です。

| 層 | 計算 | パラメータ数 |
| --- | --- | --- |
| 入力 3 ch → 出力 64 ch、3×3 | $(3 \\times 3 \\times 3 + 1) \\times 64$ | 1,792 |
| 入力 64 ch → 出力 128 ch、3×3 | $(3 \\times 3 \\times 64 + 1) \\times 128$ | 73,856 |

> **よくある勘違い** パラメータ数は**入力画像のサイズに依存しません**。28×28 でも 224×224 でも、同じ層なら同じパラメータ数です。画像サイズで変わるのは**計算量と特徴マップのメモリ**のほうです。

# プーリング

一定の範囲を代表値ひとつにまとめ、特徴マップを小さくします。

| 種類 | 内容 |
| --- | --- |
| 最大プーリング | 範囲内の最大値を取る（**もっとも一般的**） |
| 平均プーリング | 範囲内の平均を取る |
| グローバル平均プーリング | 各チャネルを 1 つの値に潰す。全結合層の代わりに使える |

> **試験のポイント** プーリング層には**学習するパラメータがありません**。サイズは公式どおり小さくなります（2×2・ストライド 2 なら半分）。「位置の微小なずれに強くなる」ことが目的です。

# 全体の構成

\`\`\`diagram:flow
title: 典型的な CNN
畳み込み＋活性化 :: 模様を検出する
プーリング :: 小さくして、ずれに強くする
これを繰り返す :: 層が深いほど複雑な模様を捉える
平坦化または GAP :: 特徴マップをベクトルに直す
全結合層＋ソフトマックス :: クラスを予測する
\`\`\`

浅い層は**エッジや色**、深い層は**部品や物体**に反応するようになります。この階層性は学習の結果として現れるもので、人が設計したものではありません。

## 受容野

出力の 1 マスが、元の画像のどれだけの範囲を見ているかを**受容野**といいます。層を重ねるほど広がり、3×3 の畳み込みを 2 回重ねると 5×5 相当、3 回で 7×7 相当になります。

**VGG が大きなフィルタをやめて 3×3 を重ねたのは、この性質のため**です。同じ受容野をより少ないパラメータで得られ、しかも活性化関数を挟む回数が増えて表現力も上がります。

# コードで確かめる

\`\`\`python
import numpy as np

def out_size(i, f, s, p):
    return (i + 2 * p - f) // s + 1

print(out_size(28, 3, 1, 0))    # 26
print(out_size(28, 3, 1, 1))    # 28  サイズが保たれる
print(out_size(32, 5, 2, 2))    # 16
print(out_size(28, 2, 2, 0))    # 14  2×2 プーリングで半分

def conv_params(f, c_in, c_out, bias=True):
    return (f * f * c_in + (1 if bias else 0)) * c_out

print(conv_params(3, 3, 64))    # 1792
print(conv_params(3, 64, 128))  # 73856
\`\`\`

# この節のまとめ

- 畳み込みは小さなフィルタを画像全体で使い回す（**重み共有**）ので、パラメータが少なく、位置のずれに強い
- 出力サイズは $O = \\lfloor (I + 2P - F) / S \\rfloor + 1$。割り切れないときは切り捨て
- $F=3, S=1, P=1$ なら入力とサイズが変わらない（$P = (F-1)/2$）
- パラメータ数は $(F \\times F \\times C_{in} + 1) \\times C_{out}$。**入力チャネル数を掛け忘れない**
- パラメータ数は画像サイズに依存しない。変わるのは計算量とメモリ
- プーリングに学習パラメータはない。最大プーリングが一般的で、ずれへの頑健さが目的

\`\`\`quiz
入力 32、フィルタ 5、ストライド 1、パディング 0 のときの出力サイズは :: (32 - 5) / 1 + 1 = 28 です。
入力と同じサイズを保つには、3×3 のフィルタでパディングをいくつにしますか :: 1 です。P = (F - 1) / 2 の関係になります。
入力 64 チャネル、出力 32 チャネル、3×3 の畳み込み層のパラメータ数は :: (3 × 3 × 64 + 1) × 32 = 18,464 個です。入力チャネル数を掛けるのを忘れないこと。
畳み込み層のパラメータ数は入力画像のサイズで変わりますか :: 変わりません。変わるのは計算量と特徴マップのメモリです。
プーリング層に学習するパラメータはありますか :: ありません。最大値や平均を取るだけです。
3×3 の畳み込みを 2 回重ねると、受容野はどれだけになりますか :: 5×5 相当です。大きなフィルタ 1 枚より少ないパラメータで同じ広さを見られます。
\`\`\`
`,
  },
  {
    id: 'dl-cnn-2',
    categoryId: 'dl-cnn',
    title: '代表的な CNN アーキテクチャ',
    goal: '主要なモデルを「何を解決するために生まれたか」で並べ、残差接続の意義を説明できる',
    minutes: 25,
    body: `
# ざっくり言うと

アーキテクチャの名前は多いのですが、**それぞれ 1 つの問題を解くために生まれています。** 年表として覚えるより、**「何が困っていて、どう解いたか」**で並べるほうが記憶に残り、応用も効きます。

\`\`\`diagram:tree
title: 何を解決したかで並べる
深くしたい :: VGG（3×3 を積む）
深くすると学習できない :: ResNet（残差接続）
計算量を抑えつつ表現力がほしい :: Inception（複数サイズを並列）、Bottleneck（1×1 で絞る）
スマホで動かしたい :: MobileNet（深さ方向分離畳み込み）
大きさの決め方を最適化したい :: EfficientNet（幅・深さ・解像度を同時に）
\`\`\`

# 深くする流れ

| モデル | 年 | 何をしたか |
| --- | --- | --- |
| LeNet | 1998 | 畳み込みとプーリングの原型。手書き数字 |
| AlexNet | 2012 | ReLU・ドロップアウト・GPU・データ拡張で ILSVRC を制した |
| VGG | 2014 | **3×3 の畳み込みだけを積み重ねる**単純な構成で 16〜19 層 |
| GoogLeNet | 2014 | Inception モジュールで、複数のフィルタサイズを並列に |
| ResNet | 2015 | **残差接続**で 100 層超の学習を可能にした |

**AlexNet は「深層学習の実用性を示した」という位置付け**で、内容としては ReLU・ドロップアウト・データ拡張・GPU 学習という、いまでは当たり前の要素の組み合わせです。

# 1×1 畳み込みという道具

**1×1 の畳み込みは、空間方向には何もせず、チャネル方向だけを混ぜます。** つまり「各画素に対して全結合層をかける」操作です。

用途は 2 つあります。

- **チャネル数を減らして計算量を下げる**（ボトルネック構造）
- チャネル間の情報を混ぜて表現力を上げる

たとえば 256 ch を 3×3 で 256 ch に畳み込むと約 59 万個のパラメータですが、1×1 で 64 ch に落として 3×3 を掛け、1×1 で 256 ch に戻すと約 7 万個で済みます。**ResNet の深い版はこの構造を使っています。**

# 残差接続（ResNet）

深くすると、**訓練誤差まで悪化する**という現象が観測されました。過学習ではありません（過学習なら訓練誤差は下がるはずです）。層を増やしたのに浅いモデルより学習できない、という**劣化問題**です。

解決は驚くほど単純で、**入力をそのまま出力に足す**ことでした。

\`\`\`math
y = F(x) + x
\`\`\`

\`\`\`diagram:compare
title: 何が変わったか
actors: 素の積み重ね | 残差接続
層は「出力そのもの」を学ぶ
層は「入力からの差分」を学ぶ
何もしないことを学ぶのが難しい
重みを 0 にすれば恒等写像になる
層を増やすほど最適化が難しくなる
足し算の経路を通って勾配がそのまま流れる
数十層の段階で訓練誤差が悪化しはじめる
1000 層規模でも学習できる
\`\`\`

> **試験のポイント** 残差接続の効果は 2 つの言い方で説明されます。**「恒等写像を学びやすくする」**と**「勾配が直接流れる経路ができる」**です。どちらも同じ足し算から出てきます。

**DenseNet** はこの発想を推し進め、足すのではなく**すべての前の層の出力を連結**します。

# 軽量化の流れ

スマートフォンや組み込み機器で動かすには、計算量とパラメータを減らす必要があります。

**深さ方向分離畳み込み（depthwise separable convolution）**は、通常の畳み込みを 2 段に分けます。

\`\`\`diagram:flow
title: 深さ方向分離畳み込み
depthwise :: チャネルごとに独立に 3×3 を掛ける（チャネルは混ざらない）
pointwise :: 1×1 でチャネルを混ぜる
結果 :: 計算量とパラメータが大幅に減る
\`\`\`

$3 \\times 3$、入力 $C_{in}$、出力 $C_{out}$ で比べると、通常が $9 C_{in} C_{out}$ に対し、分離版は $9 C_{in} + C_{in} C_{out}$ です。比は $1 / C_{out} + 1/9$ なので、**出力チャネル数 $C_{out}$ が大きいほど差が開きます**（$C_{out} = 64$ で約 8 分の 1、256 なら約 9 分の 1）。これを使うのが **MobileNet** です。

**EfficientNet** は、幅（チャネル数）・深さ（層数）・入力解像度を**バラバラに増やすのではなく、決まった比率でまとめて増やす**（複合スケーリング）と効率がよい、という発見に基づいています。

# 転移学習とファインチューニング

**大規模データで学習済みのモデルを、手元の課題に流用する**手法です。データが少ないときに最も効果があります。

| 方法 | 内容 | 使いどころ |
| --- | --- | --- |
| 特徴抽出器として使う | 重みは凍結し、最後の分類層だけ学習する | データが少ない、対象が似ている |
| ファインチューニング | 学習済みの重みも小さい学習率で更新する | データがある程度ある |

> **よくある勘違い** ファインチューニングでは**学習率を小さくします**。せっかく学習された重みを、大きな更新で壊さないためです。また、**入力の前処理（正規化の統計）も学習時と揃える**必要があります。

# この節のまとめ

- アーキテクチャは「何を解決したか」で並べると整理できる
- VGG は 3×3 を積む単純な構成、GoogLeNet は複数サイズを並列（Inception）
- 1×1 畳み込みは空間を変えずチャネルだけを混ぜる。**ボトルネックで計算量を下げる**のに使う
- 深くすると訓練誤差まで悪化する劣化問題を、**残差接続 $y = F(x) + x$** が解決した
- 残差接続の効果は「恒等写像を学びやすい」「勾配が直接流れる」の 2 つで説明される
- 深さ方向分離畳み込み（MobileNet）は計算量をおおむね 8〜9 分の 1 にする

\`\`\`quiz
VGG が大きなフィルタではなく 3×3 を積み重ねたのはなぜですか :: 同じ受容野をより少ないパラメータで得られ、活性化関数を挟む回数も増えて表現力が上がるからです。
1×1 の畳み込みは何をする操作ですか :: 空間方向には何もせず、チャネル方向だけを混ぜます。チャネル数を減らして計算量を下げる用途にも使います。
残差接続はどんな問題を解決しましたか :: 層を深くすると訓練誤差まで悪化する劣化問題です。入力を出力に足すことで解決しました。
残差接続が効く理由を 2 つ挙げてください :: 恒等写像を学びやすくすること（重みが 0 でも入力が通る）と、勾配が足し算の経路を直接流れることです。
深さ方向分離畳み込みは、どのように計算量を減らしますか :: チャネルごとの 3×3（depthwise）と、チャネルを混ぜる 1×1（pointwise）に分けます。
ファインチューニングで学習率を小さくするのはなぜですか :: 学習済みの重みを大きな更新で壊さないためです。前処理も学習時と揃える必要があります。
\`\`\`
`,
  },
  {
    id: 'dl-rnn-1',
    categoryId: 'dl-rnn',
    title: 'RNN と時間方向の逆伝播',
    goal: '再帰構造と BPTT を掴み、長い系列で勾配が消失・爆発する理由を説明できる',
    minutes: 25,
    body: `
# ざっくり言うと

文章・音声・株価のように**順番に意味があるデータ**では、「いま何が来たか」だけでなく「これまで何が来たか」が効きます。

RNN は、**自分の出力を次の入力に回す**ことで、その「これまで」を隠れ状態として持ち回ります。CNN が空間方向に重みを共有したのに対し、**RNN は時間方向に重みを共有**します。

# 再帰の式は 1 本

\`\`\`math
\\mathbf{h}_t = \\tanh(\\mathbf{x}_t \\mathbf{W}_x + \\mathbf{h}_{t-1} \\mathbf{W}_h + \\mathbf{b})
\`\`\`

今回の入力 $\\mathbf{x}_t$ と、**前の時刻の隠れ状態** $\\mathbf{h}_{t-1}$ の両方を受け取ります。全結合層に「前回の出力」を足しただけの形です。

> **試験のポイント** $\\mathbf{W}_x$ と $\\mathbf{W}_h$ は**すべての時刻で同じもの**を使います（時間方向の重み共有）。系列が何ステップ続いてもパラメータは増えません。

\`\`\`diagram:seq
title: 隠れ状態が時間を渡っていく
actors: 時刻 t-1 | 時刻 t
時刻 t-1 -> 時刻 t :: 隠れ状態 h を渡す
時刻 t -> 時刻 t-1 :: 逆伝播では勾配が過去へ戻る
時刻 t-1 -> 時刻 t :: 重み W は全時刻で共通
\`\`\`

# 入出力の形で分類する

| 形 | 例 |
| --- | --- |
| 多対一 | 文章 → 感情（分類） |
| 一対多 | 画像 → 説明文（キャプション生成） |
| 多対多（同じ長さ） | 単語列 → 品詞列 |
| 多対多（違う長さ） | 日本語 → 英語（機械翻訳。seq2seq） |

**双方向 RNN** は、前から読む RNN と後ろから読む RNN を並べて連結します。「文全体を見てから判断する」用途（品詞付け、固有表現抽出）で有効ですが、**未来の入力が必要なので、逐次的な予測や生成には使えません。**

# BPTT：時間方向の逆伝播

学習は、**時間方向に展開して普通の逆伝播をする**だけです。これを **BPTT（Backpropagation Through Time）**といいます。

ただし系列が長いと、展開したネットワークは非常に深くなります。100 ステップの系列は 100 層のネットワークと同じです。ここで問題が起きます。

\`\`\`math
\\frac{\\partial \\mathbf{h}_t}{\\partial \\mathbf{h}_{t-k}} = \\prod_{i} (\\text{活性化の微分} \\times \\mathbf{W}_h)
\`\`\`

**同じ $\\mathbf{W}_h$ が何度も掛かる**のが RNN 特有の事情です。

| $\\mathbf{W}_h$ の大きさ | 何が起きるか | 対処 |
| --- | --- | --- |
| 小さい（1 未満） | 勾配が指数的に 0 へ（**勾配消失**） | LSTM・GRU（ゲート構造） |
| 大きい（1 超） | 勾配が指数的に発散（**勾配爆発**） | **勾配クリッピング** |

> **試験のポイント** RNN では**勾配消失と勾配爆発の両方**が起きます。**爆発にはクリッピング**、**消失には LSTM・GRU** という対処の対応を覚えてください。順伝播型ネットワークでは消失が主に語られますが、RNN では同じ行列が繰り返し掛かるため爆発も現実的な問題になります。

計算量とメモリを抑えるため、系列を適当な長さで区切って逆伝播する **Truncated BPTT** が実務では使われます。順伝播は続けたまま、逆伝播だけを打ち切る点が要点です。

> **よくある勘違い** 勾配消失は「昔の入力を覚えられない」ことと同義ではありません。**順伝播では情報は流れています。** 学習の際に、遠い過去へ勾配が届かず**関係を学べない**のが問題です。

# コードで確かめる

\`\`\`python
import numpy as np

T, H = 20, 4                       # 20 ステップ、隠れ状態 4 次元

for scale in [0.3, 1.2]:
    rng = np.random.default_rng(0)
    W = rng.normal(size=(H, H)) * scale
    grad = np.eye(H)
    for _ in range(T):
        grad = grad @ W            # 同じ行列を掛け続ける
    print(scale, f"{np.abs(grad).max():.3e}")
# 0.3 -> 3.174e-06（消える）   1.2 -> 3.490e+06（爆発する）
\`\`\`

**同じ行列を掛け続けると、指数的に消えるか発散するかのどちらかになる。** これが RNN の学習の難しさの正体です。

# この節のまとめ

- RNN は隠れ状態を次の時刻へ渡すことで「これまで」を持ち回る。**重みは全時刻で共通**
- 入出力の形は多対一・一対多・多対多で整理できる
- 双方向 RNN は未来の入力も使うので、逐次的な生成には使えない
- BPTT は時間方向に展開した逆伝播。長い系列は深いネットワークと同じ
- 同じ $\\mathbf{W}_h$ が繰り返し掛かるため、**勾配消失と勾配爆発の両方**が起きる
- 爆発には勾配クリッピング、消失には LSTM・GRU で対処する

\`\`\`quiz
RNN の重みは時刻ごとに別々ですか :: 同じものを全時刻で共有します。系列が長くなってもパラメータは増えません。
BPTT とは何ですか :: 時間方向に展開したネットワークに対して逆伝播を行うことです（Backpropagation Through Time）。
RNN で勾配爆発が起きたときの定番の対処は :: 勾配クリッピングです。勾配のノルムに上限を設けます。
勾配消失への対処は何ですか :: LSTM や GRU のようにゲートで情報の流れを制御する構造を使うことです。
双方向 RNN が使えない場面はどこですか :: 未来の入力が必要なので、逐次的に次を予測・生成する場面では使えません。
Truncated BPTT は何を打ち切りますか :: 逆伝播だけを一定の長さで打ち切ります。順伝播は続けたまま行います。
\`\`\`
`,
  },
  {
    id: 'dl-rnn-2',
    categoryId: 'dl-rnn',
    title: 'LSTM・GRU と系列変換',
    goal: 'ゲートの役割を数と機能で覚え、seq2seq の仕組みと限界を説明できる',
    minutes: 25,
    body: `
# ざっくり言うと

LSTM は「難しい構造」に見えますが、やっていることは 1 つです。**掛け算で流れていた情報の経路を、足し算に変える。**

前の節で見たとおり、勾配が消えるのは同じ行列が掛かり続けるからでした。そこで LSTM は、記憶を保つ専用の経路（**セル状態**）を作り、そこは基本的に**足し算だけ**で更新します。**ResNet の残差接続と同じ発想**です（歴史的には LSTM のほうが 20 年近く先で、この発想を深さ方向へ持ち込んだのが Highway Network や ResNet だと見るほうが正確です）。ただし残差接続と違い、この経路は忘却ゲートで開閉されます。恒等写像に近づくのは、忘却ゲートが 1 に近いときだけです。

# LSTM のゲート

\`\`\`diagram:flow
title: LSTM の 3 つのゲート
忘却ゲート :: 前のセル状態をどれだけ残すか
入力ゲート :: 新しい情報をどれだけ書き込むか
出力ゲート :: セル状態をどれだけ隠れ状態へ出すか
\`\`\`

| ゲート | 役割 | 活性化 |
| --- | --- | --- |
| 忘却ゲート | 過去の記憶を捨てる割合を決める | シグモイド |
| 入力ゲート | 新しい候補をどれだけ取り込むか決める | シグモイド |
| 出力ゲート | 記憶のどこを外に出すか決める | シグモイド |
| （ゲートではない）新しい候補 | 書き込む内容そのもの | tanh |

**ゲートは上の 3 つだけ**で、4 行目の「新しい候補」はゲートではなく書き込む中身にあたります。

**ゲートにシグモイドを使うのは、0 から 1 の「開き具合」を表したいから**です。ゲートの出力とセル状態は**要素積**で掛け合わされます（[行列積と要素積の違い](textbook/m-linalg-1)がここで効きます）。

セル状態の更新は、おおよそ次の形です。

\`\`\`math
\\mathbf{c}_t = f_t \\odot \\mathbf{c}_{t-1} + i_t \\odot \\mathbf{\\tilde{c}}_t
\`\`\`

**第 1 項が「残す」、第 2 項が「足す」。** 忘却ゲートが 1 に近ければ前の記憶がほぼそのまま通るので、勾配も減衰せずに戻れます。この保持経路を **CEC（定誤差カルーセル）**と呼びます。

> **試験のポイント** LSTM のゲートは **3 つ**（忘却・入力・出力）、GRU は **2 つ**（リセット・更新）です。**数と名前の対応は頻出**なので、ここだけは正確に覚えてください。

# GRU

GRU は LSTM を簡略化したものです。

\`\`\`diagram:compare
title: LSTM と GRU
ゲートは 3 つ（忘却・入力・出力）
ゲートは 2 つ（リセット・更新）
セル状態と隠れ状態を分けて持つ
隠れ状態ひとつにまとめる
パラメータが多い
パラメータが少なく学習が速い
長い依存関係に強いとされる
性能は多くの課題で同等
\`\`\`

GRU の**更新ゲート**は、LSTM の忘却ゲートと入力ゲートを 1 つにまとめたものです（残す割合と入れる割合を足して 1 にする）。**リセットゲート**は、新しい候補を作るときに過去をどれだけ参照するかを決めます。

> **よくある勘違い** 「GRU は LSTM より必ず劣る」わけではありません。**多くの課題で性能は同等**で、パラメータが少ないぶん学習が速いという利点があります。データが少ないときは GRU が有利なこともあります。

# seq2seq：系列を系列に変える

機械翻訳のように**入力と出力の長さが違う**課題では、2 つの RNN を組み合わせます。

\`\`\`diagram:flow
title: seq2seq の流れ
エンコーダ :: 入力系列を読み、最後の隠れ状態にまとめる
文脈ベクトル :: 入力全体を表す固定長のベクトル
デコーダ :: そのベクトルから出力系列を 1 語ずつ生成する
\`\`\`

学習時は、**デコーダに正解の単語を入力する**手法がよく使われます（**教師強制、teacher forcing**）。学習が速く安定しますが、推論時は自分の出力を次の入力にするため、**学習時と推論時で条件が食い違う**という弱点があります（露出バイアス）。

> **試験のポイント** seq2seq の最大の問題は、**入力全体を固定長のベクトルに押し込む**ことです。長い文ほど情報が入りきらず、性能が落ちます。**この情報のボトルネックを解いたのが Attention** で、次の節の主題になります。

生成のしかたにも選択肢があります。毎回もっとも確率の高い語を選ぶ**貪欲法**は速いものの、後から見て良くない選択に陥りがちです。**ビームサーチ**は候補を複数（ビーム幅ぶん）保持しながら進め、より良い系列を探します。

# この節のまとめ

- LSTM は記憶の経路を足し算にすることで、勾配が減衰せずに過去へ届くようにした
- ゲートは LSTM が **3 つ**（忘却・入力・出力）、GRU が **2 つ**（リセット・更新）
- ゲートにシグモイドを使うのは 0〜1 の開き具合を表すため。掛け合わせは**要素積**
- GRU はセル状態を持たず隠れ状態だけ。パラメータが少なく、性能は多くの課題で LSTM と同等
- seq2seq はエンコーダとデコーダの組。**固定長の文脈ベクトルがボトルネック**になる
- 教師強制は学習を安定させるが、学習時と推論時の条件が食い違う（露出バイアス）

\`\`\`quiz
LSTM のゲートは何個で、それぞれ何ですか :: 3 つです。忘却ゲート・入力ゲート・出力ゲートです。
GRU のゲートは何個で、それぞれ何ですか :: 2 つです。リセットゲートと更新ゲートです。
LSTM が勾配消失を緩和できるのはなぜですか :: セル状態が主に足し算で更新され、忘却ゲートが開いていれば勾配が減衰せずに過去へ届くからです。
ゲートにシグモイドを使うのはなぜですか :: 0 から 1 の「開き具合」を表したいからです。セル状態とは要素積で掛け合わせます。
seq2seq の構造上の弱点は何ですか :: 入力全体を固定長のベクトルに押し込むため、長い系列で情報が入りきらないことです。
教師強制（teacher forcing）の利点と弱点は :: 学習が速く安定する一方、推論時は自分の出力を使うため条件が食い違います（露出バイアス）。
\`\`\`
`,
  },
  {
    id: 'dl-attn-1',
    categoryId: 'dl-attn',
    title: '単語の分散表現と Attention',
    goal: '単語をベクトルで表す考え方を掴み、Attention が seq2seq の何を解決したかを説明できる',
    minutes: 25,
    body: `
# ざっくり言うと

言語をニューラルネットワークで扱うには、まず**単語を数のベクトルにする**必要があります。one-hot では語彙の数だけ次元が必要で、しかも「犬」と「猫」が無関係になってしまいます。

そこで、**意味が近い語は近いベクトルになる**ように、低次元のベクトルを学習します。これが**分散表現（単語埋め込み）**です。

そのうえで、この節のもう一つの主題が **Attention** です。前の節で見た seq2seq のボトルネック——**入力全体を固定長のベクトルに押し込む**問題——を、Attention は「**必要なときに入力側を見に行く**」ことで解きます。

# 単語をベクトルにする

| 表し方 | 次元 | 意味の関係 |
| --- | --- | --- |
| one-hot | 語彙数（数万〜） | すべての語が等距離。似ている語も無関係 |
| 分散表現 | 数百 | 意味が近い語が近くに並ぶ |

**word2vec** は分散表現を学習する代表的な手法で、2 つの方式があります。

\`\`\`diagram:compare
title: word2vec の 2 方式
CBOW：周りの語から中心の語を当てる
skip-gram：中心の語から周りの語を当てる
学習が速い
低頻度語に強い
文脈をまとめて入力する
1 語から複数を予測する
\`\`\`

どちらも「**周りに一緒に出てくる語が似ていれば、意味も似ている**」という前提（分布仮説）に立っています。学習された空間では「王 − 男 ＋ 女 ≒ 女王」のような演算が成り立つことが知られています。

ほかに、共起行列を数え上げて分解する **GloVe**、単語を部分文字列（サブワード）に分けて未知語に強くした **fastText** があります。

> **よくある勘違い** word2vec は「文脈によって意味が変わる」ことを表せません。**1 つの語には 1 つのベクトル**しか割り当てられないので、「bank（銀行／土手）」を区別できません。これを解いたのが、後述する BERT のような**文脈化された表現**です。

現在の大規模モデルでは、語をさらに細かい単位に分ける**サブワード分割**（BPE、WordPiece、SentencePiece）が標準です。未知語をほぼなくし、語彙数を抑えられます。

# Attention：どこを見るかに重みを付ける

seq2seq のデコーダが 1 語を出力するとき、入力のすべてを均等に参照する必要はありません。**いま訳している語に対応する入力語を、重点的に見ればよい**はずです。

\`\`\`diagram:flow
title: Attention の 3 手
問い合わせを作る :: いま何を知りたいか（クエリ）
入力の各位置と照合する :: 似ているほど大きなスコア
スコアを重みにして足し合わせる :: 関係が深い位置ほど強く反映される
\`\`\`

用語は **Query・Key・Value** の 3 つです。

| 記号 | 役割 | 比喩 |
| --- | --- | --- |
| Query（Q） | 探す側 | 検索窓に入れた語 |
| Key（K） | 探される側の見出し | 各文書のタイトル |
| Value（V） | 実際に取り出す中身 | 各文書の本文 |

**Q と K の内積で「どれくらい関係があるか」を測り、ソフトマックスで重みに直し、V を重み付けで足す。** これが Attention のすべてです。

\`\`\`math
\\mathrm{Attention}(Q, K, V) = \\mathrm{softmax}(\\frac{QK^{T}}{\\sqrt{d_k}})V
\`\`\`

$\\sqrt{d_k}$ で割るのは、**次元が大きいと内積の値が大きくなりすぎ、ソフトマックスが極端に尖って勾配が消える**のを防ぐためです（スケール化内積注意）。

この 4 段階（内積 → スケール → ソフトマックス → 重み付き和）を、小さな例で追えるようにしました。割り算を切ると、重みが 1 点に集中するのが見えます。

\`\`\`widget:attention
\`\`\`

> **試験のポイント** Attention の重みは**ソフトマックスの出力**なので、足すと 1 になります。「どこをどれだけ見たか」の割合として読めるため、**可視化して対応関係を眺める手がかりになる**という副産物もあります（ただし、その重みをそのまま判断の「根拠」と見てよいかには議論があります）。

# Attention が変えたこと

\`\`\`diagram:compare
title: seq2seq に Attention を足すと
固定長のベクトルに全部を押し込む
入力の各位置を保持し、必要な位置を都度参照する
長い文で情報が落ちる
長い文でも性能が落ちにくい
どこを見たか分からない
重みを見れば対応関係が分かる
デコーダは 1 本の文脈ベクトルだけを受け取る
デコーダは時刻ごとに違う文脈ベクトルを受け取る
\`\`\`

Attention は当初、**RNN の補助**として提案されました。それが「**RNN をやめて Attention だけで作れるのではないか**」という発想に至ったのが、次の節の Transformer です。

# この節のまとめ

- one-hot は次元が大きく語の関係も表せない。分散表現は低次元で意味の近さを表す
- word2vec の CBOW は周辺から中心を、skip-gram は中心から周辺を予測する
- word2vec は 1 語に 1 ベクトルなので、文脈による意味の違いを表せない
- 現在はサブワード分割（BPE・WordPiece）が標準で、未知語をほぼなくせる
- Attention は Q と K の内積でスコアを作り、ソフトマックスで重みにして V を足し合わせる
- $\\sqrt{d_k}$ で割るのは、内積が大きくなりすぎてソフトマックスが尖るのを防ぐため

\`\`\`quiz
分散表現が one-hot より優れている点は何ですか :: 次元が小さく、意味の近い語が近いベクトルになる点です。
CBOW と skip-gram の違いは :: CBOW は周辺の語から中心の語を、skip-gram は中心の語から周辺の語を予測します。
word2vec の限界は何ですか :: 1 つの語に 1 つのベクトルしか割り当てられず、文脈による意味の違いを表せません。
Attention の Query・Key・Value はそれぞれ何にあたりますか :: Query が探す側、Key が探される側の見出し、Value が実際に取り出す中身です。
Attention のスコアを √d_k で割るのはなぜですか :: 次元が大きいと内積が大きくなりすぎ、ソフトマックスが尖って勾配が消えるためです。
Attention は seq2seq の何を解決しましたか :: 入力全体を固定長ベクトルに押し込むボトルネックです。時刻ごとに必要な位置を参照できるようになりました。
\`\`\`
`,
  },
  {
    id: 'dl-attn-2',
    categoryId: 'dl-attn',
    title: 'Transformer と事前学習モデル',
    goal: 'Self-Attention の仕組みと計算量を押さえ、BERT と GPT の学習方法の違いを説明できる',
    minutes: 30,
    body: `
# ざっくり言うと

Transformer は、**RNN を完全にやめて Attention だけで系列を扱う**構造です。論文の題が「Attention Is All You Need」であることが、そのまま主張になっています。

なぜ RNN をやめたかったのか。**RNN は前の時刻の計算が終わらないと次に進めない**ため、系列の長さに比例した時間がかかり、GPU の並列性を活かせないからです。Attention なら全位置を**同時に**計算できます。

**現在の主戦場**にあたる節です。丁寧に読む価値があります。

# Self-Attention

前の節の Attention は「デコーダが入力側を見る」ものでした。**Self-Attention は、同じ系列が自分自身を見ます。**

\`\`\`diagram:flow
title: Self-Attention の流れ
各位置の埋め込みから Q・K・V を作る :: 3 つの重み行列を掛ける
Q と K の内積でスコアを出す :: 全位置対全位置
ソフトマックスで重みにする :: 行ごとに合計 1
V を重み付けで足す :: その位置の新しい表現
\`\`\`

「it が何を指すか」のような**語どうしの関係**を、距離に関係なく 1 ステップで捉えられます。RNN では離れた語の関係を学ぶのに何ステップも勾配を通す必要がありましたが、**Self-Attention では距離が 1**です。

## Multi-Head Attention

Attention を 1 組だけ使うのではなく、**複数の組（ヘッド）に分けて並列に計算し、最後に連結**します。

ヘッドごとに違う関係（文法的な係り受け、共参照、語の意味の近さなど）を捉えられるようになります。**各ヘッドの次元は $d_{model} / h$ に分けられる**ので、ヘッドを増やしても計算量は大きく変わりません。

## 位置エンコーディング

Self-Attention は**語順を見ていません**。入力を並べ替えると出力も同じ順に並べ替わるだけで、**どの位置にあったかという情報はどこにも入っていません**。

そこで、位置に応じたベクトルを埋め込みに**足し込みます**。原論文では異なる周期の正弦・余弦を使い、後のモデルでは学習可能な位置埋め込みも使われます。相対的な位置を扱う方式もあり、たとえば RoPE は埋め込みに足すのではなく、**各層で Q と K に回転を掛ける**形を取ります。

> **試験のポイント** 位置エンコーディングが必要な理由は、「**Self-Attention が語順の情報を持たないから**」です。RNN は構造上、順番に処理するので位置情報が自然に入りますが、Transformer には明示的に与える必要があります。

# 全体の構造

\`\`\`diagram:compare
title: エンコーダとデコーダ
エンコーダ：入力全体を見て表現を作る
デコーダ：出力を 1 語ずつ生成する
Self-Attention は全位置を参照できる
Self-Attention は未来をマスクする
BERT はこちら側だけを使う
GPT はこちら側だけを使う
分類・抽出などの理解に向く
文章生成に向く
\`\`\`

エンコーダの各層は「Self-Attention →（残差接続と層正規化）→ 位置ごとの全結合（FFN）→（残差接続と層正規化）」という並びです。**デコーダの各層はサブ層が 3 つ**で、あいだに**エンコーダの出力を参照する Attention**（cross-attention）が入ります。前の節で見た「デコーダが入力側を見る Attention」は、これにあたります。**残差接続と層正規化は、深く積むために不可欠**でした（[初期化と正規化層](textbook/dl-opt-3)を参照）。

デコーダで未来をマスクするのは、**生成時に自分より後ろの語を見てはいけない**からです。これを**マスク付き Self-Attention** といいます。

> **試験のポイント** Self-Attention の計算量は系列長 $n$ に対して $O(n^2)$ です。全位置対全位置のスコアを作るためで、**長い系列でメモリと計算が急増する**という弱点になります。RNN は $O(n)$ ですが並列化できません。この対比は頻出です。

長い系列への対処として、注意を近傍や一部に限る疎な Attention、線形近似する手法、状態空間モデルなどが提案されています。

# 事前学習と微調整

Transformer が変えたもう一つの点は、**学習の進め方**です。

\`\`\`diagram:flow
title: 2 段階の学習
事前学習 :: 大量のラベルなしテキストで、自己教師あり学習を行う
獲得されるもの :: 言語の一般的な知識と文脈を捉える表現
微調整（ファインチューニング） :: 手元の少量のラベル付きデータで課題に合わせる
\`\`\`

**人がラベルを付けなくてよい**（自己教師あり）ことが、データを桁違いに増やせた理由です。

| モデル | 構造 | 事前学習の課題 |
| --- | --- | --- |
| BERT | エンコーダのみ | **マスク言語モデル**（文中の語を隠して当てる）＋次文予測 |
| GPT | デコーダのみ | **次単語予測**（左から右へ） |
| T5・BART | エンコーダ＋デコーダ | 系列を系列に変換する形で統一 |

**BERT は前後両方を見られる**ので文の理解に強く、分類や抽出に向きます。**GPT は左から右へ生成する**ので文章生成に向きます。**この違いは「どちら向きに文脈を見るか」の 1 点から出てきます。**

> **よくある勘違い** BERT は文章生成のためのモデルではありません。**左から右へ自己回帰的に生成する形で学習していない**ため、そのままでは生成に向きません。**「理解の BERT、生成の GPT」**という対応で覚えてください。

大規模化については、**モデルの大きさ・データ量・計算量を増やすほど性能が向上する**という経験則（スケーリング則）が知られています。近年は、微調整の代わりに**プロンプト**で振る舞いを指示する使い方（few-shot、指示チューニング）や、**重みの更新量だけを低ランクで表す** LoRA（[低ランク近似](textbook/m-linalg-3)）のような軽量な適応が広く使われています。

# この節のまとめ

- Transformer は RNN をやめ、Self-Attention で全位置を並列に処理する
- Self-Attention は自分自身を見る Attention で、離れた語の関係も距離 1 で捉えられる
- Multi-Head は複数の関係を並列に捉える。各ヘッドの次元は $d_{model} / h$
- **位置エンコーディングが要るのは、Self-Attention が語順の情報を持たないから**
- 計算量は系列長に対して $O(n^2)$。RNN は $O(n)$ だが並列化できない
- BERT はエンコーダのみでマスク言語モデル、GPT はデコーダのみで次単語予測

\`\`\`quiz
Transformer が RNN をやめた理由は何ですか :: RNN は前の時刻の計算を待つ必要があり並列化できないためです。Attention なら全位置を同時に計算できます。
Self-Attention の計算量は系列長 n に対してどうなりますか :: O(n²) です。全位置対全位置のスコアを作るためで、長い系列では負担が大きくなります。
位置エンコーディングはなぜ必要ですか :: Self-Attention は語順の情報を持たず、並び替えても結果が変わらないためです。
Multi-Head Attention の利点は何ですか :: ヘッドごとに異なる関係を捉えられることです。各ヘッドの次元は d_model を分けるので計算量は大きく増えません。
BERT と GPT の事前学習課題の違いは :: BERT はマスクした語を当てるマスク言語モデル、GPT は左から右への次単語予測です。
デコーダで未来をマスクするのはなぜですか :: 生成時にはまだ存在しない後ろの語を、学習時にも見せないためです。
\`\`\`
`,
  },
];
