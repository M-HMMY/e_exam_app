import type { TextbookSection } from '../../types';

export const intro: TextbookSection[] = [
  {
    id: 'i-1',
    categoryId: 'intro',
    title: 'この試験はどんな試験か',
    goal: '試験の形式・出題の重心・このアプリの使い方をつかむ',
    minutes: 20,
    body: `
# ざっくり言うと

E資格は、**深層学習を実装できる人かどうか**を確かめる試験です。運転免許でいえば筆記試験にあたり、コードを書かせるのではなく「その仕組みを分かっているか」を選択式で問います。

この試験には他の資格と違う特徴が 1 つあります。**受験するには、先に JDLA が認定した講座を修了しておく必要がある**ことです。つまり「講座で一通り触れた人が、その内容を整理して定着させる場」として設計されています。裏を返すと、講座を終えた直後の断片的な知識を、試験の形に組み直す作業がどうしても必要になります。この教本はそこを埋めるために書かれています。

> **試験のポイント** 出題は「用語を知っているか」ではなく「**なぜそうするのか**」を問う形が中心です。手法の名前と、それが解決した問題を対にして覚えてください。

# 試験の形

| 項目 | 内容 |
| --- | --- |
| 実施方式 | CBT（試験会場のパソコンで受験） |
| 形式 | 多肢選択式のみ。記述も実技もない |
| 問題数・時間 | おおむね 100 問前後を 120 分 |
| 受験条件 | JDLA 認定プログラムを、試験日から遡って一定期間内に修了していること |
| 実施回数 | 年に複数回 |

数字は改訂されることがあります。**申し込みの前に必ず JDLA の公式サイトで最新の要項を確認してください。** この教本が扱うのは中身であって手続きではありません。

1 問あたりに使える時間はおよそ 70 秒です。計算問題で詰まったときに粘ると後半が崩れるので、**分からない問題を飛ばす練習**まで含めて対策だと考えてください。

# 出題の重心

シラバスは大きく 5 つに分かれます。配分は改訂で動きますが、重心の位置は安定しています。

\`\`\`diagram:stack
title: 出題のおおよその重心（下が土台）
深層学習 :: 最も出題が多い。順伝播・最適化・CNN・RNN・Attention・生成・強化学習
機械学習 :: 評価指標と汎化。計算問題が出る
応用数学 :: 線形代数・確率統計・情報理論。上の 2 つを読むための言語
開発・運用環境 :: 分散処理・軽量化・アクセラレータ。用語中心
\`\`\`

積み上げの図にしたのには理由があります。**下の層を飛ばして上だけ覚えると、必ず途中で崩れる**からです。応用数学は単独の出題数こそ多くありませんが、深層学習の問題文がその記号で書かれているため、ここが読めないと上の層すべてで失点します。

> **よくある勘違い** 「数学は苦手だから応用数学は捨てて、深層学習だけ覚える」——これが最も失敗しやすい進め方です。逆に、**応用数学に必要なのは高校数学＋αの記号の読み方だけ**で、証明を書けるようになる必要はありません。投資対効果はむしろ高い分野です。

# この教本の使い方

節ごとに、次の道具が用意してあります。

\`\`\`diagram:flow
title: 1 つの節を読み終えるまで
本文を読む :: 図と数式で仕組みを掴む
一問一答で試す :: 答えを見る前に自分の言葉で言ってみる
手応えを記録する :: 3 段階。迷ったら「もう一度読みたい」
確認問題を解く :: 記録が残り、間違えた問題は復習に回る
\`\`\`

**手応えの記録**が、このアプリの中心です。読了のチェックだけでは「読んだが自信がない節」を拾えません。3 段階で残しておくと、ホーム画面と直前チェックシートがその節を優先的に出してくれます。正直に「もう一度読みたい」を選ぶほど、あとの復習が楽になります。

間違えた確認問題は自動的に**復習**へ回ります。忘れかけた頃に出題する仕組み（間隔反復）が入っているので、復習画面に出たものを片付けるだけで定着します。

試験直前には[直前チェックシート](sheet)を使ってください。全節の「まとめ」「試験のポイント」「よくある勘違い」だけを抜き出して並べたページです。

知らない言葉が出てきたら[用語ミニ辞典](textbook/i-4)で引けます。次の 2 節は、本文を読むための準備運動です。数式に慣れている人は[コードの読み方](textbook/i-3)まで飛ばして構いません。

# この節のまとめ

- E資格は多肢選択式のみ。実装させるのではなく、実装の中身を分かっているかを問う
- 受験には JDLA 認定プログラムの修了が必要。要項は必ず公式サイトで確認する
- 1 問あたり約 70 秒。飛ばす判断も対策のうち
- 出題の重心は深層学習だが、その下に応用数学がある。土台を飛ばすと上が崩れる
- 節を読んだら手応えを 3 段階で記録する。それがこのアプリの復習の起点になる

\`\`\`quiz
E資格を受験するために、事前に済ませておく必要があるものは何ですか :: JDLA が認定したプログラム（講座）の修了です。他の多くの資格試験と違い、誰でもすぐに申し込めるわけではありません。
出題形式は :: 多肢選択式のみです。記述問題も、実際にコードを書かせる実技もありません。
1 問あたりに使える時間はおよそ何秒ですか :: およそ 70 秒です。粘りすぎると後半が崩れるので、飛ばす判断も練習しておきます。
応用数学を捨てるとなぜ危ないのですか :: 深層学習の問題文そのものが数学記号で書かれているため、読めないと上位分野でも失点するからです。
節を読み終えたら何を記録しますか :: 3 段階の手応えです。「もう一度読みたい」を選んだ節は、ホームと直前チェックで優先的に表示されます。
\`\`\`
`,
  },
  {
    id: 'i-2',
    categoryId: 'intro',
    title: '数式の読み方',
    goal: '本文に出てくる数学記号を、解かずに読めるようになる',
    minutes: 25,
    body: `
# ざっくり言うと

深層学習の教材が難しく見える最大の理由は、内容そのものよりも**記号に慣れていないこと**です。書いてあることは「掛けて、足して、微分する」だけなのに、$\\sum$ や $\\partial$ が並んだ瞬間に読む気が失せる。それは理解力の問題ではなく、単に読み方を習っていないだけです。

この節では、**式を解くのではなく読む**ための最低限を用意します。ここを 25 分かけておくと、以降のすべての章で「読めない」という理由で止まることがなくなります。

> **ここだけ覚える** 数式は「文」です。等号は「である」、$\\sum$ は「全部足す」、$\\partial$ は「片方だけ動かしたときの変化」。単語の意味さえ分かれば、あとは日本語と同じ順序で読めます。

# 文字の書き分け

深層学習の式では、**文字の見た目が中身の形を表します。**

| 書き方 | 意味 | 例 |
| --- | --- | --- |
| $x$（細字・小文字） | ただの数（スカラー） | 学習率 $\\eta$、損失 $L$ |
| $\\mathbf{x}$（太字・小文字） | 数を一列に並べたもの（ベクトル） | 入力 $\\mathbf{x}$、バイアス $\\mathbf{b}$ |
| $\\mathbf{W}$（太字・大文字） | 数を縦横に並べたもの（行列） | 重み $\\mathbf{W}$ |
| $\\mathbf{W}^T$ | 行と列を入れ替えたもの（転置） | $\\mathbf{W}^T\\mathbf{x}$ |

**太字かどうかを見るだけで、その文字が 1 個の数なのか、束なのかが分かります。** 教材によって太字ではなく矢印を使うこともありますが、意味は同じです。

# 添字は「何番目か」

$x_i$ は「$x$ の $i$ 番目」です。ベクトルの中身を 1 つ取り出すときに使います。2 つ付く場合は行と列で、$w_{ij}$ は「$i$ 行 $j$ 列の重み」を指します。

上付きは 2 通りの意味で使われるので、**文脈で見分ける**必要があります。

| 書き方 | 意味 |
| --- | --- |
| $x^2$ | 2 乗（数のべき） |
| $\\mathbf{W}^{(2)}$ | 第 2 層の重み（層の番号。かっこ付きで書かれることが多い） |
| $\\mathbf{x}^{(i)}$ | $i$ 番目のデータ（サンプルの番号） |

> **よくある勘違い** $\\mathbf{W}^{(2)}$ を「$\\mathbf{W}$ の 2 乗」と読んでしまう誤りは非常に多いです。**かっこが付いていたら番号**、付いていなければべき乗、と機械的に判断してください。

# 総和と総乗

$\\sum$（シグマ）は「全部足す」、$\\prod$（パイ）は「全部掛ける」です。下に「どこから」、上に「どこまで」を書きます。

\`\`\`math
\\sum_{i=1}^{n} x_i = x_1 + x_2 + \\cdots + x_n
\`\`\`

読み方は「$i$ を 1 から $n$ まで動かしながら $x_i$ を全部足す」。プログラムでいえば for ループそのもので、次のコードと同じ意味です。

\`\`\`python
total = 0
for i in range(1, n + 1):
    total += x[i]
\`\`\`

**$\\sum$ を見たら for ループに読み替える。** これだけで、平均も分散も損失関数も一気に読めるようになります。

# 微分は「変化のしかた」

$\\frac{dy}{dx}$ は「$x$ をほんの少し動かしたら $y$ がどれだけ動くか」です。値そのものではなく、**傾き**を表します。

変数が複数あるときは、**片方だけ動かして他は止めておく**という約束にします。これが偏微分で、記号が $d$ から $\\partial$（ラウンドディー）に変わります。

\`\`\`math
\\frac{\\partial L}{\\partial w}
\`\`\`

これは「$w$ だけを少し動かしたときの $L$ の変化」と読みます。深層学習でやっていることは、煎じ詰めればこれだけです。**「この重みを少し増やすと、損失は増えるか減るか」を全部の重みについて調べ、減る方向へ動かす。** その「調べる」が偏微分で、「動かす」が学習です。

すべての重みについての偏微分を並べたベクトルを**勾配**といい、$\\nabla L$（ナブラ）と書きます。

> **試験のポイント** 勾配は「損失が最も**増える**向き」を指します。だから重みは勾配の**逆向き**に動かします。更新式の $-\\eta \\nabla L$ のマイナスはこれが理由で、符号を問う出題があります。

# よく出る記号の残り

| 記号 | 読み | 意味 |
| --- | --- | --- |
| $\\hat{y}$ | ワイハット | モデルの予測値（$y$ は正解） |
| $\\mathbb{E}[X]$ | 期待値 | 平均的にいくつになるか |
| $P(A \\mid B)$ | 条件付き確率 | $B$ が起きたときの $A$ の確率 |
| $\\arg\\max_x f(x)$ | アーグマックス | $f$ を最大にする $x$ **そのもの**（最大値ではない） |
| ノルム | ベクトルの長さ | $L_2$ ノルムなら各成分の 2 乗和の平方根 |
| $\\propto$ | 比例 | 定数倍を無視して等しい |
| $\\eta$ | イータ | 学習率としてよく使われる |

> **よくある勘違い** $\\max f(x)$ と $\\arg\\max f(x)$ は別物です。前者は**一番大きい値**、後者は**それを与える入力**。分類器が「どのクラスか」を答えるときに使うのは後者です。

# 実際に読んでみる

線形回帰の損失（平均二乗誤差）は次のように書かれます。

\`\`\`math
L = \\frac{1}{n} \\sum_{i=1}^{n} (y^{(i)} - \\hat{y}^{(i)})^2
\`\`\`

ここまでの道具だけで、左から順に読めます。「$i$ 番目のデータについて、正解 $y$ と予測 $\\hat{y}$ の差を取り、2 乗して、全部足して、データ数 $n$ で割る」。つまり**ズレの二乗の平均**です。

2 乗する理由も式が語っています。差のままだと $+3$ と $-3$ が打ち消し合ってしまう。2 乗すれば符号が消え、しかも大きなズレほど重く効きます。

**式は覚えるものではなく、読むものです。** 意味が読めれば、忘れてもその場で組み立て直せます。

# この節のまとめ

- 太字かどうかで、スカラー・ベクトル・行列を見分ける
- 下付きは「何番目」、上付きはべき乗か層番号。かっこが付いていたら番号
- $\\sum$ は for ループ。「全部足す」と読み替える
- $\\partial$ は「片方だけ動かしたときの変化」。勾配は損失が増える向きなので、逆向きに進む
- $\\arg\\max$ は最大値ではなく、最大にする入力そのもの
- 式は暗記せず読む。読めれば忘れても組み立て直せる

\`\`\`quiz
$\\mathbf{W}^{(2)}$ はどう読みますか :: 「第 2 層の重み行列」です。かっこ付きの上付きは層番号やサンプル番号で、べき乗ではありません。
$\\sum_{i=1}^{n} x_i$ をプログラムに直すと :: 変数を 0 で初期化して、i を 1 から n まで動かしながら x[i] を足していく for ループです。
$\\partial L / \\partial w$ は何を表しますか :: 他の変数を止めたまま w だけをわずかに動かしたときに、損失 L がどれだけ変化するかです。
重みを勾配の逆向きに動かすのはなぜですか :: 勾配は損失が最も増える向きを指すからです。減らしたいので符号を反転させます。
$\\max$ と $\\arg\\max$ の違いは :: $\\max$ は一番大きい「値」、$\\arg\\max$ はそれを与える「入力」です。分類の予測ラベルは後者です。
平均二乗誤差で差を 2 乗するのはなぜですか :: 符号を消して打ち消し合いを防ぐためと、大きなズレをより重く扱うためです。
\`\`\`
`,
  },
  {
    id: 'i-3',
    categoryId: 'intro',
    title: 'コードの読み方（NumPy の最小限）',
    goal: 'shape・ブロードキャスト・行列積・axis の 4 つを押さえ、コード問題を形で解けるようにする',
    minutes: 25,
    body: `
# ざっくり言うと

E資格ではコードの穴埋めが出ます。ただし**書かせるのではなく、読ませて選ばせる**形です。必要なのは Python を書く力ではなく、**NumPy の 4 つの約束を知っていること**だけです。

その 4 つとは、形（shape）、ブロードキャスト、行列積と要素積の違い、そして axis。この節ではそれだけをやります。逆にいうと、ここを曖昧にしたまま進むと、コード問題は毎回勘で選ぶことになります。

> **ここだけ覚える** NumPy の問題は、ほぼすべて「**形が合うかどうか**」で答えが決まります。値の計算をする必要はありません。shape を追いかけてください。

# 配列と形（shape）

NumPy の配列には必ず「形」があります。

\`\`\`python
import numpy as np

x = np.array([1, 2, 3])            # shape = (3,)      1 次元
W = np.array([[1, 2, 3],
              [4, 5, 6]])          # shape = (2, 3)    2 行 3 列
\`\`\`

深層学習では、この形が**（バッチサイズ, 特徴量の数）**という意味を持ちます。画像なら**（バッチ, チャネル, 高さ, 幅）**の 4 次元になります。

\`\`\`diagram:stack
title: よく出る shape の読み方
(32, 784) :: 32 枚の画像を、それぞれ 784 個の数に平らに伸ばしたもの
(32, 1, 28, 28) :: 32 枚・1 チャネル（白黒）・縦 28・横 28 のまま扱うもの
(32, 10) :: 32 件それぞれについて、10 クラス分のスコア
\`\`\`

# ブロードキャスト

形の違う配列同士でも、**足りない方が自動的に引き伸ばされて**計算できます。これがブロードキャストです。

\`\`\`python
x = np.ones((3, 4))          # (3, 4)
b = np.array([1, 2, 3, 4])   # (4,)
x + b                        # (3, 4)  b が 3 行ぶんコピーされたように振る舞う
\`\`\`

規則は「**右端の次元から見比べて、同じか、どちらかが 1 なら通る**」です。バイアスの加算がループなしで書けるのはこの仕組みのおかげです。

> **よくある勘違い** ブロードキャストは実際にメモリをコピーしているわけではありません。ただし**形が合ってしまうために、間違った計算が例外を出さずに通ってしまう**ことがあります。$(3,1)$ と $(1,3)$ を足すと $(3,3)$ になる、というのが典型例です。

# 行列積と要素積

ここが最も間違えやすい箇所です。

\`\`\`diagram:compare
title: アスタリスクとアットマークはまったく別の演算
actors: A * B（要素積） | A @ B（行列積）
同じ位置どうしを掛ける :: 形は変わらない
内側の次元をつぶして足す :: 形が変わる
(2,3) と (2,3) から (2,3) :: 形が一致していること（またはブロードキャスト可能）が条件
(2,3) と (3,4) から (2,4) :: 左の列数と右の行数が一致していることが条件
アダマール積とも呼ぶ :: LSTM のゲートなどで使う
np.dot / np.matmul と同じ :: 全結合層の本体
\`\`\`

全結合層の計算は行列積で書きます。

\`\`\`math
\\mathbf{y} = \\mathbf{x}\\mathbf{W} + \\mathbf{b}
\`\`\`

\`\`\`python
x = np.random.randn(32, 784)   # (32, 784)
W = np.random.randn(784, 128)  # (784, 128)
b = np.zeros(128)              # (128,)
y = x @ W + b                  # (32, 128)
\`\`\`

**784 という内側の数が消えて、(32, 128) になる。** この「内側がつぶれる」感覚が掴めれば、層をいくつ重ねても形を追えます。

# axis は「つぶす向き」

\`sum\` や \`max\` に付ける \`axis\` は、**どの軸をつぶすか**を指定します。

\`\`\`python
a = np.array([[1, 2, 3],
              [4, 5, 6]])      # (2, 3)

a.sum(axis=0)   # [5, 7, 9]     (3,)  行方向につぶす＝列ごとの合計
a.sum(axis=1)   # [6, 15]       (2,)  列方向につぶす＝行ごとの合計
\`\`\`

> **よくある勘違い** 「axis=0 は行」ではなく「**axis=0 をつぶす**」です。指定した軸が結果から消える、と覚えてください。\`keepdims=True\` を付けると、消える代わりに長さ 1 で残ります。ブロードキャストと組み合わせるときはこれが要ります。

# 実例：ソフトマックス

ここまでの道具で、頻出のソフトマックス実装が読めます。

\`\`\`python
def softmax(x):                              # x: (バッチ, クラス数)
    x = x - x.max(axis=1, keepdims=True)     # (1) 最大値を引く
    e = np.exp(x)
    return e / e.sum(axis=1, keepdims=True)  # (2) 行ごとの合計で割る
\`\`\`

- (1) の引き算は**オーバーフロー対策**です。指数関数は入力が大きいとすぐ桁があふれます。全要素から同じ値を引いても結果は変わらないので、最大値を引いて安全な範囲に収めています。**この行の意図を問う出題は頻出です。**
- (2) で \`axis=1\` かつ \`keepdims=True\` なのは、**データ 1 件ごとに**合計を 1 にしたいからです。ここを \`axis=0\` にすると、クラスをまたいで正規化してしまい意味が変わります。

# PyTorch はこれだけ

フレームワークの問題も出ますが、問われるのは骨格だけです。

\`\`\`python
model = MyNet()
optimizer = torch.optim.Adam(model.parameters(), lr=1e-3)

for x, t in loader:
    optimizer.zero_grad()      # (1) 前回の勾配を消す
    y = model(x)               # (2) 順伝播
    loss = criterion(y, t)     # (3) 損失を計算
    loss.backward()            # (4) 逆伝播で勾配を求める
    optimizer.step()           # (5) 勾配を使って重みを更新
\`\`\`

この 5 行の**順序**が問われます。特に (1) を忘れると勾配が前回ぶんに足し込まれてしまう、という点は頻出です。

> **試験のポイント** zero_grad → 順伝播 → 損失 → backward → step。この並びと、それぞれが何をするかを言えるようにしておいてください。

# この節のまとめ

- NumPy の問題は形（shape）を追えば解ける。値を計算する必要はない
- ブロードキャストは右端の次元から見比べ、同じか片方が 1 なら通る
- 要素積は形が変わらない、行列積は内側の次元が消える
- \`axis=k\` は「k 番目の軸をつぶす」。\`keepdims=True\` で長さ 1 として残せる
- ソフトマックスで最大値を引くのはオーバーフロー対策。結果は変わらない
- 学習ループは zero_grad → 順伝播 → 損失 → backward → step の順

\`\`\`quiz
shape が (32, 784) の配列と (784, 128) の配列を行列積で掛けると、結果の形は :: (32, 128) です。内側の 784 が消えます。
要素積と行列積の違いは :: 要素積は同じ位置どうしを掛けるので形が変わりません。行列積は内側の次元がつぶれて形が変わります。
a.sum(axis=1) は何をしますか :: 1 番目の軸（列方向）をつぶして、行ごとの合計を出します。指定した軸が結果から消えます。
keepdims=True は何のために付けますか :: つぶした軸を長さ 1 として残し、元の配列とブロードキャストで計算できるようにするためです。
ソフトマックスで最大値を引くのはなぜですか :: 指数関数のオーバーフローを防ぐためです。全要素から同じ値を引いても出力は変わりません。
optimizer.zero_grad() を忘れるとどうなりますか :: 勾配が前回の値に足し込まれ、意図しない大きさで更新されてしまいます。
\`\`\`
`,
  },
  {
    id: 'i-4',
    categoryId: 'intro',
    title: '用語ミニ辞典',
    goal: '章をまたいで何度も出る用語を、必要なときに引けるようにする',
    minutes: 20,
    body: `
# ざっくり言うと

本文が説明なしで使う言葉を、ここにまとめておきます。**通読する必要はありません。** 読んでいて詰まったときに戻ってきてください。

各章の本文では、その章で初めて出る用語には必ずその場で説明を付けています。ここに載せているのは、章をまたいで何度も出てくる言葉です。

# 学習の枠組み

| 用語 | 意味 |
| --- | --- |
| 教師あり学習 | 入力と正解の組を与えて学ばせる。分類・回帰 |
| 教師なし学習 | 正解を与えず、データの構造だけを見つけさせる。クラスタリング・次元削減 |
| 強化学習 | 報酬を頼りに試行錯誤で方策を学ぶ |
| 自己教師あり学習 | データ自身から正解を作る（文の一部を隠して当てさせる等）。BERT の学習法 |
| 汎化 | 訓練で見ていないデータでも正しく動くこと。機械学習の目的そのもの |
| 過学習 | 訓練データに合わせすぎて、新しいデータで性能が落ちること |
| 未学習 | そもそも訓練データにすら合っていない状態 |
| バイアスとバリアンス | 誤差の 2 つの原因。単純すぎる（バイアス大）／敏感すぎる（バリアンス大） |

# データの扱い

| 用語 | 意味 |
| --- | --- |
| 訓練データ | 重みを更新するために使うデータ |
| 検証データ | ハイパーパラメータを選ぶために使うデータ。重みの更新には使わない |
| テストデータ | 最後に 1 度だけ性能を測るデータ。触るのは最後 |
| バッチ | 一度にまとめて処理するデータの束 |
| ミニバッチ | 全データを小分けにした束。実務ではこれが標準 |
| エポック | 訓練データ全体を 1 周すること |
| イテレーション | 重みを 1 回更新すること。1 エポック ＝ データ数 ÷ バッチサイズ 回 |
| データ拡張 | 反転・切り抜きなどで訓練データを水増しすること |

# ネットワークの部品

| 用語 | 意味 |
| --- | --- |
| ニューロン（ユニット） | 入力の重み付き和を取り、活性化関数に通す最小単位 |
| 全結合層 | 前の層のすべてと繋がった層。線形変換 $\\mathbf{xW}+\\mathbf{b}$ |
| 活性化関数 | 層の出力を曲げる関数。これがないと何層重ねても 1 層と同じになる |
| パラメータ | 学習で更新される値。重みとバイアス |
| ハイパーパラメータ | 人が決める値。学習率、層数、バッチサイズなど。学習では変わらない |
| 損失関数 | 予測と正解のズレを 1 つの数にしたもの。これを小さくするのが学習 |
| 順伝播 | 入力から出力へ計算を進めること |
| 逆伝播 | 出力側の誤差から各重みの勾配を求めること |
| 勾配消失 | 逆伝播の途中で勾配が小さくなりすぎ、手前の層が学習しなくなる現象 |
| 勾配爆発 | 逆に大きくなりすぎて発散する現象 |

# 最適化まわり

| 用語 | 意味 |
| --- | --- |
| 勾配降下法 | 勾配の逆向きに重みを動かす、学習の基本手続き |
| 学習率 | 1 回にどれだけ動かすか。大きすぎると発散、小さすぎると進まない |
| SGD | ミニバッチごとに更新する勾配降下法 |
| モーメンタム | 前回までの更新方向を慣性として引き継ぐ工夫 |
| Adam | モーメンタムと、勾配の大きさによる学習率調整を組み合わせた定番 |
| 正則化 | モデルの自由度を抑えて過学習を防ぐ工夫の総称 |
| ドロップアウト | 訓練中にユニットをランダムに無効化する正則化 |
| バッチ正規化 | 層の出力の分布を整えて学習を安定させる手法 |

# 畳み込みと系列

| 用語 | 意味 |
| --- | --- |
| カーネル（フィルタ） | 画像の上を滑らせる小さな重みの窓 |
| ストライド | カーネルを動かす歩幅 |
| パディング | 端を埋めて出力サイズを保つこと |
| チャネル | 画像の色数など、同じ位置に重なっている情報の枚数 |
| プーリング | 領域をまとめて縮める操作。位置ずれに強くなる |
| 受容野 | 出力の 1 点が、元の入力のどれだけの範囲を見ているか |
| 系列データ | 順序に意味があるデータ。文章・音声・時系列 |
| 埋め込み（Embedding） | 単語などの離散的なものを、意味を持った数のベクトルに置き換えること |
| Attention | 入力のどこを重く見るかに重みを付ける仕組み |

# 評価

| 用語 | 意味 |
| --- | --- |
| 混同行列 | 予測と正解の組み合わせを 2×2（多クラスなら $n \\times n$）で並べた表 |
| 正解率（Accuracy） | 全体のうち当たった割合。不均衡データでは当てにならない |
| 適合率（Precision） | 陽性と予測したもののうち、本当に陽性だった割合 |
| 再現率（Recall） | 本当に陽性のもののうち、拾えた割合 |
| F 値 | 適合率と再現率の調和平均 |
| ROC 曲線・AUC | しきい値を動かしたときの性能を面積で表したもの |
| 交差検証 | データを分割して、訓練と検証の組を入れ替えながら複数回評価する方法 |

# 運用

| 用語 | 意味 |
| --- | --- |
| 推論 | 学習済みモデルを使って予測すること |
| データ並列 | 同じモデルを複数の装置に置き、データを分けて処理する |
| モデル並列 | 大きすぎるモデルを分割して複数の装置に置く |
| 量子化 | 重みを低いビット数で表して軽くすること |
| 蒸留 | 大きなモデルの出力を教師にして、小さなモデルを学習させること |
| 枝刈り（プルーニング） | 重要でない結合を削って軽くすること |
| エッジ推論 | 手元の機器で推論すること。通信せずに済み、遅延も小さい |
`,
  },
];
