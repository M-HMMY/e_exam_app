# CLAUDE.md

このファイルは、このリポジトリで作業するときに最初に読む前提です。

## このアプリは何か

E資格（JDLA Deep Learning for ENGINEER）の学習アプリ。**教本を軸にした学習アプリ**であって、問題集アプリではありません。

土台は `fe_exam_app`（基本情報技術者試験の学習アプリ）から引き継いでいます。画面・記法・運用の作法はそちらと共通なので、迷ったら向こうの実装を見れば答えがあります。

## 最優先の方針

**「通読できる教本」が最優先の要件です。** 演習機能より教本の質を優先してください。これは `fe_exam_app` で確立した方針で、このアプリでも同じです。

その上で、E資格には固有の事情が 2 つあります。

1. **受験者は認定プログラムを修了済み**です。まったくの初心者ではありませんが、**講座で断片的に触れた知識が整理されていない**状態で来ます。教本の役目は「教える」ことより「つなげる」ことです。
2. **数式とコードから逃げられません。** ただし、必要なのは式を解く力ではなく**読む力**です。入門編の [数式の読み方](src/data/textbook/intro.ts) と [コードの読み方](src/data/textbook/intro.ts) がその前提を作っているので、本編はそこに依存して書けます。

## 変えないこと

- **ランタイム依存は React だけ。** ルータ・Markdown・数式・図はすべて自前。ライブラリを足す前に、本当に必要か検討する
- **外部通信をしない。** 学習記録は localStorage のみ。サーバもアカウントもない
- **オフラインで動く。** PWA として成立させる
- TypeScript は strict（未使用変数もエラー）

## 節の書き方

本文は Markdown サブセットです。1 つの節は次の骨格で書きます。

```
# ざっくり言うと          ← 日常語で「何の話か」。式は出さない
（本論。見出し・表・図・数式・コード）
> **試験のポイント** …     ← 暗記すべき事項
> **よくある勘違い** …     ← 先回りして潰す誤解
# この節のまとめ           ← 箇条書き 5〜6 個
```quiz                    ← 一問一答（採点も記録もしない）
問い :: 答え
```
```

「この節のまとめ」「試験のポイント」「よくある勘違い」の 3 つは、[src/lib/digest.ts](src/lib/digest.ts) が自動で抜き出して**直前チェックシート**に並べます。**書式を崩すと拾われません。**

### 使える記法

| 記法 | 用途 |
| --- | --- |
| `# ## ###` | 見出し |
| `- ` `1. ` | 箇条書き（2 スペースで 1 段ネスト） |
| `\|` の表 | 表 |
| `> ` | 引用（＝ポイント枠） |
| `**強調**` | 太字 |
| `` `コード` `` | 等幅 |
| `[文字](textbook/m-linalg-1)` | アプリ内リンク（外部 URL は不可） |
| `$...$` | 行内数式 |
| ` ```math ` | 別行立ての数式（1 行 1 式） |
| ` ```python ` | コードブロック |
| ` ```diagram:種類 ` | 図（flow / stack / tree / matrix / cycle / seq / bits / compare） |
| ` ```widget:id ` | 対話ウィジェット |
| ` ```quiz ` | 一問一答（`問い :: 答え`） |

数式で使える記法は [src/lib/math.tsx](src/lib/math.tsx) の冒頭に書いてあります。`^{}` `_{}` `\frac{}{}` と、SYMBOL 表にあるギリシャ文字・記号だけです。足りなくなったら KaTeX への差し替えを検討してください（この 1 ファイルの置き換えで済みます）。

### バックスラッシュの落とし穴（重要）

本文は TypeScript の**テンプレートリテラル**の中にあります。そのため次の 3 つはエスケープが必要です。

| 書きたいもの | ファイルに書く形 |
| --- | --- |
| `\sum` | `\\sum` |
| バックティック | バックスラッシュ + バックティック |
| `${` | `\${` |

`\\` を `\` と書くと、`\sum` が `sum` になって画面に出ます。**`npm run check` がこれを検出します**が、そもそも壊さないのが確実です。

さらに厄介なことに、**bash のヒアドキュメント（`cat > file <<'EOF'`）はバックスラッシュを勝手に落とします。** この環境で実際に何度も壊しました。バックスラッシュを含むファイルを書くときは、

- **Write ツールを使う**（いちばん確実）、または
- **Python の raw 文字列**（`r'''...'''`）から書き出す

のどちらかにしてください。教本を大量に書くときは後者が便利です。`fe_exam_app` の作業では、Markdown を Python の raw 文字列で書き、エスケープ処理をしてから `.ts` に書き出す小道具を使いました。

## 問題の書き方

[src/data/questions/math.ts](src/data/questions/math.ts) の先頭コメントに約束を書いてあります。要点は次のとおりです。

- **`sectionId` は必ず付ける。** 解説から教本へ戻る導線と、節ごとの出題に使う
- 解説では、正解の理由だけでなく**誤答がなぜ誤りか**にも触れる
- 実装を問う場合は `code` にコード断片を置き、空欄は `___` で示す
- 通常の文字列リテラルでもバックスラッシュは 2 つ重ねる

## 作業の作法

### 必ず通すもの

```
npm run check   # データの整合性（ID 重複・参照切れ・記法の崩れ・数式のエスケープ漏れ）
npx tsc --noEmit
npm run build
```

`npm run check` は「型では防げない食い違い」を捕まえるためのものです。**新しい不整合の型を見つけたら、直すついでに [scripts/check.ts](scripts/check.ts) へ検査を足してください。** 実際、このスクリプトは書いた直後に自分が仕込んだドリルの不具合（選択肢が 2 個になる場合がある）を捕まえています。

### 内容の正しさ

**書いた本人が読み返しても間違いは見つかりません。** `fe_exam_app` では、新規に書いた約 7 万字を独立した 5 つのエージェントに読ませて、**16 件の誤り**（うち 4 件は重大な事実誤り）を発見しました。自己レビューでは 1 件も見つかっていません。

まとまった量を書いたら、**書いた人とは別の目で検証する工程を必ず入れてください。**

### 事実の扱い

試験の要項（問題数・時間・受験条件・出題比率）は**改訂されます。** 断定的に書かず、「おおむね」と添えたうえで「最新の要項は JDLA の公式サイトで確認」と促してください。入門編の [i-1](src/data/textbook/intro.ts) がその書き方の見本です。

技術的な内容も、シラバス改訂で範囲が動きます。特に生成モデル・Transformer 周辺は近年強化されているので、古い教材の記述をそのまま持ち込まないこと。

### 公開のルール

公開リポジトリにする前提です。

- **コミットの署名は GitHub の noreply アドレスを使う。** 実メールアドレスはコミット履歴に永久に残ります
- 個人情報・ローカルパス・API キーを含めない

## いまの状態

| 項目 | 状況 |
| --- | --- |
| アプリの仕組み | 完成。型チェック・ビルド・整合性チェックすべて通る |
| 教本 | 47 節（入門編 4・応用数学 8・機械学習 6・深層学習 25・開発運用環境 4）。**全章を書き終えた** |
| 確認問題 | 200 問（応用数学 35・深層学習 115・機械学習 28・開発運用環境 22。全問が節にひも付き。1 節あたり 4 問以上） |
| 計算ドリル | 14 種類（行列積・内積・全結合層のパラメータ数・イテレーション数・混同行列・畳み込みの出力サイズ・畳み込みのパラメータ数・割引収益・Q 学習の更新・IoU・モデルのメモリ量・エントロピー・ベイズの定理・Adam のモーメント） |
| 対話ウィジェット | 10 個（計画していたものはすべて実装済み。行列積 / エントロピー / 確率分布 / 活性化関数 / ソフトマックス / 勾配降下 / 畳み込み / Attention / 混同行列 / ROC） |

書き終えた章の内訳は次のとおりです。

| 章 | 節 |
| --- | --- |
| 線形代数 | ベクトルと行列 / 固有値と固有ベクトル / 特異値分解と低ランク近似 |
| 確率・統計 | 確率の基礎とベイズの定理 / 代表的な確率分布 / 最尤推定・MAP・ベイズ |
| 情報理論 | 自己情報量とエントロピー / 交差エントロピーと KL ダイバージェンス |
| 順伝播型ネットワーク | 全結合層と順伝播 / 活性化関数 / 誤差逆伝播法 |
| 最適化 | 勾配降下法とミニバッチ / モーメンタムから Adam へ / 重みの初期化と正規化層 |
| 正則化 | 過学習と L1・L2 正則化 / ドロップアウト・早期終了・データ拡張 |
| 機械学習の基礎 | 学習の種類と全体像 / 線形回帰とロジスティック回帰 / SVM・決定木・アンサンブル / 教師なし学習 |
| 実用的な方法論 | データの分け方と交差検証 / 評価指標 |
| 畳み込みニューラルネットワーク | 畳み込みとプーリング / 代表的な CNN アーキテクチャ |
| 回帰結合型ニューラルネットワーク | RNN と時間方向の逆伝播 / LSTM・GRU と系列変換 |
| Attention と自然言語処理 | 単語の分散表現と Attention / Transformer と事前学習モデル |
| 生成モデル | オートエンコーダと VAE / GAN と派生モデル / 拡散モデルと自己回帰モデル |
| 強化学習 | 強化学習の枠組みと価値関数 / 価値ベースの学習と DQN / 方策ベースの手法と発展 |
| その他の重要分野 | 物体検出 / セグメンテーション / 音声処理 / 距離学習と学習の工夫 / 説明性とグラフニューラルネットワーク |
| 開発・運用環境 | 分散学習 / アクセラレータとエッジ / モデルの軽量化 / 実装と運用 |

数式の表示は [src/lib/math.tsx](src/lib/math.tsx) が担い、**使える記号と命令の一覧は [src/lib/mathSymbols.ts](src/lib/mathSymbols.ts)** にあります。表に無い命令は記号にならず名前がそのまま画面に出るので、`npm run check` がエラーとして検出します。新しい記号が要るときは、まずこの表に足してください。

## 次にやること

優先順に並べています。

1. **ブラウザでの通し確認** … `launch.cmd`（デスクトップのショートカット）で開発サーバが立ち上がります。`npm run check` はウィジェットを描画して NaN や崩れを検出しますが、**見た目そのものは人が見るしかありません**
2. **確認問題をさらに厚くする** … 200 問。本番はおおむね 100 問前後なので、模試を何度も回すなら 250〜300 問ほしいところです。**新しく足すときは、まず既存問題との重複を疑ってください**（レビューで 8 問の重複が見つかりました）。`scripts/` に節ごとの問題数を数える使い捨てスクリプトを書けば、薄い節はすぐ洗い出せます
3. **ウィジェットの磨き込み** … 10 個そろいましたが、数値入力欄は「1e」のような入力途中の文字列が残る挙動があります（値は壊れません）。気になるなら文字列で状態を持つ形に直せます

教本 47 節・確認問題 200 問・計算ドリル 14 種類・対話ウィジェット 10 個で、当初の計画はひと通り埋まりました。

新しく書いたものは、必ず `npm run check` を通したうえで、**書いた人とは別の目で検証**してください（この教本の 200 件近い記述の誤りは、すべて独立したレビューで見つかったものです）。
